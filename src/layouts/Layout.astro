---
import '../styles/global.css';
import { SITE } from '../data/seo';

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
  ogImageAlt?: string;
  ogType?: 'website' | 'article';
  noindex?: boolean;
  article?: {
    datePublished: string;
    dateModified: string;
    author: string;
    section?: string;
    tags?: string[];
  };
  jsonLd?: Record<string, unknown> | Record<string, unknown>[];
}

const {
  title = 'AI Native Business — ainative',
  description = 'AI Native Business — the book and the companion software. A personal research project by Manav Sehgal into building AI-native businesses. Open source, local-first, and free.',
  ogImage = '/og-image.png',
  ogImageAlt = SITE.ogImageAlt,
  ogType = 'website',
  noindex = false,
  article,
  jsonLd,
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const jsonLdSchemas = jsonLd ? (Array.isArray(jsonLd) ? jsonLd : [jsonLd]) : [];
---
<!doctype html>
<html lang="en">
  <head>
    <!-- No-FOUC theme initialization (must run before any rendering) -->
    <script is:inline>
      (function() {
        var s = localStorage.getItem('theme');
        var t = (s === 'light' || s === 'dark')
          ? s
          : (matchMedia('(prefers-color-scheme: light)').matches ? 'light' : 'dark');
        document.documentElement.setAttribute('data-theme', t);
      })();
    </script>

    <!-- No-FOUC reader-settings init for /field-notes/ articles -->
    <script is:inline>
      (function () {
        try {
          var fs = localStorage.getItem('afn:font-scale');
          var lh = localStorage.getItem('afn:line-height');
          var rt = localStorage.getItem('afn:reader-theme');
          var em = localStorage.getItem('afn:explain-mode');
          if (fs === 'S' || fs === 'M' || fs === 'L') document.documentElement.dataset.fontScale = fs;
          if (lh === 'compact' || lh === 'default' || lh === 'relaxed') document.documentElement.dataset.lineHeight = lh;
          if (rt === 'light' || rt === 'sepia' || rt === 'dark') document.documentElement.dataset.readerTheme = rt;
          if (em === 'hide' || em === 'margin' || em === 'inline') document.documentElement.dataset.explainMode = em;
        } catch (e) { /* no-op */ }
      })();
    </script>

    <!-- Google tag (deferred until idle or first interaction) -->
    <script is:inline>
      (function() {
        var loaded = false;
        function loadGA() {
          if (loaded) return;
          loaded = true;
          var s = document.createElement('script');
          s.src = 'https://www.googletagmanager.com/gtag/js?id=G-59FCBN11J6';
          s.async = true;
          document.head.appendChild(s);
          window.dataLayer = window.dataLayer || [];
          function gtag(){dataLayer.push(arguments);}
          window.gtag = gtag;
          gtag('js', new Date());
          gtag('config', 'G-59FCBN11J6');
        }
        if ('requestIdleCallback' in window) {
          requestIdleCallback(loadGA, { timeout: 3000 });
        } else {
          setTimeout(loadGA, 3000);
        }
        ['mousedown','keydown','touchstart','scroll'].forEach(function(e) {
          addEventListener(e, loadGA, { once: true, passive: true });
        });
      })();
    </script>

    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />

    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="author" content="ainative" />
    <link rel="canonical" href={canonicalURL} />
    {noindex && <meta name="robots" content="noindex, nofollow" />}

    <!-- Open Graph -->
    <meta property="og:type" content={ogType} />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={new URL(ogImage, Astro.site)} />
    <meta property="og:image:alt" content={ogImageAlt} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:type" content="image/png" />
    <meta property="og:site_name" content="ainative" />
    <meta property="og:locale" content="en_US" />

    {article && (
      <>
        <meta property="article:published_time" content={article.datePublished} />
        <meta property="article:modified_time" content={article.dateModified} />
        <meta property="article:author" content={article.author} />
        {article.section && <meta property="article:section" content={article.section} />}
        {article.tags?.map((tag) => <meta property="article:tag" content={tag} />)}
      </>
    )}

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content="@manavsehgal" />
    <meta name="twitter:creator" content="@manavsehgal" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={new URL(ogImage, Astro.site)} />
    <meta name="twitter:image:alt" content={ogImageAlt} />

    <!-- App icons -->
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <link rel="icon" type="image/png" sizes="64x64" href="/ainative-s-64.png" />
    <link rel="icon" type="image/png" sizes="128x128" href="/ainative-s-128.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />

    <!-- Manifest -->
    <link rel="manifest" href="/manifest.json" />

    <!-- Feeds -->
    <link rel="alternate" type="application/rss+xml" title="AI Native Field Notes (RSS)" href="/feed.xml/" />
    <link rel="alternate" type="application/feed+json" title="AI Native Field Notes (JSON Feed)" href="/feed.json/" />
    <link rel="sitemap" type="application/xml" title="Sitemap" href="/sitemap-index.xml" />

    <!-- Preload critical fonts -->
    <link rel="preload" href="/fonts/geist-sans-latin-400-normal.woff2" as="font" type="font/woff2" crossorigin>
    <link rel="preload" href="/fonts/geist-sans-latin-700-normal.woff2" as="font" type="font/woff2" crossorigin>
    <link rel="preconnect" href="https://www.googletagmanager.com" crossorigin>

    <!-- Theme-color split: Safari paints address bar in matching scheme -->
    <meta name="theme-color" media="(prefers-color-scheme: light)" content="#ffffff" />
    <meta name="theme-color" media="(prefers-color-scheme: dark)" content="#0f172a" id="meta-theme-color" />
    <meta name="application-name" content="ainative" />
    <meta name="apple-mobile-web-app-title" content="ainative" />
    <meta name="format-detection" content="telephone=no" />

    <!-- JSON-LD Structured Data -->
    {jsonLdSchemas.map((schema) => (
      <script type="application/ld+json" set:html={JSON.stringify(schema)} />
    ))}

    <!-- Additional head content from child layouts -->
    <slot name="head" />
  </head>
  <body class="bg-surface text-text min-h-screen">
    <slot />

    <!-- IntersectionObserver for scroll-reveal -->
    <script>
      function initScrollReveal() {
        const observer = new IntersectionObserver(
          (entries) => {
            entries.forEach((entry) => {
              if (entry.isIntersecting) {
                entry.target.classList.add('is-visible');
                entry.target.addEventListener('transitionend', () => {
                  (entry.target as HTMLElement).style.willChange = 'auto';
                }, { once: true });
                observer.unobserve(entry.target);
              }
            });
          },
          { threshold: 0.1, rootMargin: '0px 0px -40px 0px' }
        );

        document.querySelectorAll('[data-animate], .img-reveal').forEach((el) => {
          observer.observe(el);
        });

        // SVG child element reveals — toggles .visible on children
        // when a parent [data-svg-animate] enters the viewport
        const svgObserver = new IntersectionObserver(
          (entries) => {
            entries.forEach((entry) => {
              if (entry.isIntersecting) {
                entry.target.querySelectorAll('.svg-reveal, .line-draw, .bar-grow, .ring-expand, .bar-grow-x, .dot-pop').forEach((child) => {
                  child.classList.add('visible');
                });
                svgObserver.unobserve(entry.target);
              }
            });
          },
          { threshold: 0.15, rootMargin: '0px 0px -20px 0px' }
        );

        document.querySelectorAll('[data-svg-animate]').forEach((el) => {
          svgObserver.observe(el);
        });
      }

      initScrollReveal();

      // Re-init after Astro view transitions
      document.addEventListener('astro:after-swap', initScrollReveal);
    </script>

    <!-- Image hover zoom-pulse: zoom in then auto-zoom-out after 300ms -->
    <script>
      function initImgZoom() {
        document.querySelectorAll('main img').forEach((img) => {
          let timeout: ReturnType<typeof setTimeout> | null = null;
          img.addEventListener('mouseenter', () => {
            img.classList.add('img-zoomed');
            timeout = setTimeout(() => {
              img.classList.remove('img-zoomed');
              timeout = null;
            }, 1800);
          });
          img.addEventListener('mouseleave', () => {
            if (timeout) { clearTimeout(timeout); timeout = null; }
            img.classList.remove('img-zoomed');
          });
        });
      }
      initImgZoom();
      document.addEventListener('astro:after-swap', initImgZoom);
    </script>

    <!-- Enable smooth theme transitions after first paint -->
    <script>
      function enableThemeTransitions() {
        requestAnimationFrame(() => {
          document.documentElement.classList.add('theme-transitions');
        });
      }
      enableThemeTransitions();
      document.addEventListener('astro:after-swap', enableThemeTransitions);
    </script>
  </body>
</html>
