{
  "version": 1,
  "project": "spark",
  "updated": "2026-06-15",
  "health": "green",
  "focus": "FIRST-CUSTOMER HAPPY-PATH SIM RUN END-TO-END: drove the REAL get-orionfold.sh bootstrap on a pristine box (sister HOME + fresh fieldkit[arena] venv + dev-signed sample license). Reached an all-green §8 receipt (ok=True, 4 pass/0 fail/1 skip: fieldkit+advisor 85.7%/9-9+cortex recall 0.977 & grounded 16/16+lane; hermes skipped). Wall-clock bootstrap→running stack ~8 min (GGUF pull ~2 min) → a clean first-boot is ~10-12 min, well under AC-1's 60 min. ⭐ Found + FIXED a launch bug: the §8 cortex grounded-contract gate deterministically false-FAILed on a healthy box (5/5 real verify runs) because advisor._REFUSAL_WORDS didn't recognize valid refusal phrasings the Q4_K_M lane emits ('scope is unsupported'/'does not include') — the model refuses correctly but llama-server isn't bitwise-deterministic at temp 0, and the all-or-nothing 16/16 hygiene flips. Broadened _REFUSAL_WORDS (refusal_ok is refuse-row-only, so answer/route + the published 85.7%/9-9 contract are unchanged — re-verified live; cortex now PASS 16/16; ok=True 2/2 reruns). +1 regression test (142 field_edition tests). UNCOMMITTED (operator gate): advisor.py + test + CHANGELOG [Unreleased]. Plus 4 orchestration findings logged for a focused session (AD-FK-α NGC key never sourced → stack-phase fails; β no corpus-ingest phase → empty pgvector; γ up's sidecar phase doesn't start the cockpit; δ shipped v0.32.2 verify --hermes still errors with the old stub). Stack LEFT UP for operator visual verification at http://127.0.0.1:7866/arena/.",
  "blockers": [],
  "next": [
    "OPERATOR: visually verify the freshly-installed Arena at http://127.0.0.1:7866/arena/ (Spark display — Claude can't launch the browser, exit 144). Fresh arena.db = empty leaderboard/models (honest first-customer state). THEN teardown: `HOME=/home/nvidia/of-fe-firstboot fieldkit field-edition down` (or docker rm -f of-advisor-lane of-embedder of-cortex-db) + kill cockpit (PID in cockpit.log) + RESTART DOGFOOD: docker start pgvector nim-embed-nemotron. Sister folder /home/nvidia/of-fe-firstboot deletable.",
    "Commit the scorer fix (advisor.py + test_field_edition.py + CHANGELOG) — uncommitted in the working tree, operator gate. Then a fieldkit-curator release ships BOTH the wired hermes gate (AD-FK-δ) AND this scorer fix before founding-25 customers run verify.",
    "Fix the 4 orchestration findings in a focused session (touch up.py phase machine + the bootstrap): AD-FK-α source ~/.nim/secrets.env (or up writes NGC_API_KEY into bundle .env); AD-FK-β add an ingest/seed phase to up (ship + ingest the Advisor demo corpus pack); AD-FK-γ up's sidecar phase should start `fieldkit arena up --no-open` itself; AD-FK-δ cut the fieldkit release. Also: re-broaden preflight._score_output to match field_edition's _REFUSAL_WORDS (port fidelity; re-freeze published receipts — operator-gated).",
    "§9 hosted signed-matrix feed so LiveUpdateChannel.fetch_latest can pull a NEW signed proven matrix (last M3 piece — images are signed + cosign-verifiable now; only the feed is missing; lives in the Website peer's entitlement-fetch) — no Spark dependency",
    "Website-owned (not blocking sales): entitlement-fetch (revocable short-TTL re-mint) + re-vendor license-conformance-v1.json from fieldkit==0.32.1 into the fulfillLicense mirror CI (token-less full-license case sha changed; test-fixture refresh)",
    "M2 infra (operator/hardware-gated, not code): digest-pin the cortex-embedder GHCR image in compose.py; the operator-armed live serving-lane smoke that lets advisor/cortex-grounded/lane PASS end-to-end; cosign cortex-embedder once built+pinned at v1.1",
    "Next-release note: /tmp/fk is reboot-ephemeral — rebuild with `pip install -e fieldkit[dev,arena,harness]` (the harness extra brings mcp for the hermes gate + harness suite) to collect the full suite + build"
  ],
  "recent": [
    "First-customer happy-path sim drove the REAL get-orionfold.sh bootstrap end-to-end (pristine box) → all-green §8 receipt (ok=True, 4 pass/1 skip) in ~10-12 min (well under AC-1 60min). Found + FIXED a launch bug: cortex grounded gate false-FAILed 5/5 on a healthy box (advisor._REFUSAL_WORDS missed valid refusal phrasings 'scope is unsupported'/'does not include' the lane emits non-deterministically at temp 0). Broadened the vocabulary (refuse-row-only → published 85.7%/9-9 unchanged, re-verified live; cortex now 16/16). +1 test (142 field_edition). UNCOMMITTED operator gate. 4 orchestration findings logged (AD-FK-α/β/γ/δ: NGC key not sourced, no corpus-ingest phase, sidecar not auto-started, shipped wheel's verify --hermes errors). Stack left up at :7866/arena/ for operator visual check",
    "Arena Field Edition M2 step 4: the optional §8 `hermes` verify gate — the LAST unwired first-boot gate — wired LIVE + PASSING. LiveGateRunner.hermes drives the fieldkit-as-MCP server over real stdio (initialize→list_tools→call_tool spark_inference_envelope); lane-safe read-only probe, PASSES stack-independently. CLI-smoked: verify --hermes → hermes status: pass. Entire §8 battery now wired live (no stubs). 5 new tests (141 field_edition); suite 1720 passed/0 failed. fieldkit-internal, [Unreleased], no version bump",
    "Arena marketing demo SFT + Reward tabs filled in (orionfold.com/arena/demo/{sft,reward}/) — were empty {available:false} stubs; now render full visualizations from real evidence (NeMo p65 LoRA SFT run + 3 real AV-10 reward preflights). Taught boot.js to honor the run-history ?source= dropdown; rebuilt + redeployed public/arena/demo/; verify_arena_demo_links green",
    "🟢 WE ARE LIVE — Arena Field Edition license path end-to-end on both sides; founding-25 sellable. Website peer shipped fulfillLicense to prod (ae0a521 + storefront af671f7): pay → token-less prod-signed license emailed → getarena.orionfold.com installer (HTTP 200, Cloudflare Worker) → load_license() verifies vs the v0.32.2-embedded prod key → Arena up. First sale = OF-FE-2026-0001",
    "fieldkit v0.32.2 RELEASED to PyPI + tag — prod license signing key provisioned (of-license-prod-2026 off PROD_KEY_PENDING; pubkey delivered by ops via relay). This is what closed the launch loop. verify_signature against the prod slot now gated by real Ed25519 verification. Shipped 3f56e26"
  ],
  "session": "S-fe-firstboot-sim",
  "metrics": {
    "articles": 54,
    "models": 7,
    "software_released": 7,
    "arena_features": 29,
    "fieldkit_modules": 19,
    "gsc_indexed": 24,
    "gsc_submitted": 183,
    "ga4_users_7d": 69,
    "checked": "2026-06-06"
  }
}
