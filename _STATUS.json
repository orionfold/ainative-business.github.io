{
  "version": 1,
  "project": "spark",
  "updated": "2026-06-13",
  "health": "green",
  "focus": "Arena Field Edition M2 — the full proven matrix is PUBLISHED + fully digest-pinned. (1) Q4_K_M Advisor quantized BF16→F16→Q4_K_M, quality-gated on a live lane (curveball-v0.2 85.7% = the Q8_0 baseline, same 3 safe-direction misses, refusals 9/9 — the §8 Advisor floor), published to HF Orionfold/Advisor-GGUF (rev dad6d1e5) + pinned in LaneConfig.gguf_revision. (2) The CUDA-13/SM121 llama.cpp lane image: reproducible multi-stage Dockerfile (deploy/field-edition/llama-server-cuda13, llama.cpp 856c3ad, forced driver-stub link), built + smoked (serves Q4_K_M, /v1/models 200 in 3s, finish_reason=stop, reasoning split correct), pushed to ghcr.io/orionfold/llama-server-cuda13:0.1 (sha256:93993cc2) + pinned in LaneConfig.image. (3) NIM embedder is the v1 default (digest-pinned, recall-proven; open embedder→v1.1 via --open-embedder) — ICP already runs NGC. (4) up.pull wired (resumable hf_hub_download, pin-by-revision). Net: unpinned_images()==[] for the default; `up --dry-run` renders an all-@sha256 Compose bundle → a live `up` resolves the whole stack by digest. 92 field_edition tests; full suite 1272 passed (only the 3 documented env arena failures). Shipped 06df6f7 + d31423e.",
  "blockers": [],
  "next": [
    "⏩ FULL-PUBLIC LAUNCH THIS WEEKEND (Mac timeline collapsed, supersedes M0–M4/~08-21). NEXT SESSION = the guided live `up` (operator-armed): operator brings up the box + cockpit, I run `fieldkit field-edition up` (resolves the whole stack by digest now) → wire + live-smoke the advisor/lane/hermes + cortex-grounded-contract generation-half verify gates (#5; advisor PASS already proven 85.7%, cortex full PASS, lane launch→gen→teardown) → near-all-green launch receipt. Open spec Q: §8 lane gate 'teardown clean' vs §6 resident-stays-warm",
    "cosign-sign the GHCR lane image (§9/M3 — the signed LiveUpdateChannel waits on this)",
    "bootstrap (get.orionfold.com) + DNS + Ed25519 license server (AC-7)",
    "DISTRIBUTION DECISION (pre-GA, discussed this session): images GHCR-now→NGC-for-GA (Inception + reuse the customer's existing NGC auth); weights+license+bootstrap → Supabase signed URLs (per-customer, revocable); convert orionfold GitHub User→Organization (it's a personal User account — weak for a paid product)",
    "HF card: feature Q4_K_M as the default variant + the curveball parity on Orionfold/Advisor-GGUF README (hf-publisher); re-freeze decision for the advisor corpus manifest's retired _FLOWS/ path"
  ],
  "recent": [
    "Proven matrix PUBLISHED + fully digest-pinned: Q4_K_M→HF (rev dad6d1e5), lane image→GHCR (sha256:93993cc2); up --dry-run renders an all-@sha256 bundle, unpinned_images()==[]. Shipped d31423e",
    "Q4_K_M quality-gated on a live lane before publish: curveball-v0.2 85.7% (=Q8_0 baseline) + refusals 9/9 — did NOT regress the launch default",
    "Lane image: reproducible CUDA-13/SM121 Dockerfile (forced driver-stub -lcuda link fix), built + smoked (serves Q4_K_M, reasoning split correct)",
    "NIM embedder = v1 default (digest-pinned); open embedder→v1.1 (--open-embedder). up.pull wired (resumable hf_hub_download). 92 field_edition tests",
    "Findings: frozen advisor corpus manifest still referenced retired _FLOWS/ path (1/182) → local compat symlink (flag re-freeze); HF xet cache root-owned → upload needs HF_HUB_DISABLE_XET=1; orionfold is a GitHub USER acct so GHCR push needs orionfold creds (memory saved)"
  ],
  "session": "S-fe-proven-matrix-published",
  "metrics": {
    "articles": 54,
    "models": 7,
    "software_released": 7,
    "arena_features": 29,
    "fieldkit_modules": 18,
    "gsc_indexed": 24,
    "gsc_submitted": 183,
    "ga4_users_7d": 69,
    "checked": "2026-06-06"
  }
}
