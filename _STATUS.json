{
  "version": 1,
  "project": "spark",
  "updated": "2026-06-15",
  "health": "green",
  "focus": "AD-FK-α/β/γ CLOSED — `up`/bootstrap is now a real unattended `curl … | sh` (the 3 gaps the first-customer sim had to hand-drive). All fieldkit-internal in CHANGELOG [Unreleased], UNCOMMITTED (operator gates the main push); a fieldkit-curator release publishes them. AD-FK-α: compose.read_ngc_api_key() (env → ~/.nim/secrets.env) + compose_env() inject NGC_API_KEY into EVERY docker compose call (up.stack fails fast with a named fix when missing; down/repair use a placeholder — α broke all three); get-orionfold.sh also sources secrets.env. AD-FK-γ: up.sidecar now STARTS the cockpit (spawns `fieldkit arena up --no-open` detached, log→~/.orionfold/cockpit.log, pid→cockpit.pid, health-polls :7866, fails honestly on early-exit; re-entrant). AD-FK-β (wheel-vendored, operator-chosen over HF/GitHub): new fieldkit.field_edition.ingest + an `ingest` phase in up (matrix→bundle→pull→stack→INGEST→sidecar→resident→[verify]). The Advisor demo corpus rides the wheel as a sha-pinned frozen pack data/advisor-corpus-pack-v01.jsonl.gz (986 KB gz, 182 sources, CORPUS_PACK_SHA=e2f4a2d64ada); up.ingest chunks→embeds→upserts it OFFLINE into advisor_corpus_v01, reproducing the exact 647 chunks the recall@5 0.977 proof measured. Idempotent; also `fieldkit field-edition ingest [--force]`. ⚠️ Proof-control: the pack is a DELIBERATE FROZEN SNAPSHOT — the fieldkit CHANGELOG is itself a corpus source AND a recall gold (row 0077), so editing it drifts a repo-rebuild; the builder's --check validates the shipped artifact (sha==runtime pin), not a repo round-trip; a re-freeze is a deliberate `build_advisor_corpus_pack.py` run gated on re-running every bench. 164 field_edition tests (+22), full offline suite 1743 passed/0 failed/16 skipped; wheel carries ingest.py + the pack; up --dry-run shows the ingest phase. Only AD-FK tail left: re-broaden preflight._score_output (operator-gated re-freeze).",
  "blockers": [],
  "next": [
    "Cut a fieldkit-curator release to ship AD-FK-α/β/γ off [Unreleased] (uncommitted now — operator gates the push). When the release moves the CHANGELOG, re-freeze the corpus pack (`python scripts/field_edition/build_advisor_corpus_pack.py`, re-pin CORPUS_PACK_SHA in ingest.py, re-run benches) ONLY if the corpus moved materially — the shipped snapshot is valid as-is. Also re-broaden preflight._score_output to match field_edition's _REFUSAL_WORDS (port fidelity; re-freeze published receipts — operator-gated). This is the last AD-FK-* tail.",
    "§9 hosted signed-matrix feed so LiveUpdateChannel.fetch_latest can pull a NEW signed proven matrix (last M3 piece — images are signed + cosign-verifiable now; only the feed is missing; lives in the Website peer's entitlement-fetch) — no Spark dependency",
    "Website-owned (not blocking sales): entitlement-fetch (revocable short-TTL re-mint) + re-vendor license-conformance-v1.json from fieldkit==0.32.1 into the fulfillLicense mirror CI (token-less full-license case sha changed; test-fixture refresh)",
    "M2 infra (operator/hardware-gated, not code): digest-pin the cortex-embedder GHCR image in compose.py; the operator-armed live serving-lane smoke that lets advisor/cortex-grounded/lane PASS end-to-end; cosign cortex-embedder once built+pinned at v1.1",
    "Next-release note: /tmp/fk is reboot-ephemeral — rebuild with `pip install -e fieldkit[dev,arena,harness]` (the harness extra brings mcp for the hermes gate + harness suite) to collect the full suite + build"
  ],
  "recent": [
    "AD-FK-α/β/γ CLOSED — `up`/bootstrap is now a real unattended `curl|sh` (fieldkit-internal, [Unreleased], uncommitted/operator-gated). α: NGC_API_KEY sourced (env → ~/.nim/secrets.env) into every docker compose call (up.stack fails fast if missing; fixes down/repair too). γ: up.sidecar STARTS the cockpit detached + health-polls. β (wheel-vendored, operator-chosen): fieldkit.field_edition.ingest + an `ingest` phase in up; the 182-source Advisor demo corpus rides the wheel as a sha-pinned frozen pack (986 KB gz, CORPUS_PACK_SHA=e2f4a2d64ada), ingested OFFLINE → the exact 647 chunks the recall 0.977 proof measured; also `field-edition ingest`. Pack is a deliberate frozen snapshot (the CHANGELOG is a corpus source + recall gold, so --check validates the artifact, not a repo rebuild). 164 field_edition tests (+22); full offline suite 1743 passed/0 failed/16 skipped; wheel carries the pack",
    "fieldkit v0.32.3 RELEASED to PyPI + tag e8b1aae — patch ships the §8 hermes gate LIVE (last unwired first-boot gate, from the 2026-06-14 session) + the cortex refusal-scorer fix (found by today's first-customer sim). Closes AD-FK-δ. audit-docs 17/18, audit-landing 6/6, offline 1721 passed/0 failed (142 field_edition), git+PyPI install-verify ✓, stats refreshed (LOC 62,491)",
    "First-customer happy-path sim drove the REAL get-orionfold.sh bootstrap end-to-end (pristine box) → all-green §8 receipt (ok=True, 4 pass/1 skip) in ~10-12 min (well under AC-1 60min). Found + FIXED a launch bug: cortex grounded gate false-FAILed 5/5 on a healthy box (advisor._REFUSAL_WORDS missed valid refusal phrasings 'scope is unsupported'/'does not include' the lane emits non-deterministically at temp 0). Broadened the vocabulary (refuse-row-only → published 85.7%/9-9 unchanged, re-verified live; cortex now 16/16). +1 test (142 field_edition). Committed+pushed 0e1b153, then shipped in v0.32.3. 4 orchestration findings logged (AD-FK-α/β/γ NGC key not sourced/no corpus-ingest phase/sidecar not auto-started — deferred; δ closed by v0.32.3). FE stack torn down, dogfood restored (recreated the 2 containers after a port-conflict corrupted their bridge endpoints; 646-chunk corpus intact)",
    "Arena Field Edition M2 step 4: the optional §8 `hermes` verify gate — the LAST unwired first-boot gate — wired LIVE + PASSING. LiveGateRunner.hermes drives the fieldkit-as-MCP server over real stdio (initialize→list_tools→call_tool spark_inference_envelope); lane-safe read-only probe, PASSES stack-independently. CLI-smoked: verify --hermes → hermes status: pass. Entire §8 battery now wired live (no stubs). 5 new tests (141 field_edition); suite 1720 passed/0 failed. fieldkit-internal, [Unreleased], no version bump",
    "🟢 WE ARE LIVE — Arena Field Edition license path end-to-end on both sides; founding-25 sellable. Website peer shipped fulfillLicense to prod (ae0a521 + storefront af671f7): pay → token-less prod-signed license emailed → getarena.orionfold.com installer (HTTP 200, Cloudflare Worker) → load_license() verifies vs the v0.32.2-embedded prod key → Arena up. First sale = OF-FE-2026-0001"
  ],
  "session": "S-adfk-abg-unattended-up",
  "metrics": {
    "articles": 54,
    "models": 7,
    "software_released": 7,
    "arena_features": 29,
    "fieldkit_modules": 19,
    "gsc_indexed": 24,
    "gsc_submitted": 183,
    "ga4_users_7d": 69,
    "checked": "2026-06-06"
  }
}
