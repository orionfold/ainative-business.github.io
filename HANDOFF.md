<!--
  Session handoff for ainative-business.github.io (Spark-operated monorepo).
  Convention:
  - Replace "Open items" each session; do NOT append.
  - "Recent decisions" is a short running log — keep ~2 latest, prune older.
  - Full history of any pruned entry is recoverable via `git log -p HANDOFF.md`.
  - Last pruned/reset: 2026-06-01.
-->

# HANDOFF — ainative-business.github.io

## Current state

- **This repo is the single Spark-owned monorepo** at `/home/nvidia/ainative-business.github.io` — build workspace *and* website. The old two-repo `ai-field-notes`→Mac sync model is **retired**; author directly here (do NOT use `sync-field-notes`).
- **Live** at `ainative.business` (GitHub Pages). `origin/main` tip = **`606333d`** (Phase-1 Arena launch article — see top DONE block), atop `931b941` (HANDOFF queue) + `a498411` (stats/HANDOFF post-v0.16.0) + **`f50267f`** (**`fieldkit v0.16.0` cut**: the first Arena release M1→M8, tag `fieldkit/v0.16.0` + <https://pypi.org/project/fieldkit/0.16.0/>; the M8 control plane is BUILT → side-by-side-FIXED → SHIPPED — see the top three Recent decisions + spec §12.7), atop `d6ce51a` (M8 #1/#2 bench-registry + regression-producer fixes), `6755ea7` (Arena M8 control plane BUILT: `fieldkit.arena.jobs` dispatcher + cockpit `/arena/jobs/`), `820f9e1`/`5f80fe9` (the M8 spec + fieldkit-reconciliation), `57f2155` (M8 spec authored), `ea3ed0d` (Bet-6 cost plane), and the earlier **Flows rename + ALL-CAPS-folder normalization** `8b16503`. **Nothing unpushed.** NB the M8 commit changed `fieldkit/` + `arena-app/` only — the public `dist/` is unchanged until the running cockpit is rebaked (`fieldkit arena build --repo-root arena-app`) and/or a marketing-site deploy runs. The Flows rename (`8b16503`): `WORKFLOWS.md` `git mv`'d into the new `_FLOWS/` folder as `_FLOWS/the-machine-that-builds-machines.md` (the "Flows" stream: process flow, data flow, operator flow-state — "Flows", not the CC-`Workflow`-tool-colliding "Workflow", names this body of work), and the remaining ALL-CAPS-folder holdouts normalized to lowercase-kebab — `_GUIDES/{INDEX,NARRATIVE-CONTRACT,PRODUCT-ARTICLES}.md` + `_SPECS/INDEX.md`. Every live path reference remapped across skills (`.claude/skills/**`), `fieldkit` source/docs/tests, `scripts/`, `src/content.config.ts`, `src/styles/products.css`, `src/content/artifacts/README.md` (dead `/Volumes/...` link repointed repo-relative), `CLAUDE.md`, the two `index.md` files, `_SPECS/roadmap-reconciliation.md`, and the meta-program transcript. **Deliberately NOT rewritten** (append-only / immutable, per `_SPECS/index.md`'s own note): `fieldkit/CHANGELOG.md` and `_SPECS/archive/**`. Bare proper-noun rule mentions ("NARRATIVE-CONTRACT Rule 8", no `.md`) left as the contract's display name. Build green (490 pages), both verifiers pass. Convention going forward: files inside `_FLOWS/`/`_GUIDES/`/`_SPECS/` are lowercase-kebab (memory `feedback_flows_folder_naming`). **Working tree clean** but for two pre-existing untracked items (`.claude/scheduled_tasks.lock`, `src/data/arena-mirror/` — leave alone).
- **Build/verify loop:** `node node_modules/astro/astro.js build` (490 pages; `npm run build` is broken on this checkout per `reference_astro_build_smb_symlink_break`) → `node scripts/verify_artifact_rendering.mjs` (18 pages/7 kinds) + `node scripts/verify_field_notes_rendering.mjs` (412 explainers/63 pages). `build:og` is **CI-only** (needs Chrome); CI regenerates OG on push.
- **arena-app/ + fieldkit/ build separately** from the marketing site — pushing arena/fieldkit changes does NOT change the public `dist/`. After any `arena-app/` edit, rebuild the served bundle: `fieldkit arena build --repo-root arena-app` (bakes `_webui`, gitignored).
- **Secrets:** `.env.local` (gitignored, chmod 600) holds `PYPI_TOKEN`, `HF_TOKEN`, `OPENROUTER_API_KEY` — `fieldkit-curator` + `hf-publisher` auth here.

**Recently shipped (all live):** **MTBM-arc opener `the-meta-program-on-spark` (`bca63e7`, pushed)** — thesis-spine deep-dive (pane→hands→engine + Ch-14 meta-program; `book_chapters:[10,11,14]`; new signature `MetaProgramRecursion`; inline fn-diagram; 8 explainers); closes the editorial-overlay "Now (not gated)" item below; patent-strategist `lane_summary`+`siblings` (`9df9438`); positioning+`known_drift` backfill on the 4 older vertical quants (`ddfa492`); per-product OG card for `/products/` (`0cb175d`); Arena marketing refresh — demo bundle + launch article + landing (`0bf2ec7`); Orionfold LLC brand pass + go-live (deploy `26664480875`). Arena cockpit/leaderboard/telemetry work is committed on `origin/main` (arena-app only).

## ⚙️ Live runtime

- **Arena cockpit is DOWN** (torn down 2026-06-02 via `arena_lifecycle.sh down` after the Phase-1 launch screenshot pass — `:7866` dead, no process). It was up earlier to capture the M8 `/arena/jobs/` shots; `arena.db` was seeded with 6 demo jobs and **cleaned back to its real state afterward** (0 jobs, 14 leaderboard rows), so the db is publish-clean. Bring it back (M8 `_webui` bake, venv `/tmp/arena-venv`, db `~/.fieldkit/arena.db`): `.claude/skills/arena-lifecycle/scripts/arena_lifecycle.sh restart [--browser]`. NB the lifecycle verbs are `up | down | restart | status` (not `stop`).
- **pgvector** container UP (`:5432`, db `vectors`, table `blog_chunks`) — backs the Second Brain index.
- **NIM embedder UP** (`nim-embed`, `llama-nemotron-embed-1b-v2`) on host **`:8001`** via **bridge port-map `-p 8001:8000`** (NOT `--network host` — that collides with Triton's gRPC port 8001; see memory `reference_second_brain_reindex`). The Second Brain MCP needs it for query-time embedding; `docker rm -f nim-embed` to free it (~tiny, 1B). The throwaway dist server `:8099` + visible Chromium CDP `:9222` are torn down.

## Open items

### ▶ NEXT UP — the spec-authoring backlog is EMPTY; the next acts are *builds*, not *writes*
**Decision 2026-06-02:** Phase 1 (Arena M8 control plane) **shipped** (BUILT → side-by-side-fixed → released in `fieldkit v0.16.0` → launch article `606333d`), and **all four `_FLOWS` §3 spec stubs are now WRITTEN** (decisions-locked + signed off + unbuilt): `cost-plane-v1` = §13 [M9], `second-brain-pipeline-v1` = §14 [M10], `autonomous-harness-v1` = §15 [M11], and the Phase-3 engine `rlvr-loop-v1` = the **standalone `_SPECS/rlvr-loop-v1.md`** (10 decisions RV-1…10; new `fieldkit.rl` + `fieldkit.reward`) — written this session. The write order executed in dependency order (cross-bets feed Phase 2; engine lands last). **There is no remaining stub to write** — what's left for each is a *build*, gated to its release. Spec-feedable numbers per stub are in `_SPECS/roadmap-reconciliation.md` §"Spec-feedable facts". The four builds in their release-gate order:

1. ✅ **`cost-plane-v1` (Bet 6 → Arena M9) — WRITTEN.** Decisions locked + signed off 2026-06-02 as **`_SPECS/spark-arena-v1.md` §13** (10 decisions M9-1…10, code-reconciled against the built `fieldkit.arena`/`harness`, 3 risks R18–R20, `fieldkit.cost` abstraction, schema `4→5` first-ALTER migration). **Spec-only, UNBUILT** — release gate ~`fieldkit v0.17.0`. The implementation (persist `_compare_cost_usd()`, `openrouter_price_snapshot` seeded from H6 JSON, `$/quality-point` on the public leaderboard, cockpit cost cells, version-control the untracked H6 evidence) is the next *build* when this milestone opens; it's the hard prerequisite for the Phase-2 budget governor.
2. ✅ **`second-brain-pipeline-v1` (Bet 5 → Arena M10) — WRITTEN.** Decisions locked + signed off 2026-06-02 as **`_SPECS/spark-arena-v1.md` §14** (12 decisions M10-1…12, code-reconciled against the built `arena/jobs.py` [the `reindex`/`rag_eval`/`scout_ingest` job sockets are **already drilled** as named stubs], `fieldkit.rag`, + the on-disk `rag-eval-work/` + `second-brain-mcp/`; 3 risks R21–R23; `fieldkit.memory` abstraction over `fieldkit.rag`; arena.db `5→6` + pgvector `blog_chunks` provenance columns). 4 open calls signed off "recommended" (ingest fork → adopt `rag.Pipeline` as canonical; query backend → single `fieldkit.memory`; provenance → columns on `blog_chunks`; version-control the external `ingest_blog.py`/`qa-eval`/eval-config/SB-server). **Spec-only, UNBUILT** — release gate ~`fieldkit v0.18.0`. The build (promote the 3 job kinds to dispatchable, `fieldkit.memory` multi-source ingest, the `/arena/knowledge/` pane, the eval-gated re-index) is the next *build* when this milestone opens; it feeds the Phase-2 freshness monitor + closes the §7 12-of-63-staleness / external-script drift.
3. ✅ **`autonomous-harness-v1` (Phase 2 — the hands) — WRITTEN.** Decisions locked + signed off 2026-06-02 as **`_SPECS/spark-arena-v1.md` §15 = Arena M11** (placement chosen consistent with M8/M9/M10, not the standalone file the roadmap originally named — user-confirmed). 9 decisions AH-1…9, code-reconciled against the built `arena/jobs.py` (the `drain_jobs()`/`check_and_enqueue_regressions()` sockets are **already built** — comments literally say "the Phase-2 cron will call this on a schedule"; M11 adds the *scheduler*, not the dispatch), the absent `fieldkit.budget`/`fieldkit.cost`, the one live `.claude/` hook, and the stale `scheduled_tasks.lock`. New abstraction `fieldkit.budget` (governor; `LOCAL_CEILING=33%` failure-mode escalation; M9 cost-plane a **soft** prerequisite w/ token+envelope fallback). Scheduler over the built drain + hook battery 1→N + stage-only morning standup + freshness-monitor emitting M8 triggers; **no new arena.db table / no `user_version` bump** (AH-9); two-layer containment carried forward. 3 risks R24–R26. **Spec-only, UNBUILT** — release gate ~`fieldkit v0.19.0`. The build (the cron glue, the `.claude/` hooks, `fieldkit.budget`, the `/arena/standup/` render) is the next *build* when this milestone opens; it drains M8 overnight, enforces M9's ledger, refreshes M10 on schedule, and is the execution home for the Phase-3 engine.
4. ✅ **`rlvr-loop-v1` (Phase 3 — the engine) — WRITTEN 2026-06-02 (this session), gate ~`fieldkit v0.20.0`.** Standalone `_SPECS/rlvr-loop-v1.md` (placement user-confirmed — the engine spans the new `fieldkit.rl`/`fieldkit.reward`, mostly outside `fieldkit.arena`). 10 decisions RV-1…10 + 5 risks RV-R1…5, code-reconciled against the built `fieldkit/`. **Two reconciliation wins baked in:** the reward's `failure_class` **reuses the already-built `fieldkit.lineage.FailureLabel`** 9-class enum (no new enum), and `rl_run`/`requant` are the **pre-drilled `arena/jobs.py` sockets** this spec promotes to `DISPATCHABLE`. Locks the three grounding corrections (RV-1 wrap the hand-rolled REINFORCE loop not Unsloth/NeMo-RL; RV-4 held-out-every-≤10-steps **hard gate** w/ held-out-only checkpoint selection; RV-5 pin vLLM + restart-elimination as the top wall-clock win), `(success, failure_class, auxiliary)` reward over a **≥100-row corpus** (RV-3/10), no arena.db churn (RV-8), publishable kinds deferred to second reuse (RV-9). M11 (§15) is its overnight execution home + budget brake. **Build:** `fieldkit.rl` (REINFORCE-with-KL + pinned-vLLM kill-and-restart) + `fieldkit.reward` (adapter over the 7 `fieldkit.eval` scorers), the `rl_run`/`requant` `DISPATCHABLE` promotion, the held-out-gate `eval_rerun` wiring.

**Discipline (closed):** each stub opened with "locked decisions — confirm before build" like M8 §12.1; each was **user-green-lit**. Bets 5/6 **and Phase 2** landed as **Arena-milestone sections** of `spark-arena-v1.md` (§13 = M9 cost plane ✅, §14 = M10 recall pane ✅, §15 = M11 autonomous harness ✅); the **Phase-3 engine landed standalone** (`_SPECS/rlvr-loop-v1.md` ✅, placement user-confirmed). All four captured in `_SPECS/index.md` (Active specs + Planned queue, all ✅). **Next time the roadmap advances it's a BUILD** (start with M9 cost plane, the prerequisite chain's root).

### ✅ DONE 2026-06-02 — Phase-1 Arena launch article SHIPPED + PUSHED (`product-writer`)
The Phase-1 launch is **committed + pushed** — `origin/main` tip = **`606333d` "Add product launch: Orionfold Arena — Control Plane"** at **`products/arena-control-plane/product.md`** (decision: **new article**, user-confirmed, not an extend of the M7 `orionfold-arena` launch). Working tree clean.
- **What shipped (`606333d`):** `products/arena-control-plane/` — `product.md` (`series: Cockpit`, positioning-first, 7-beat blueprint, cross-links the M7 launch + the H4 deep-dive), `assets/build-metrics.json` (mined), `screenshots/{01-jobs-board,02-dispatch-and-regression,03-board-columns}.png` (2× DPR, captured against the live `:7866` cockpit; `arena.db` seeded then cleaned). Gate at commit time: `verify_product_article.sh arena-control-plane` **PASS**; `astro build` **492 pages**; both rendering verifiers pass.
- **Build-metrics (honest, mined):** M8 was a **~2.6 h sprint** (spec-lock `57f2155` → release `f50267f`), 2 sessions / 107 turns, **18.6M tokens processed, 95.9% cache**, 104k generated; **1,762 lines authored source** + **35 dispatcher/API tests**; built **100% on Opus 4.8**. `feature_count: 3`. No `project-stats.json` refresh needed (the "products" bucket = NVIDIA-products-covered; pipeline scans `articles/**` only).
- **§5 series ledger now has its first per-phase launch entry.** Phases 2 & 3 (below) remain gated on those phases shipping.

### Operator actions (you own these)
- ✅ **`ai-field-notes` archived read-only** (2026-06-02). Git history + the 92M research-evidence layer preserved; local copy kept at `/home/nvidia/archive-ai-field-notes`. Mac PR #11 is now moot (repo is read-only).
- **PSI authenticated key** still missing (blocks PageSpeed in `/seo-monitor`).

### Destination work
- **#7 remainder — human-eye browser pass on the LoRA/adapter/dataset detail pages.** The 4 quant pages were browser-verified 2026-05-31 (0 console errors); the LoRA (`/artifacts/loras/patent-strategist-v3-nemo/`), adapter, and dataset detail + empty-state listing pages have only HTML-level verifier coverage, no human-eye/Lighthouse pass yet.
- **#11 — bakeoff article's gated catalog footer last-write-wins.** `chrome_footers.collect_gated_articles()` keys by article-slug, so when multiple manifests bind one article the alphabetically-last wins (currently points at the surviving NeMo lane). Low priority; fix when the next multi-binding case lands.
- **#18 — patent-strategist W3 fine-tune** (source-side, ETA ~2 weeks). Likely ships more `kind: lora` manifests; the render path + notebooks-as-artifacts scaffold are ready (new manifests can carry `notebooks:{colab,kaggle}` with no code change). NB future quant *republishes* get `positioning`+`known_drift` only — NOT `stack_origin`/`lane_summary`/`siblings` (those are for Orionfold fine-tunes; see 2026-05-31 decision).

### Editorial overlay — the-machine-that-builds-machines journey → article series (sequenced at roadmap extraction points)
The book is **published** (Amazon / Leanpub / Orionfold) — do **NOT** edit `src/data/book/chapters/**`. Overlay the "machine that builds machines" journey as **articles** instead, threading the published chapters via the `book_chapters: [10, 11, 14]` frontmatter cross-link (editorial reference, **not** a series retag, **not** a book edit — H4 precedent). It's a **hybrid of two genres** stitched by existing machinery: deep-dives via `tech-writer` (the **MTBM arc** A1–A9 + the **Harnesses series** H1–H6 already exist), launches via `product-writer` (`products/**`, `series: Cockpit`, build-metrics + feature tour). Extraction points, mapped to `_FLOWS/the-machine-that-builds-machines.md` §3 phases:
- ✅ **DONE 2026-06-02 — thesis-spine deep-dive SHIPPED** as `the-meta-program-on-spark` (`bca63e7`, pushed, published in full): the `pane → hands → engine` arc + the Ch-14 *meta-program-as-operator-instance* framing, `book_chapters:[10,11,14]`, signature `MetaProgramRecursion`, inline fn-diagram, 8 explainers. This was the book-overlay opener; the gated Phase-1/2/3 items below remain.
- ✅ **DONE 2026-06-02 — Phase 1 ship — Arena control plane** → `product-writer` launch **SHIPPED + PUSHED** as **`products/arena-control-plane/`** (`606333d`, `series: Cockpit`): M8 build-metrics infographic + 3-shot `/arena/jobs/` feature tour (board · dispatch+scan-regressions · cards-with-results), cross-linking the M7 launch + the H4 deep-dive. The §5 series now has its first per-phase launch entry; Phases 2 & 3 below remain gated on those phases shipping.
- **Phase 2 ship — autonomous harness** → the Harnesses deep-dives **already exist and are published** (H3 `hardening-the-hermes-harness-on-spark`, H4 `hermes-drives-the-spark-via-fieldkit-mcp` — the MCP write-surface concept); so the new piece is a `product-writer` launch for the **built** cockpit surface (morning-standup · cron queue · budget-governor) that **cross-links the existing H4**. Gated on Phase 2 shipping.
- **Phase 3 ship — closed-loop RLVR** → `tech-writer` MTBM installment (new A-slug; the arc already scopes "RL on agent trajectories — GRPO…") + Looking-Beyond-Spark extrapolation, **plus** the first §5 "living-model" `product-writer` launch (the `fieldkit.lineage` delta chart as hero). Gated on Phase 3.
- **Mechanism to stake now:** both skills support `status: upcoming` placeholders — drop one per extraction point to claim the slug + commitment, promote to `published` as each phase lands. Each product launch's build-metrics block is the per-phase proof of Ch-14's configuration-over-code economics; collected across the series they're a running ledger of the machine compounding.

### ✅ Roadmap & specs — corpus harvest DONE 2026-06-02 (output 1; output 2 JIT-deferred)
The grounding harvest is complete. **Output (1) — the §3 reconciliation note — is written at `_SPECS/roadmap-reconciliation.md`**, and its sharpest corrections are **folded into `_FLOWS/the-machine-that-builds-machines.md` §3 per-phase *Measured grounding* notes + a §7 drift bullet** (committed). The §3 roadmap has since grown two cross-cutting bets atop the four phased ones — **Bet 5 (recall layer)** and **Bet 6 (cost plane)**. Headline findings: §3's sequencing survives; **two abstractions were wrong** — Phase 3 named Unsloth/NeMo-RL where a **hand-rolled ~280-LOC REINFORCE** is what ran (`clawgym-on-spark-grpo`), and the GRPO bottleneck is the **vLLM restart (~3.5 min/step), not the trainer (~22 s)** — and **one risk was missing**: the **training-pool↔held-out inversion** (`t2po`, 81.8 pp gap → held-out-every-10-steps is now a hard gate). Phase 1 dispatch-through-MCP is **validated end-to-end** (`hermes-drives`, 0% format error). Spec-feedable numbers per stub (`rlvr-loop-v1`, `autonomous-harness-v1`, Arena M8) are in the note's appendix. **Update 2026-06-02: the Arena M8 stub is now WRITTEN** (`_SPECS/spark-arena-v1.md` §12 — see top Recent-decisions entry); the four remaining stubs (`rlvr-loop-v1`, `autonomous-harness-v1`, `second-brain-pipeline-v1`, `cost-plane-v1`) are still named-not-written but **now prioritized** — see the **▶ NEXT UP** block at the top of Open items (write order: `cost-plane-v1` → `second-brain-pipeline-v1` → `autonomous-harness-v1` → `rlvr-loop-v1`).
- **Output (2) — the per-spec evidence index (article + `evidence/` file → spec section) — is deliberately deferred**, built JIT when each spec is written (targeted to that spec's open questions, not harvested cold).
- **Method note:** the Second Brain MCP path *was* unusable — its index held **12/63 articles and none of the roadmap-relevant ones** (stale; its `ingest_blog.py` pointed at the retired `nvidia-learn` path). Reading fell back to disk via 3 read-only Explore agents (Workflow was *not* opted into).
- ✅ **Second Brain RE-INDEXED + reranker resolved 2026-06-02.** Repointed `/home/nvidia/rag-eval-work/ingest_blog.py` `ARTICLES` → monorepo, brought up the embedder, full-rebuild → **50 articles / 313 chunks** (was 12); pipeline verified fully local end-to-end (embed `:8001` → pgvector `:5432`, 0.048s, returns roadmap content). **Reranker:** hosted NGC endpoint is **410 Gone** AND the local `llama-3.2-nv-rerankqa-1b-v2` NIM **has no GB10-compatible profile** (only an ONNX build, crashes `cudaErrorSymbolNotFound`; no `-dgx-spark` image yet). **Resolved:** edited `/home/nvidia/second-brain-mcp/server.py` (outside this repo, not tracked here) — `rerank` now defaults **False** (local cosine), `RERANK_URL` env-overridable → local `:8002`, auth conditional, so `rerank=True` self-enables when a GB10/`-dgx-spark` reranker or frontier HW arrives. `nim-rerank` container torn down. `ask_blog` additionally needs the cached local Llama-3.1-8B generator on `:8000` (not running). Runbook + GB10 finding in memory `reference_second_brain_reindex`.

### SEO watch
- **Re-run `/seo-monitor`** (~1–2 weeks out). Confirm live `sitemap-0.xml` settled at ~182 URLs (no `/field-notes/tags/` or `/stages/`); GSC *Discovered–not-indexed* (355, **stale — those URLs are already indexed** per the 2026-05-30 inspection) should bleed down as the Validate-Fix completes. GSC "unused verification token" `fePoYwMX…` is **HOLD / do-not-remove** (Workspace-owned TXT; removing risks email). Journal: `seo-progress.md`.

### ✅ Skill-path drift — FIXED + PUSHED 2026-06-02 (`6cdefd4`)
The publish-workflow path bug discovered while shipping `the-meta-program-on-spark` is resolved and **shipped (`6cdefd4`, now pushed — ancestor of `origin/main`)**. Net: stats now refresh to the path the site actually reads, the dead README plumbing is gone, and the LOC KPI is honest for the monorepo. Memory `feedback_refresh_stats_on_publish` updated (paths + the 3 decisions). **Only remaining loose thread:** the retired `sync-field-notes` skill still references the *source* repo's old `src/data/project-stats.json` path (lines 32, 213) — left as-is since that skill is itself slated for deprecation (see Cleanup below).

### Cleanup (non-blocking, tied to the retired two-repo model)
- **Dead-sync docs deleted 2026-06-02** (`MAC-TO-SPARK-TRANSITION.md`, `SYNC-WORKFLOW.md`, `mirrors/`, `handoff/`) as part of the `_GUIDES/`+`_SPECS/` consolidation — see the 2026-06-02 decision below.
- All dead `mirrors/destination-overrides.md` references cleared 2026-06-02 (in `_SPECS/spark-arena-v1.md`, `src/content/artifacts/README.md`, `chrome_footers.py`, `site-rendering-rubric.md` — reframed to monorepo reality). Still open: deprecate/remove the now-obsolete `sync-field-notes` skill itself (a separate decision — the skill body is retired but not deleted).
- Optional: relocate the CC memory namespace symlink (`-home-nvidia-ai-field-notes`, 93 files) into this namespace for a fully clean cutover (safe as-is — points into `~/.claude/projects`, not the repo).

## Recent decisions (short running log — prune older)

### 2026-06-02 (RLVR-loop locked decisions WRITTEN — standalone `_SPECS/rlvr-loop-v1.md` — the FOURTH and FINAL `_FLOWS` §3 stub)
The last stub landed — the **engine** of `pane → hands → engine`, closing the four-stub backlog. **Placement was a user-green-lit fork** (`AskUserQuestion`): unlike M9/M10/M11 (Arena sections), `rlvr-loop-v1` landed **standalone** — the two new modules `fieldkit.rl` (trainer) + `fieldkit.reward` (scorer→reward adapter) live mostly outside `fieldkit.arena`; the loop is a self-contained training system Arena merely triggers + watches (a tight, explicit Arena seam, not an Arena section). Drafted **code-reconciled against the built `fieldkit/src/fieldkit/`** — surfacing 6 facts, headlined by **two reconciliation wins**: **(a)** the reward's `failure_class` is **ALREADY MODELED** — `fieldkit.lineage.FailureLabel` is a shipped 9-class string enum (`keep`/`discard`/`crash`/`eval_budget_overrun`/`train_budget_overrun`/`size_blocked`/`preflight_crash`/`harness_abort`/`disqualified` + `baseline`) with an `is_informational` property, so RV-3's `(success, failure_class, auxiliary)` reward **reuses it, no new enum**; **(b)** the `rl_run`/`requant` job kinds are **pre-drilled** in `arena/jobs.py` `JobKind` (in `ALL` not `DISPATCHABLE`, comment "→ Phase 3 `rlvr-loop-v1`"), so RV-6 just promotes them (the M8-`eval_rerun` pattern). Other facts: `fieldkit.rl`/`fieldkit.reward` both **absent** (the new modules); the 7 verifiers + `Trajectory`/`AgentRun`/`PassAtK`/`GroupStats` present in `fieldkit.eval` (RV-2 thin adapter); `ARTIFACT_KINDS` still **8** (RV-9 defers publishable `verifier`/`reward`/`rl_run` kinds to second reuse per `[[feedback_keep_scorer_local_until_reuse]]`); arena.db at `user_version=4`, RV-8 adds **no table/no bump**. Wrote the full standalone spec (10 decisions RV-1…10, scope, grounding from `roadmap-reconciliation` §"Phase 3" — the richest correction set, 5 risks RV-R1…5, deliverables, release gate ~`v0.20.0`). The **three grounding corrections** are locked: **RV-1** wrap the hand-rolled ~280-LOC REINFORCE-with-KL loop (`clawgym-on-spark-grpo`), NOT Unsloth/NeMo-RL (fallback lane only); **RV-4** held-out-every-≤10-steps **hard gate** + held-out-only checkpoint selection (the 81.8 pp `t2po` pool↔held-out inversion); **RV-5** pin vLLM + restart-elimination (≈3.5 min/step) as the top wall-clock win (trainer is only ≈22 s). New abstractions `fieldkit.rl` + `fieldkit.reward`. **Scope boundary:** v1 ships the *loop* (one vertical), NOT the storefront (publishable kinds deferred) and NOT full-parameter GRPO (frontier HW, `_FLOWS` §6). **Spec-only, UNBUILT.** Propagated to `_SPECS/index.md` (Active specs table + Planned queue → all four ✅) + `_FLOWS` §3 Phase 3 ("Abstraction added" now-written pointer) + §7 (all-four-written + the grounding bullet's RV-mapping) + header (UPDATE 5). **The roadmap's spec-authoring backlog is now EMPTY — the next advance is a BUILD, starting with M9 cost plane (the prerequisite-chain root).**

### 2026-06-02 (Arena M11 autonomous-harness locked decisions WRITTEN — `spark-arena-v1.md` §15)
Third prioritized stub landed — the **hands** of `pane → hands → engine`. **Placement was a user-green-lit fork** (`AskUserQuestion`): the roadmap text + §12.6/§13.6/§14.6 sequencing named a standalone `_SPECS/autonomous-harness-v1.md`, but in practice all four roadmap items (M8/M9/M10) had landed as Arena sections — user chose **Arena §15 = M11** for consistency (Phase 2's build is the autonomy layer *over* the M8 pane: drains the M8 queue, renders the standup from the M8 board + M9 ledger, emits M8 triggers). Drafted `autonomous-harness-v1` (Phase 2 / Bet 2) **code-reconciled against the built `fieldkit/src/fieldkit/` + the live `.claude/`** — surfacing 6 facts: the **drain + regression-producer are already built** (`arena/jobs.py` `drain_jobs()`/`check_and_enqueue_regressions()`, both commented "the Phase-2 cron will call this on a schedule" — M11 adds the *scheduler*, not dispatch); **`fieldkit.budget` does NOT exist** (the new module M11 introduces); **`fieldkit.cost` (M9) is also unbuilt** so the governor's `$/task` has no source yet (⇒ M9 a **soft** prerequisite, AH-5); **exactly one hook today** (repo `SessionStart`; `~/.claude` hooks `{}`; the §6.5 Stop hook named-but-uninstalled); `scheduled_tasks.lock` is a **stale dead-session lock**, not a scheduler; arena.db at `user_version=4` and M11 adds **no schema** (AH-9). Wrote **§15** (9 decisions AH-1…9, deliverables, no-schema architecture [scheduler flow · hook battery · governor decision · standup render], grounding from `roadmap-reconciliation` §"Phase 2" + `_FLOWS` §3 Phase 2, risks R24–R26, what M11 feeds). New abstraction **`fieldkit.budget`** (`BudgetGovernor`/`BudgetDecision`/`SpendDigest`/`check_budget`; `LOCAL_CEILING=33%` failure-mode escalation). **Scope boundary (AH-7):** M11 ships the *scheduler+hooks+budget+standup* over what the dispatcher already supports — it does **NOT** promote `rl_run` (Phase 3) or `reindex`/`rag_eval`/`scout_ingest` (M10) to dispatchable. Renumbered Change-log→§16, References→§17; added the §16 change-log row + a `~v0.19.0` release gate. **Spec-only, UNBUILT.** Propagated to `_SPECS/index.md` (Planned queue: cost ✅, second-brain ✅, autonomous-harness ✅, rlvr ▶ next/last) + `_FLOWS` §3 Phase 2 (now-written pointer) + §7 (three-of-four written) + header (UPDATE 4). **Last stub to write: `rlvr-loop-v1` (Phase 3 — the engine), likely standalone.**

<!-- 2026-06-02 Arena-M10-recall-layer-WRITTEN entry (`spark-arena-v1.md` §14: 12 decisions M10-1…12 signed off; promote the pre-drilled `reindex`/`rag_eval`/`scout_ingest` job sockets to DISPATCHABLE; `fieldkit.memory` over the existing `fieldkit.rag.Pipeline` [adopted canonical, resolving the external-`ingest_blog.py` fork]; multi-source provenance card on `blog_chunks`; eval-gated re-index wrapping the on-disk `rag-eval-work/` harness; arena.db `5→6` contingent on M9's `4→5`; 4 open calls signed off "recommended"; ~v0.18.0 gate) pruned per "keep ~2 latest" — output live in `_SPECS/spark-arena-v1.md` §14 + `_SPECS/index.md` Planned queue. Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Arena-M9-cost-plane-WRITTEN entry (`spark-arena-v1.md` §13: 10 decisions M9-1…10 signed off; persist the ephemeral `_compare_cost_usd()`; per-side cost + `tokens_in` on `compare_responses`; public `$/quality-point` on `leaderboard_rows`; `openrouter_price_snapshot` pinned from H6 evidence; schema `4→5` first ALTER-migration R18; new `fieldkit.cost`; ledger-not-governor scope; VC the untracked H6 evidence M9-10; ~v0.17.0 gate) pruned per "keep ~2 latest" — output live in `_SPECS/spark-arena-v1.md` §13 + `_SPECS/index.md` Planned queue. Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 `_FLOWS` spec-stub-prioritization entry (post-M8 write order locked: cost→second-brain→autonomous-harness→rlvr; cross-bets-5/6-feed-Phase-2 rationale) pruned per "keep ~2 latest" — its order is now executing (cost ✅ §13, second-brain ✅ §14) and is live in `_SPECS/index.md` Planned queue + the ▶ NEXT UP block; recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Phase-1-Arena-launch-article entry (first `product-writer` launch since M7; new article `products/arena-control-plane/` `series: Cockpit`, positioning-first, build-metrics ~2.6h sprint / 95.9% cache, 3-shot `/arena/jobs/` tour 2× DPR, cross-links M7 + H4; tagline≤120/summary≤300 gotchas; `606333d`) pruned per "keep ~2 latest" — shipped + pushed; output live in `products/arena-control-plane/` + the §5 series ledger. Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 fieldkit-v0.16.0-CUT entry (full-remediation release via `fieldkit-curator`: tag `fieldkit/v0.16.0` + PyPI, both install-verifies green; remediated CHANGELOG 3-milestone drift [M6/M7/M8] + missing `arena` landing tagline + `audit_landing.py` `fieldkitModules`-indirection regex; version skip 0.13.0→0.16.0 intentional, fresh tag lineage; suite 1129 pass; wheel ships baked `_webui/`; stats LOC 35,446→37,146; `f50267f`) pruned per "keep ~2 latest" — output live in `fieldkit/CHANGELOG.md` + on PyPI. **Open thread it left: `fieldkit-curator` SKILL.md still hardcodes the archived `ai-field-notes.git` install URL + `/tmp/fk` venv — adapted live to monorepo URL + `/tmp/fk-m8`; worth patching the skill.** Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Arena-M8-side-by-side entry (browser-use walk of `/arena/jobs/` found + FIXED two gaps: #1 `eval_rerun` bench-path resolver [filesystem bench registry + `BenchNotRegistered`], #2 regression producer `check_and_enqueue_regressions` + `leaderboard_baseline` table [user_version 3→4] + `POST /api/jobs/check-regressions` + scan button; +11 tests → 1129 pass; browser E2E 0 console errors; `d6ce51a`) pruned per "keep ~2 latest" — output live in `_SPECS/spark-arena-v1.md` §12.7 + `fieldkit.arena.jobs` + the shipped v0.16.0. Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Arena-M8-BUILT entry (all 8 decisions green-lit M8-1…8; new `fieldkit.arena.jobs` dispatcher + store schema 2→3 + 2 new MCP tools + `/arena/jobs/` board; suite 1118 pass) pruned per "keep ~2 latest" — its output is live in `_SPECS/spark-arena-v1.md` §12.7 as-built map, the side-by-side-FIXED entry above, and the now-shipped `fieldkit v0.16.0` (top entries); recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Specs—Arena M8 control-plane milestone WRITTEN entry pruned per "keep ~2 latest" — its output is live in `_SPECS/spark-arena-v1.md` §12 (8 locked decisions M8-1…8, schema, dispatcher/trigger-producer arch, R13–R17) + the as-built §12.7, and it directly grounded the now-BUILT-and-FIXED M8 control plane (top two entries); commits `5f80fe9` + audit follow-up. Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Bet-6 cost-plane entry pruned per "keep ~2 latest" — its output is live in `_FLOWS/the-machine-that-builds-machines.md` (Bet-6 section + §4 table rows + §7 drift bullet, committed `ea3ed0d`); names `fieldkit.cost` + stub `_SPECS/cost-plane-v1.md` (named, not written). Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Roadmap/spec corpus-harvest entry pruned per "keep ~2 latest" — its output is live (`_SPECS/roadmap-reconciliation.md` + the `_FLOWS` §3 per-phase *Measured grounding* notes + §7 drift bullet) and it directly grounded the now-written Arena M8 spec (top entry); recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Skill-path-drift entry (stats output repointed to `src/data/field-notes/project-stats.json`; `refresh_readme.py` + `tech-writer` `index` mode deleted; LOC KPI simplified to fieldkit-only `35,446`; `project-stats.json` path swept across skills + guides) pruned per "keep ~2 latest" — shipped + pushed (ancestor of `4e5a67b`); recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Doc-consolidation entry (`_GUIDES/`+`_SPECS/` reorg, INDEX files, `specs/`→`_SPECS/` reference rewire, `handoff/`+`mirrors/`+dead-sync-docs deleted) pruned per "keep ~2 latest"; its outcome is live in CLAUDE.md's doc map + `_FLOWS/the-machine-that-builds-machines.md` §7 + `_GUIDES/index.md`/`_SPECS/index.md`; recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 WORKFLOWS-roadmap-resequence entry pruned per "keep ~2 latest" (§3 pane→hands→engine phasing, MCP-harness-already-built correction, Ch-14 fold-in, article-overlay strategy — all now live in `_FLOWS/the-machine-that-builds-machines.md` §1/§3/§5 + the Editorial-overlay open-item above; its "stale uber-corpus memory" sub-note is RESOLVED — the memory now points at the existing archive path); recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-01 WORKFLOWS.md-authoring entry pruned per "keep ~2 latest" (its roadmap + key-insight content is superseded by the 2026-06-02 resequence; recover via `git log -p HANDOFF.md`. -->

<!-- 2026-05-31 catalog-narrative entry (patent-strategist lanes + 4 older-quant positioning) pruned per "keep ~2 latest"; recover via `git log -p HANDOFF.md`. -->
<!-- 2026-05-29 Orionfold LLC go-live entry pruned per "keep ~2 latest"; recover via `git log -p HANDOFF.md`. -->
