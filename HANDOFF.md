<!--
  Session handoff for ainative-business.github.io (Spark-operated monorepo).
  Convention:
  - Replace "Open items" each session; do NOT append.
  - "Recent decisions" is a short running log — keep ~2 latest, prune older.
  - Full history of any pruned entry is recoverable via `git log -p HANDOFF.md`.
  - Last pruned/reset: 2026-06-01.
-->

# HANDOFF — ainative-business.github.io

## Current state

- **This repo is the single Spark-owned monorepo** at `/home/nvidia/ainative-business.github.io` — build workspace *and* website. The old two-repo `ai-field-notes`→Mac sync model is **retired**; author directly here (do NOT use `sync-field-notes`).
- **Live** at `ainative.business` (GitHub Pages). `origin/main` tip = **`606333d`** (Phase-1 Arena launch article — see top DONE block), atop `931b941` (HANDOFF queue) + `a498411` (stats/HANDOFF post-v0.16.0) + **`f50267f`** (**`fieldkit v0.16.0` cut**: the first Arena release M1→M8, tag `fieldkit/v0.16.0` + <https://pypi.org/project/fieldkit/0.16.0/>; the M8 control plane is BUILT → side-by-side-FIXED → SHIPPED — see the top three Recent decisions + spec §12.7), atop `d6ce51a` (M8 #1/#2 bench-registry + regression-producer fixes), `6755ea7` (Arena M8 control plane BUILT: `fieldkit.arena.jobs` dispatcher + cockpit `/arena/jobs/`), `820f9e1`/`5f80fe9` (the M8 spec + fieldkit-reconciliation), `57f2155` (M8 spec authored), `ea3ed0d` (Bet-6 cost plane), and the earlier **Flows rename + ALL-CAPS-folder normalization** `8b16503`. **Nothing unpushed.** NB the M8 commit changed `fieldkit/` + `arena-app/` only — the public `dist/` is unchanged until the running cockpit is rebaked (`fieldkit arena build --repo-root arena-app`) and/or a marketing-site deploy runs. The Flows rename (`8b16503`): `WORKFLOWS.md` `git mv`'d into the new `_FLOWS/` folder as `_FLOWS/the-machine-that-builds-machines.md` (the "Flows" stream: process flow, data flow, operator flow-state — "Flows", not the CC-`Workflow`-tool-colliding "Workflow", names this body of work), and the remaining ALL-CAPS-folder holdouts normalized to lowercase-kebab — `_GUIDES/{INDEX,NARRATIVE-CONTRACT,PRODUCT-ARTICLES}.md` + `_SPECS/INDEX.md`. Every live path reference remapped across skills (`.claude/skills/**`), `fieldkit` source/docs/tests, `scripts/`, `src/content.config.ts`, `src/styles/products.css`, `src/content/artifacts/README.md` (dead `/Volumes/...` link repointed repo-relative), `CLAUDE.md`, the two `index.md` files, `_SPECS/roadmap-reconciliation.md`, and the meta-program transcript. **Deliberately NOT rewritten** (append-only / immutable, per `_SPECS/index.md`'s own note): `fieldkit/CHANGELOG.md` and `_SPECS/archive/**`. Bare proper-noun rule mentions ("NARRATIVE-CONTRACT Rule 8", no `.md`) left as the contract's display name. Build green (490 pages), both verifiers pass. Convention going forward: files inside `_FLOWS/`/`_GUIDES/`/`_SPECS/` are lowercase-kebab (memory `feedback_flows_folder_naming`). M9 + M10 are **committed + pushed + RELEASED** in `fieldkit v0.18.0` — `origin/main` tip = **`b7b3d5d`** (stats+HANDOFF post-v0.18.0), atop `a97eda6` (`fieldkit v0.18.0` cut: tag `fieldkit/v0.18.0` + <https://pypi.org/project/fieldkit/0.18.0/>, both install-verifies green) + `ebe6425` (M10 recall layer) + `4da71ef` (M9 cost plane). **M11 build COMMITTED + PUSHED** (this session — see top Recent decision + ▶ NEXT UP item 3 + §15.7 as-built): **`origin/main` tip = `52c8085`** (`feat(arena): build M11 autonomous harness …`, atop `b7b3d5d`) — new `fieldkit/src/fieldkit/budget.py`, `fieldkit/src/fieldkit/arena/scheduler.py`, `fieldkit/docs/api/budget.md`, `fieldkit/tests/{test_budget.py,arena/test_scheduler.py}`, `arena-app/src/{components/arena/StandupPane.jsx,pages/arena/standup.astro}`, `.claude/hooks/{secret_scan,pre_commit_guard,post_publish,stop_feedback}.sh`, + edits to `fieldkit/src/fieldkit/arena/{jobs,server}.py`, the two `content.config.ts` + `audit_docs.py` enums, `fieldkit/CHANGELOG.md`, `fieldkit/docs/api/arena.md`, `_SPECS/spark-arena-v1.md` §15/§15.7, `arena-app/src/layouts/ArenaAppLayout.astro` (nav + CSS), `.claude/settings.json` (hook battery). Suite **1183 pass**, build **495 pages**, verifiers green, `_webui` rebaked (gitignored), `/api/standup` smoke clean, secret-scan gate clean on the staged diff. **No schema change (AH-9 — arena.db stays at `user_version 6`).** GitHub Pages auto-deploys on push. **M11 is now RELEASED in `fieldkit v0.19.0`** (2026-06-03, tag `fieldkit/v0.19.0` + <https://pypi.org/project/fieldkit/0.19.0/>, both install-verifies green) — atop **`9727c15`** (`fieldkit v0.19.0: M11 autonomous harness …`). **Phase 3 RLVR (`rlvr-loop-v1`) is now BUILT** (2026-06-03, this session — `origin/main` tip = **`1613899`** `feat(rl): build rlvr-loop-v1 …`, atop `daecd6b`): new modules **`fieldkit.reward`** + **`fieldkit.rl`**, the `rl_run`/`requant` `DISPATCHABLE` promotion + `run_rl_loop`/`requant_checkpoint` harness tools, docs `rl.md`/`reward.md`, both `content.config.ts` enums + `audit_docs.py MODULES` + `FieldkitModules.astro` taglines, CHANGELOG `[Unreleased]`. **No schema change (RV-8 — arena.db stays at `user_version 6`).** Suite **1211 pass**, build **497 pages**, verifiers green, audit-docs reward 4/4 + rl 3/3 clean, audit-landing 4/4, `_webui` rebaked. **This is the FINAL `_FLOWS` §3 build** — the whole `pane → hands → engine` roadmap is now built. **Staged, NOT released**: the `fieldkit v0.20.0` cut is the next action. The two pre-existing untracked items (`.claude/scheduled_tasks.lock`, `src/data/arena-mirror/`) — leave alone.
- **Build/verify loop:** `node node_modules/astro/astro.js build` (497 pages; `npm run build` is broken on this checkout per `reference_astro_build_smb_symlink_break`) → `node scripts/verify_artifact_rendering.mjs` (18 pages/7 kinds) + `node scripts/verify_field_notes_rendering.mjs` (412 explainers/63 pages). `build:og` is **CI-only** (needs Chrome); CI regenerates OG on push.
- **arena-app/ + fieldkit/ build separately** from the marketing site — pushing arena/fieldkit changes does NOT change the public `dist/`. After any `arena-app/` edit, rebuild the served bundle: `fieldkit arena build --repo-root arena-app` (bakes `_webui`, gitignored).
- **Secrets:** `.env.local` (gitignored, chmod 600) holds `PYPI_TOKEN`, `HF_TOKEN`, `OPENROUTER_API_KEY` — `fieldkit-curator` + `hf-publisher` auth here.

**Recently shipped (all live):** **MTBM-arc opener `the-meta-program-on-spark` (`bca63e7`, pushed)** — thesis-spine deep-dive (pane→hands→engine + Ch-14 meta-program; `book_chapters:[10,11,14]`; new signature `MetaProgramRecursion`; inline fn-diagram; 8 explainers); closes the editorial-overlay "Now (not gated)" item below; patent-strategist `lane_summary`+`siblings` (`9df9438`); positioning+`known_drift` backfill on the 4 older vertical quants (`ddfa492`); per-product OG card for `/products/` (`0cb175d`); Arena marketing refresh — demo bundle + launch article + landing (`0bf2ec7`); Orionfold LLC brand pass + go-live (deploy `26664480875`). Arena cockpit/leaderboard/telemetry work is committed on `origin/main` (arena-app only).

## ⚙️ Live runtime

- **Arena cockpit is DOWN** (torn down 2026-06-02 via `arena_lifecycle.sh down` after the Phase-1 launch screenshot pass — `:7866` dead, no process). It was up earlier to capture the M8 `/arena/jobs/` shots; `arena.db` was seeded with 6 demo jobs and **cleaned back to its real state afterward** (0 jobs, 14 leaderboard rows), so the db is publish-clean. Bring it back (M8 `_webui` bake, venv `/tmp/arena-venv`, db `~/.fieldkit/arena.db`): `.claude/skills/arena-lifecycle/scripts/arena_lifecycle.sh restart [--browser]`. NB the lifecycle verbs are `up | down | restart | status` (not `stop`).
- **pgvector** container UP (`:5432`, db `vectors`, table `blog_chunks`) — backs the Second Brain index.
- **NIM embedder UP** (`nim-embed`, `llama-nemotron-embed-1b-v2`) on host **`:8001`** via **bridge port-map `-p 8001:8000`** (NOT `--network host` — that collides with Triton's gRPC port 8001; see memory `reference_second_brain_reindex`). The Second Brain MCP needs it for query-time embedding; `docker rm -f nim-embed` to free it (~tiny, 1B). The throwaway dist server `:8099` + visible Chromium CDP `:9222` are torn down.

## Open items

### ▶ NEXT UP — **ALL FOUR `_FLOWS` §3 builds DONE + SHIPPED** (M9·M10·M11 + RLVR engine, all on PyPI); **the queued `v0.20.1` patch is CUT**; **Phase-3 editorial launch is HALF-DONE** (deep-dive SHIPPED, living-model product STAKED upcoming); **the GPU-backend follow-on is now VENDORED in code** (`fieldkit.rl.gpu_seams` is real, behind the new `fieldkit[rl]` extra — committed direct-to-main the HEAD `feat(rl): vendor the real GPU backend …` commit, **NOT yet pushed**; staged in `[Unreleased]`; see top Recent decision); remaining = **the operator-armed live run** (install `fieldkit[rl]` + bring up a pinned vLLM + an overnight `rl_run` drain) → which **unblocks promoting the living-model launch** (the real `fieldkit.lineage` delta chart)
**`origin/main` tip = the stats + HANDOFF post-v0.20.1 commit**, atop the v0.20.1 cut `55a2455` (`fieldkit v0.20.1: fix rag_eval_index NameError`, tag `fieldkit/v0.20.1` + <https://pypi.org/project/fieldkit/0.20.1/>), atop `47e9812` (the Phase-3 editorial-launch HANDOFF), atop `3c1010d` (living-model upcoming placeholder + ProductLayout upcoming-render guard), atop `420b75d` (the `the-machine-improves-itself` deep-dive + `ClosedLoopRlvr` signature).
**Decision 2026-06-03:** the `pane → hands → engine` roadmap is **fully built AND fully released**. Phase 1 (Arena M8 control plane) shipped (`fieldkit v0.16.0`); **M9 (cost plane) + M10 (recall layer) shipped in `v0.18.0`**; **M11 (autonomous harness) shipped in `v0.19.0`**; and **Phase 3 RLVR (`rlvr-loop-v1`) is now RELEASED in `fieldkit v0.20.0`** (2026-06-03, this session — tag `fieldkit/v0.20.0` + <https://pypi.org/project/fieldkit/0.20.0/>, both install-verifies green; `origin/main` tip = **`b3253e9`** (Orionfold Cortex rename + live demo + post-launch polish: 404 fix · `/arena/cortex/` route · interact-coach — see top Recent decision) atop `8f3a2ad`/`236f817` (Cortex product + demo bundle) atop `442b331` (catalog-only harness card fix) atop `585fd3c`/`9c687c5` (Phase-A dogfood + launch articles) atop `d393073` (stats refresh) atop `9a1de39` (the v0.20.0 cut) atop `66696a7`/`1613899` (the RLVR build); see `_SPECS/rlvr-loop-v1.md` §10.1 as-built). **The engine was the last roadmap stub — there is no next *build* in the four-phase arc, and no staged release pending.** The open threads are now the **Phase-3 editorial launch** (the §5 "living-model" `product-writer` piece + the MTBM RLVR `tech-writer` installment — see Editorial overlay below) and the **deliberate operator follow-on**: vendor the real GPU backend (`fieldkit.rl.gpu_seams` raises until a pinned aarch64+CUDA-13 vLLM + the `clawgym` REINFORCE loop land in the `fieldkit[rl]` extra; v1 ships the orchestration with injectable seams). The four builds:

1. ✅ **`cost-plane-v1` (Bet 6 → Arena M9) — BUILT 2026-06-02.** All 10 decisions M9-1…10 landed as designed (`_SPECS/spark-arena-v1.md` §13 + the new **§13.7 as-built map**). New module **`fieldkit.cost`** (`CostLedger`/`PriceSnapshot`/`seed_price_snapshot`/`cost_per_quality`/`CostError`); schema **`user_version` 4→5** (first ALTER migration, R18, round-trip-tested on a seeded v4 db); server persists per-run `cost_usd`/`tokens_in`/`tokens_estimated`/`price_snapshot_id`; `rebuild_leaderboard` computes `mean_cost_usd`+`cost_per_quality_point`; cockpit cost cells (compare + LiveLeaderboard `$/task`/`$/quality` + restart-surviving spend rail); mirror-safe (`openrouter_price_snapshot`→`PUBLISHABLE_TABLES`, per-run cost off it, 4 new leak anchors); H6 evidence version-controlled (M9-10). **Verified:** suite **1142 pass** (+13), `audit-docs cost 5/5 + arena` clean, `astro build` **493 pages** (new `/fieldkit/api/cost/`), both verifiers green, arena cockpit bundle **rebaked**. **RELEASED in `fieldkit v0.18.0`** (2026-06-02, tag `fieldkit/v0.18.0` + PyPI — bundled with M10; v0.17.0 was skipped). It's the hard prerequisite for the Phase-2 budget governor.
2. ✅ **`second-brain-pipeline-v1` (Bet 5 → Arena M10) — BUILT 2026-06-02.** All 12 decisions M10-1…12 landed (`_SPECS/spark-arena-v1.md` §14 + the new **§14.7 as-built map**). New module **`fieldkit.memory`** (`__all__` = `MemoryIndex`, `KnowledgeCard`, `Provenance`, `ingest_sources`, `coverage_report`, `resolve_qa_set`, `MemoryError`); arena.db **`user_version 5→6`** (new `reindex_runs` + `rag_eval_runs` tables, round-trip-tested on a seeded v5 db); pgvector `blog_chunks` provenance card via `MemoryIndex.ensure_schema` (idempotent ALTER, R21). The 3 job kinds (`reindex`/`rag_eval`/`scout_ingest`) **promoted to `DISPATCHABLE`** + 3 new harness MCP tools (`memory` surface) + the recall@k **promotion gate** (M10-6). `ask_second_brain` + the version-controlled SB server retrieve through the **single `fieldkit.memory` backend** with a provenance trust-tier filter (M10-9). New **`/arena/knowledge/` cockpit pane** (coverage·freshness — the `article_index`⋈index diff, M10-8 · re-index button · RAG-eval trend cosine-only · provenance query console). Mirror: `rag_eval_runs` aggregates→`PUBLISHABLE_TABLES`, `reindex_runs`→`FORBIDDEN_TABLES` (+ sentinel). External RAG assets version-controlled (M10-12) under `articles/rag-eval-ragas-and-nemo-evaluator/evidence/`. **Verified:** suite **1153 pass** (+11), `audit-docs memory 7/7 + arena` clean, `astro build` **494 pages** (new `/fieldkit/api/memory/`), both verifiers green, cockpit `_webui` **rebaked** (knowledge pane bakes). **Reconciliation:** `MemoryIndex` owns the canonical `(slug,chunk_idx)`-keyed `blog_chunks` (not `rag.Pipeline`'s `id`-keyed `chunks` table) so the eval gold set + SB server keep working — one version-controlled ingest, eval continuity preserved. **Operator actions left** (guard-declined live-infra): (a) the first `/api/knowledge/reindex` runs `ensure_schema()` on the shared `vectors` pgvector db → backfills provenance columns (until then the query console is a clean 503 + the pane shows an `index_note`); (b) deploy `…/evidence/second-brain-server.py` over the live `/home/nvidia/second-brain-mcp/server.py` + `pip install -e fieldkit` in its venv. **RELEASED in `fieldkit v0.18.0`** (2026-06-02, tag `fieldkit/v0.18.0` + <https://pypi.org/project/fieldkit/0.18.0/>, both install-verifies green; bundled with M9). It is the hard prerequisite for the Phase-2 freshness monitor + closes the §7 12-of-63-staleness / external-script drift.
3. ✅ **`autonomous-harness-v1` (Phase 2 — the hands) — BUILT 2026-06-02 (this session).** All 9 decisions AH-1…9 landed as written; full as-built table at `_SPECS/spark-arena-v1.md` **§15.7**. New module **`fieldkit.budget`** (`__all__` = `BudgetGovernor`, `BudgetDecision`, `SpendDigest`, `EscalationReason`, `MemoryEnvelope`, `check_budget`, `BudgetError`) — the **brake** (allow/escalate/defer) the drain consults before each job; `LOCAL_CEILING=33%` failure-mode escalation (AH-4); M9 cost plane a **soft** prerequisite (AH-5: `CostLedger` wired → `$/task`+ceiling; absent → token+`MemoryEnvelope` guard); store-agnostic by duck-typing (never imports `fieldkit.arena`). New **`fieldkit.arena.scheduler`** (`DrainLock` one-drain-at-a-time w/ stale-pid steal · `run_drain_cycle` = drain+freshness-sweep+standup · `build_standup` = Ran/Regressed/Queued/Spend, aggregate-only no `payload_json`). **`drain_jobs` gained an optional `governor`** (the one cross-module change — a non-`allow` verdict releases the claim back to `queued`, writes a `budget_<action>` `job_triggers` audit row, breaks the pass; `governor=None` = exact M8/M10 behavior). New **`GET /api/standup`** (read-only render, never drains) + **`/arena/standup/` cockpit pane** (Spend rail + buckets, stage-only). **Hook battery** in repo `.claude/`: `pre_commit_guard.sh` (PreToolUse → secret-scan **hard-blocks**, verifiers **advisory** R25), `post_publish.sh` (PostToolUse → stats nudge + freshness enqueue), `stop_feedback.sh` (§6.5 loop), `secret_scan.sh` (shared) — all deterministic shell, wired into `.claude/settings.json` (**live for this repo now**). **No new arena.db table / no `user_version` bump** (AH-9 — schema stays at M10's `6`). 3 risks R24–R26 honored. **Verified:** suite **1183 pass / 16 skip** (+30: `test_budget.py` + `test_scheduler.py`), `audit-docs budget 7/7 + arena` clean, `astro build` **495 pages** (new `/fieldkit/api/budget/`), both verifiers green, cockpit `_webui` **rebaked** (standup page + StandupPane bundled + nav tab), `/api/standup` smoke (200, no leak, fresh-box empty-not-404), secret-scan blocks a planted secret. **Operator action left** (deliberate non-default): the `run_drain_cycle` glue ships but **no cron is registered** — wiring `/schedule` (or a `~/.claude` routine) to call it overnight is an explicit operator opt-in (the autonomy gate stays human-armed). **RELEASED in `fieldkit v0.19.0`** (2026-06-03, tag `fieldkit/v0.19.0` + <https://pypi.org/project/fieldkit/0.19.0/>, both install-verifies green; suite 1183 pass, audit-docs 15/16 + audit-landing 4/4). It drains M8 overnight, enforces M9's ledger, refreshes M10 on schedule, and is the execution home for the Phase-3 engine.
4. ✅ **`rlvr-loop-v1` (Phase 3 — the engine) — BUILT 2026-06-03 (this session).** All 10 decisions RV-1…10 landed; full as-built table at `_SPECS/rlvr-loop-v1.md` **§10.1**. **New module `fieldkit.reward`** (`__all__` = `Reward`, `RewardAdapter`, `group_advantage`, `RewardError`) — the verifier-IS-the-reward thin adapter over the 7 `fieldkit.eval` scorers; the `(success, failure_class, auxiliary)` tuple **reuses the built `fieldkit.lineage.FailureLabel`** (no new enum); `group_advantage` = the value-network-free GRPO baseline (degenerate group → zero vector). **New module `fieldkit.rl`** (`__all__` = `GRPOConfig`, `RLLoop`, `RLLoopError`) — wraps the proven hand-rolled REINFORCE-with-KL loop (NOT Unsloth/NeMo-RL, RV-1) as **orchestration with injected GPU seams** (`sampler`/`trainer`/`heldout_eval` — torch/vLLM never import at module load); **held-out gate every ≤10 steps with held-out-ONLY checkpoint selection** (RV-4, the 81.8 pp `t2po` inversion defense — a test proves it picks the held-out-best step while the pool climbs monotonically past it); pinned vLLM (RV-5); **≥100-row corpus floor** + frozen split before step 0 (RV-10). **`gpu_seams` raises until the real GPU backend is vendored into `fieldkit[rl]`** (pinned aarch64+CUDA-13 vLLM + the `clawgym` REINFORCE loop — `[[project_verl_atgpo_vllm_gap]]`); v1 ships the loop, callers inject seams. **Arena seam (RV-6):** `rl_run`/`requant` promoted to `DISPATCHABLE` (now `== ALL`) + `default_runner` branches + `_persist_rl_run` + new `run_rl_loop`/`requant_checkpoint` harness MCP tools; **async/overnight only** — the `server.py` `POST /api/jobs` allowlist stays narrow (no synchronous 8.5 h click); `rl_run` enqueues programmatically + drains under the M11 cron. **No arena.db schema change** (RV-8 — `user_version` stays 6; verified by a test). Publishable kinds deferred to second reuse (RV-9 — `ARTIFACT_KINDS` stays 8). **Verified:** suite **1211 pass / 16 skip** (+28: `test_reward.py` + `test_rl.py` + `test_jobs.py` extension), `audit-docs reward 4/4 + rl 3/3 + arena` clean, `astro build` **497 pages** (new `/fieldkit/api/rl/` + `/fieldkit/api/reward/`), both verifiers green, `audit-landing` 4/4, cockpit `_webui` **rebaked**. **RELEASED in `fieldkit v0.20.0`** (2026-06-03, tag `fieldkit/v0.20.0` + <https://pypi.org/project/fieldkit/0.20.0/>, both install-verifies green; minor bump, two new modules, schema unchanged at `user_version 6`; release-time suite 1222 pass / 5 skip in the fresh `[dev]`+wheels venv). **Operator action left** (deliberate non-default): the real GPU backend (`gpu_seams`) is a documented fast-follow.

**Discipline (closed):** each stub opened with "locked decisions — confirm before build" like M8 §12.1; each was **user-green-lit** ("Build M9/M10/M11 now (full)", then "Build rlvr-loop now (full)"). **All four `_FLOWS` §3 phased builds have now crossed spec → BUILD.** There is **no remaining roadmap build** in the four-phase arc — the next action is the `fieldkit v0.20.0` release cut, then the Phase-3 editorial launch + the GPU-backend vendoring follow-on (above).

### ✅ DONE 2026-06-02 — Phase-1 Arena launch article SHIPPED + PUSHED (`product-writer`)
The Phase-1 launch is **committed + pushed** — `origin/main` tip = **`606333d` "Add product launch: Orionfold Arena — Control Plane"** at **`products/arena-control-plane/product.md`** (decision: **new article**, user-confirmed, not an extend of the M7 `orionfold-arena` launch). Working tree clean.
- **What shipped (`606333d`):** `products/arena-control-plane/` — `product.md` (`series: Cockpit`, positioning-first, 7-beat blueprint, cross-links the M7 launch + the H4 deep-dive), `assets/build-metrics.json` (mined), `screenshots/{01-jobs-board,02-dispatch-and-regression,03-board-columns}.png` (2× DPR, captured against the live `:7866` cockpit; `arena.db` seeded then cleaned). Gate at commit time: `verify_product_article.sh arena-control-plane` **PASS**; `astro build` **492 pages**; both rendering verifiers pass.
- **Build-metrics (honest, mined):** M8 was a **~2.6 h sprint** (spec-lock `57f2155` → release `f50267f`), 2 sessions / 107 turns, **18.6M tokens processed, 95.9% cache**, 104k generated; **1,762 lines authored source** + **35 dispatcher/API tests**; built **100% on Opus 4.8**. `feature_count: 3`. No `project-stats.json` refresh needed (the "products" bucket = NVIDIA-products-covered; pipeline scans `articles/**` only).
- **§5 series ledger now has its first per-phase launch entry.** Phases 2 & 3 (below) remain gated on those phases shipping.

### Operator actions (you own these)
- ✅ **`ai-field-notes` archived read-only** (2026-06-02). Git history + the 92M research-evidence layer preserved; local copy kept at `/home/nvidia/archive-ai-field-notes`. Mac PR #11 is now moot (repo is read-only).
- **PSI authenticated key** still missing (blocks PageSpeed in `/seo-monitor`).

### Destination work
- **#7 remainder — human-eye browser pass on the LoRA/adapter/dataset detail pages.** The 4 quant pages were browser-verified 2026-05-31 (0 console errors); the LoRA (`/artifacts/loras/patent-strategist-v3-nemo/`), adapter, and dataset detail + empty-state listing pages have only HTML-level verifier coverage, no human-eye/Lighthouse pass yet.
- **#11 — bakeoff article's gated catalog footer last-write-wins.** `chrome_footers.collect_gated_articles()` keys by article-slug, so when multiple manifests bind one article the alphabetically-last wins (currently points at the surviving NeMo lane). Low priority; fix when the next multi-binding case lands.
- **#18 — patent-strategist W3 fine-tune** (source-side, ETA ~2 weeks). Likely ships more `kind: lora` manifests; the render path + notebooks-as-artifacts scaffold are ready (new manifests can carry `notebooks:{colab,kaggle}` with no code change). NB future quant *republishes* get `positioning`+`known_drift` only — NOT `stack_origin`/`lane_summary`/`siblings` (those are for Orionfold fine-tunes; see 2026-05-31 decision).

### Editorial overlay — the-machine-that-builds-machines journey → article series (sequenced at roadmap extraction points)
The book is **published** (Amazon / Leanpub / Orionfold) — do **NOT** edit `src/data/book/chapters/**`. Overlay the "machine that builds machines" journey as **articles** instead, threading the published chapters via the `book_chapters: [10, 11, 14]` frontmatter cross-link (editorial reference, **not** a series retag, **not** a book edit — H4 precedent). It's a **hybrid of two genres** stitched by existing machinery: deep-dives via `tech-writer` (the **MTBM arc** A1–A9 + the **Harnesses series** H1–H6 already exist), launches via `product-writer` (`products/**`, `series: Cockpit`, build-metrics + feature tour). Extraction points, mapped to `_FLOWS/the-machine-that-builds-machines.md` §3 phases:
- ✅ **DONE 2026-06-02 — thesis-spine deep-dive SHIPPED** as `the-meta-program-on-spark` (`bca63e7`, pushed, published in full): the `pane → hands → engine` arc + the Ch-14 *meta-program-as-operator-instance* framing, `book_chapters:[10,11,14]`, signature `MetaProgramRecursion`, inline fn-diagram, 8 explainers. This was the book-overlay opener; the gated Phase-1/2/3 items below remain.
- ✅ **DONE 2026-06-02 — Phase 1 ship — Arena control plane** → `product-writer` launch **SHIPPED + PUSHED** as **`products/arena-control-plane/`** (`606333d`, `series: Cockpit`): M8 build-metrics infographic + 3-shot `/arena/jobs/` feature tour (board · dispatch+scan-regressions · cards-with-results), cross-linking the M7 launch + the H4 deep-dive. The §5 series now has its first per-phase launch entry; Phases 2 & 3 below remain gated on those phases shipping.
- **Phase 2 ship — autonomous harness** → the Harnesses deep-dives **already exist and are published** (H3 `hardening-the-hermes-harness-on-spark`, H4 `hermes-drives-the-spark-via-fieldkit-mcp` — the MCP write-surface concept); so the new piece is a `product-writer` launch for the **built** cockpit surface (morning-standup · cron queue · budget-governor) that **cross-links the existing H4**. Gated on Phase 2 shipping.
- ✅/⏳ **Phase 3 ship — closed-loop RLVR** → **deep-dive SHIPPED** `2026-06-03` as **`the-machine-improves-itself`** (`tech-writer`, `series: Machine that Builds Machines`, `book_chapters:[10,11,14]`, `fieldkit_modules:[rl,reward,eval,lineage]`; the eval-harness-is-the-reward thesis, the t2po held-out-inversion defense as the fn-diagram accent, signature `ClosedLoopRlvr`, 10 explainers, + the Looking-Beyond-Spark `:::hardware` close; honest about the `gpu_seams` gap). The first §5 "living-model" `product-writer` launch is **STAKED as `status: upcoming`** at `products/living-model/` (the `fieldkit.lineage` delta chart as hero) — it **promotes to published** when the GPU backend lands + a real `rl_run` produces delta data (no fabricated chart). NB this needed a one-line **`ProductLayout` upcoming-render guard** (hide infographic+gallery for `status: upcoming`, mirroring `ProductCard`'s `isUpcoming`) — the products layout previously rendered the build infographic unconditionally.
- **Mechanism to stake now:** both skills support `status: upcoming` placeholders — drop one per extraction point to claim the slug + commitment, promote to `published` as each phase lands. Each product launch's build-metrics block is the per-phase proof of Ch-14's configuration-over-code economics; collected across the series they're a running ledger of the machine compounding.

### ✅ Roadmap & specs — corpus harvest DONE 2026-06-02 (output 1; output 2 JIT-deferred)
The grounding harvest is complete. **Output (1) — the §3 reconciliation note — is written at `_SPECS/roadmap-reconciliation.md`**, and its sharpest corrections are **folded into `_FLOWS/the-machine-that-builds-machines.md` §3 per-phase *Measured grounding* notes + a §7 drift bullet** (committed). The §3 roadmap has since grown two cross-cutting bets atop the four phased ones — **Bet 5 (recall layer)** and **Bet 6 (cost plane)**. Headline findings: §3's sequencing survives; **two abstractions were wrong** — Phase 3 named Unsloth/NeMo-RL where a **hand-rolled ~280-LOC REINFORCE** is what ran (`clawgym-on-spark-grpo`), and the GRPO bottleneck is the **vLLM restart (~3.5 min/step), not the trainer (~22 s)** — and **one risk was missing**: the **training-pool↔held-out inversion** (`t2po`, 81.8 pp gap → held-out-every-10-steps is now a hard gate). Phase 1 dispatch-through-MCP is **validated end-to-end** (`hermes-drives`, 0% format error). Spec-feedable numbers per stub (`rlvr-loop-v1`, `autonomous-harness-v1`, Arena M8) are in the note's appendix. **Update 2026-06-02: the Arena M8 stub is now WRITTEN** (`_SPECS/spark-arena-v1.md` §12 — see top Recent-decisions entry); the four remaining stubs (`rlvr-loop-v1`, `autonomous-harness-v1`, `second-brain-pipeline-v1`, `cost-plane-v1`) are still named-not-written but **now prioritized** — see the **▶ NEXT UP** block at the top of Open items (write order: `cost-plane-v1` → `second-brain-pipeline-v1` → `autonomous-harness-v1` → `rlvr-loop-v1`).
- **Output (2) — the per-spec evidence index (article + `evidence/` file → spec section) — is deliberately deferred**, built JIT when each spec is written (targeted to that spec's open questions, not harvested cold).
- **Method note:** the Second Brain MCP path *was* unusable — its index held **12/63 articles and none of the roadmap-relevant ones** (stale; its `ingest_blog.py` pointed at the retired `nvidia-learn` path). Reading fell back to disk via 3 read-only Explore agents (Workflow was *not* opted into).
- ✅ **Second Brain RE-INDEXED + reranker resolved 2026-06-02.** Repointed `/home/nvidia/rag-eval-work/ingest_blog.py` `ARTICLES` → monorepo, brought up the embedder, full-rebuild → **50 articles / 313 chunks** (was 12); pipeline verified fully local end-to-end (embed `:8001` → pgvector `:5432`, 0.048s, returns roadmap content). **Reranker:** hosted NGC endpoint is **410 Gone** AND the local `llama-3.2-nv-rerankqa-1b-v2` NIM **has no GB10-compatible profile** (only an ONNX build, crashes `cudaErrorSymbolNotFound`; no `-dgx-spark` image yet). **Resolved:** edited `/home/nvidia/second-brain-mcp/server.py` (outside this repo, not tracked here) — `rerank` now defaults **False** (local cosine), `RERANK_URL` env-overridable → local `:8002`, auth conditional, so `rerank=True` self-enables when a GB10/`-dgx-spark` reranker or frontier HW arrives. `nim-rerank` container torn down. `ask_blog` additionally needs the cached local Llama-3.1-8B generator on `:8000` (not running). Runbook + GB10 finding in memory `reference_second_brain_reindex`.

### SEO watch
- **Re-run `/seo-monitor`** (~1–2 weeks out). Confirm live `sitemap-0.xml` settled at ~182 URLs (no `/field-notes/tags/` or `/stages/`); GSC *Discovered–not-indexed* (355, **stale — those URLs are already indexed** per the 2026-05-30 inspection) should bleed down as the Validate-Fix completes. GSC "unused verification token" `fePoYwMX…` is **HOLD / do-not-remove** (Workspace-owned TXT; removing risks email). Journal: `seo-progress.md`.

### ✅ Skill-path drift — FIXED + PUSHED 2026-06-02 (`6cdefd4`)
The publish-workflow path bug discovered while shipping `the-meta-program-on-spark` is resolved and **shipped (`6cdefd4`, now pushed — ancestor of `origin/main`)**. Net: stats now refresh to the path the site actually reads, the dead README plumbing is gone, and the LOC KPI is honest for the monorepo. Memory `feedback_refresh_stats_on_publish` updated (paths + the 3 decisions). **Only remaining loose thread:** the retired `sync-field-notes` skill still references the *source* repo's old `src/data/project-stats.json` path (lines 32, 213) — left as-is since that skill is itself slated for deprecation (see Cleanup below).

### Cleanup (non-blocking, tied to the retired two-repo model)
- **Dead-sync docs deleted 2026-06-02** (`MAC-TO-SPARK-TRANSITION.md`, `SYNC-WORKFLOW.md`, `mirrors/`, `handoff/`) as part of the `_GUIDES/`+`_SPECS/` consolidation — see the 2026-06-02 decision below.
- All dead `mirrors/destination-overrides.md` references cleared 2026-06-02 (in `_SPECS/spark-arena-v1.md`, `src/content/artifacts/README.md`, `chrome_footers.py`, `site-rendering-rubric.md` — reframed to monorepo reality). Still open: deprecate/remove the now-obsolete `sync-field-notes` skill itself (a separate decision — the skill body is retired but not deleted).
- Optional: relocate the CC memory namespace symlink (`-home-nvidia-ai-field-notes`, 93 files) into this namespace for a fully clean cutover (safe as-is — points into `~/.claude/projects`, not the repo).

## Recent decisions (short running log — prune older)

### 2026-06-03 (GPU backend VENDORED — `fieldkit.rl.gpu_seams` is now real, behind `fieldkit[rl]`; the run stays operator-armed)
The deliberate Phase-3 follow-on. `gpu_seams` no longer raises a "not vendored" stub — it now returns the three real seams (sampler/trainer/heldout) ported from the proven `clawgym-on-spark-grpo` loop (recovered at `/home/nvidia/archive-ai-field-notes/articles/clawgym-on-spark/scripts/{rollout,grpo_train,grpo_loop.sh,...}` — the Explore agent first missed them; they DO exist). **Two new private modules** keep the GPU stack lazy: **`fieldkit._rl_gpu_serve`** (torch-FREE — the HTTP rollout sampler over the local pinned-vLLM OpenAI endpoint via `fieldkit.nim.NIMClient` [the GPU lives in a *separate* vLLM server process], the held-out gate, and the `VLLMLane` kill-and-restart serve lifecycle w/ EngineCore-aware `pkill`; a `_GpuRollout` carries the prompt so the trainer rebuilds token sequences without the bench + duck-types into `RewardAdapter`) + **`fieldkit._rl_gpu_trainer`** (the `fieldkit[rl]`/torch gate — the REINFORCE-with-KL step ported faithfully from `grpo_train.py`: per-token logp over assistant tokens, K3 KL vs a frozen CPU-resident reference snapshot, advantage-weighted loss, AdamW+grad-clip, adapter save — generalized from clawgym's multi-turn agentic trajectories to the single-turn QA `Rollout` contract, kept **resident across steps** so the 7B base loads once not per-step). New **`fieldkit[rl]` extra** (`torch`/`peft`/`transformers`/`safetensors`/`accelerate`); **vLLM is deliberately NOT a dep** — no aarch64+CUDA-13 wheel for the pinned version (`project_verl_atgpo_vllm_gap`), it's served as a separate operator process. `gpu_seams(config, *, reward=None)` gained the backward-compatible `reward=` kwarg so the held-out gate scores through the adapter (RV-4); `run_rl_loop` passes it. **Decisive box reality:** GB10 has **no torch and no vLLM installed anywhere** (host or `/tmp/fk`), aarch64+CUDA-13 — so per the user's "vendor code, gate the run" choice, this lands the **code + GPU-free tests + docs**; the live `pip install fieldkit[rl]` + pinned-vLLM bring-up + the ~8.5 h `rl_run` drain is the explicit **operator-armed** step (runbook in `docs/api/rl.md` "Operator run" + the `FK_RL_*` env knobs). **Verified (GPU-free):** `import fieldkit.rl` stays torch-free (asserted in a test), sampler builds correct single-turn messages + `_GpuRollout`s over a fake client, held-out gate scores via the reward adapter, `serve_command`/`stop_command` construct the LoRA-enabled argv + EngineCore-aware teardown, `RLBackendConfig.from_env` reads the knobs, `gpu_seams` raises the `fieldkit[rl]` pointer when torch absent. Suite **1233 pass / 5 skip** (+9 seam tests), `audit-docs rl 3/3 + reward 4/4` clean, `mypy` + `ruff` (house-style: `/tmp/fk` ships ruff 0.15.15 whose UP045/UP037 fire on existing untouched code too → kept `Optional[]`/quoted-annotation house style, fixed only genuine F401/B905/RUF043/etc.), `astro build` **514 pages**, both verifiers green. **Committed direct-to-main** as the HEAD `feat(rl): vendor the real GPU backend …` commit (atop `6a69152`) — staged in `[Unreleased]`, **NOT yet pushed** (user asked to commit, not push). **No schema change** (RV-8 — arena.db stays `user_version 6`). Next: push → optional `fieldkit v0.21.0` cut (minor — new extra + the now-real backend) → the operator-armed run → promote the living-model launch.

### 2026-06-03 (fieldkit v0.20.1 CUT — the queued `rag_eval_index` `import json` patch ships)
User said "cut v0.20.1 patch" → `fieldkit-curator` release mode (interactive). **Patch** bump (0.20.0→0.20.1; the single `[Unreleased]` **Fixed** entry, no new modules/symbols/kwargs, no schema change — arena.db stays `user_version 6`). The fix: `fieldkit.harness.mcp.rag_eval_index` shipped (since M10) without a module-level `import json` though it parses the qa-eval gold set with `json.loads` — the **real** Arena `rag_eval` dispatch raised `NameError` on a live cockpit drain; the mock-injected job tests never executed the real tool body. Surfaced by the local-knowledge-appliance dogfood; the one-line import + infra-free regression test (`test_rag_eval_index_parses_gold_jsonl`) landed in `[Unreleased]` at dogfood time, this release just finalizes them. **Gates**: `audit-docs` 17/18 PASS (`cli` SKIP; the 3-kwarg `ArenaStore` WARN is pre-existing/non-blocking — `[Unreleased]` adds no kwargs, so no `--strict-kwargs`); `audit-landing` **4/4 PASS** (no module/version/count change in a patch). **Tests**: offline `/tmp/fk/bin/pytest tests/` → **1224 pass / 5 skip / 0 fail** (5 skips = `torch` GPU path + 4 `--spark` live). **Release**: `_version.py` 0.20.0→0.20.1, CHANGELOG `[Unreleased]`→`[0.20.1]` (+ Test-suite + Articles-in-this-release [none — package-only] subsections), commit **`55a2455`**, annotated tag `fieldkit/v0.20.1`, pushed main + tag (no perm prompt — push already authorized this session). **Both install-verifies green** — git-source (monorepo URL `…@fieldkit/v0.20.1#subdirectory=fieldkit`; confirmed `import json` present in the installed `harness.mcp`) + PyPI (`fieldkit==0.20.1`, installed on first poll). `twine check` PASSED both (wheel ships baked `_webui/`). **Stats refreshed**: `fieldkit_loc` 41,584→**41,617** (+33: the import + regression test) → committed with this HANDOFF. PyPI: <https://pypi.org/project/fieldkit/0.20.1/>. Working tree otherwise clean (the two known untracked items: `.claude/scheduled_tasks.lock`, `src/data/arena-mirror/`). **The `[Unreleased]` block is now empty.** Remaining Phase-3 threads unchanged: promote the living-model launch + vendor the GPU backend (`gpu_seams`).

### 2026-06-03 (Phase-3 editorial launch — the RLVR deep-dive SHIPPED + the living-model launch STAKED upcoming)
The `pane → hands → engine` arc gets its closing editorial beat. Two artifacts, committed direct-to-main (`420b75d` then `3c1010d`; **`origin/main` tip = `3c1010d`**). **(1) The deep-dive** — `articles/the-machine-improves-itself/` (`tech-writer`, **published**), the Machine-that-Builds-Machines ENGINE installment that closes the loop the meta-program opener left open ("the part the pane is still missing"). Thesis: **the eval harness IS the reward** — the 7 `fieldkit.eval` verifiers are the reward function, no learned RM. Hero finding is **defensive**: held-out-only checkpoint selection (the t2po 81.8 pp pool↔held-out inversion), which is the **inline fn-diagram's accent node**. 8 sections, **10 explainers** (3 define / 1 why / **3 pitfall** / 1 math / 1 deeper / 1 hardware — pitfall-heavy earned by the counterintuitive finding), code blocks **faithful to the shipped v0.20.0 `fieldkit.rl`+`fieldkit.reward` surface** (`RewardAdapter`/`group_advantage`/`GRPOConfig`/`RLLoop`/`gpu_seams`, verified against `rl.py`/`reward.py`). New signature **`ClosedLoopRlvr.astro`** (closed-loop ring, `SCORE`=eval-is-reward accent; distinct from the existing `T2poPoolVsHeldout` bars). `book_chapters:[10,11,14]`, `fieldkit_modules:[rl,reward,eval,lineage]`. **Honesty up front:** the +97.5 pp number is the *predecessor* `clawgym-on-spark-grpo` run's, NOT a run through the shipped engine — the engine ships as orchestration with injected seams (`gpu_seams` raises until the `fieldkit[rl]` GPU backend is vendored); the article says so plainly. **(2) The living-model launch** — `products/living-model/` (`product-writer`, **`status: upcoming`**), the first §5 "living-model" piece: a domain model re-RLVR'd on a cadence against a freshening bench, sold on a public `fieldkit.lineage` delta chart. **Staked as a commitment, not a fabricated launch** — promotes to published when `gpu_seams` lands + a real `rl_run` draws the chart. Two judgment calls: **(a)** the classifier (correctly) blocked editing `verify_product_article.sh` (a skill gate), so instead I added a one-line **`ProductLayout` guard** — hide the build-infographic + feature-gallery for `status: upcoming` (mirrors `ProductCard`'s `isUpcoming`) so an unbuilt product renders as a clean commitment page, not a misleading "0h to build" infographic; **(b)** the placeholder's `build:` block carries the **real mined footprint of the engine it surfaces** (`fieldkit.rl`+`reward`, 624 LOC / 21 tests, window-mined), kept for provenance/schema-validity but **never rendered**. **Verified:** build green **514 pages**, both rendering verifiers pass, product verifier PASS, all cross-links + 2 book chapters resolve, published products' infographic unaffected (no regression). Stats refreshed (**52 articles**). **Decision (user, 2026-06-03):** slug = `the-machine-improves-itself` (on-brand MTBM cadence); living-model = upcoming placeholder (no real delta data yet). **Remaining Phase-3 threads:** promote the living-model launch + vendor the GPU backend (`gpu_seams`) + cut `v0.20.1` (the `rag_eval` `import json` fix in `[Unreleased]`).

### 2026-06-03 (Orionfold Cortex — the appliance becomes a named product + a working sidecar-less live demo)
The `local-knowledge-appliance` is elevated to its own Orionfold product, **Orionfold Cortex** (user picked the name; switched Recall→Cortex during prep, before anything shipped). Two commits: **`236f817`** (demo bundle) + **`8f3a2ad`** (the product). **Rename** (git mv, history-preserving): slug `local-knowledge-appliance` → `orionfold-cortex` across the product dir, public screenshots, artifact manifest (`hf_repo` → `Orionfold/cortex`, still `catalog_only`), launch prose + card positioning rebranded; cross-links fixed in the deep-dive article + `_FLOWS` + build-metrics note (CHANGELOG + the article transcript left as immutable history). 0 tracked files carry the old slug; old `/products/local-knowledge-appliance/` + card routes are gone (no redirect — revisit only if GSC complains, per user). **Routes** (consistent product shape paralleling Arena): `/cortex/` landing (mirrors `/arena/index.astro`, reads the product collection), `/cortex/demo/` → the demo's knowledge tab, `/products/orionfold-cortex/` story, `/artifacts/harnesses/orionfold-cortex/` card; homepage **`CortexTeaser`** after `ArenaTeaser`. **Live demo** (decision: **shared Arena bundle**, not a dedicated cortex bundle): the `/arena/demo/` cockpit predated the M10 pane, so it was **rebuilt with the knowledge tab** + an interactive replay — `boot.js` gains knowledge handlers (the pane polls `/api/knowledge`, so **rebuild** is a before→after flip: provenance **0/313 → 313/313**, eval trend empty → **recall@k 0.409/0.727**; the query console replays **3 real captured query responses** matched by text); `fixtures.json` gains a `knowledge` section (before/after + queries, captured live from the running cockpit) alongside preserved chat/compare/telemetry. The previously-**unscripted** flatten+rebase deploy is now **`scripts/deploy_arena_demo.mjs`** (copies the `arena/` subtree + assets + shim to `public/arena/demo/`, rewrites `/arena/` → `/arena/demo/` in html/js/css only, writes `.nojekyll`). NB this **redeploys the live `/arena/demo/`** with a newer cockpit (now Knowledge + Jobs + Standup tabs) — an upgrade; chat/compare replays unchanged. **Verified IN-BROWSER** against the built site (Playwright, served dist): before→rebuild→after flip + a 5-hit trust-tagged query both work; `/cortex/demo/` redirect lands on the knowledge tab. Build green **509 pages**, both rendering verifiers + the product-article gate pass. **Open follow-on:** the demo is the Arena cockpit chrome (honest — Cortex is Arena's recall layer); a Cortex-specific standalone demo page was the runner-up option if that chrome ever grates.

**Post-launch demo polish (same session, 4 more commits → `origin/main` tip = `b3253e9`):** (1) **`6993c3b`** — this HANDOFF entry. (2) **`5a18312`** — *model-card 404s* in the cockpit fixed: the models index + `[slug]` pages had a naïve local `href()` that prepended the baked `/arena` base to an `arena/…` path, doubling it to `/arena/arena/models/…` (→ `/arena/demo/arena/…` after rebase) while pages live at `/arena/(demo/)models/…`; both now strip the leading `arena/` like the layout's helper (**pre-existing — affects the wheel cockpit too; next `fieldkit arena build` bake picks it up**). Same commit **rebranded the cockpit "Knowledge" tab → "Cortex"** (label only). (3) **`12de0db`** — **moved the page route `/arena/knowledge/` → `/arena/cortex/`** (git mv `knowledge.astro`→`cortex.astro`, `active` key `knowledge`→`cortex`, `/cortex/demo/` redirect → `/arena/demo/cortex/`, deploy-script guard now checks `cortex/`); **the data API stays `/api/knowledge`** (renaming it would cascade through `fieldkit.arena.server` + harness + the demo shim + tests — deliberately not done), so `KnowledgePane` + `boot.js` replay are unchanged. (4) **`b3253e9`** — **Discord-style "interact here next" coach** in `boot.js` (demo-only): a blurple ping-ring pulses the next actionable element and clears on use — Cortex: rebuild → query input; Chat/Compare: first suggested-prompt chip; one at a time, `prefers-reduced-motion` fallback, 9s give-up if a target never appears. All **verified in-browser** (Playwright): model card opens its real detail page; `/cortex/demo/`→`/arena/demo/cortex/` with the rebuild→after flip + 5-hit query; old `/arena/demo/knowledge/` 404s; coach pulses+clears across the flow. Build green **509 pages**. **NB next `fieldkit arena build`**: the wheel `_webui` cockpit rebakes from this arena-app source → it gains the 404 fix + the `/arena/cortex/` route + "Cortex" tab (the coach is demo-only, won't appear in the wheel).

### 2026-06-03 (catalog-only harness fix — the `local-knowledge-appliance` card no longer tells readers to `snapshot_download` a 404)
Follow-up to the Phase-A dogfood (entry below): a reader-pasted URL surfaced that the shipped `local-knowledge-appliance` artifact card rendered an **unconditional** "How to load → `snapshot_download(hf_repo)`" block + a "Published to HuggingFace" claim — **both false** for this `hf_repo`-reserved-not-pushed harness (the Hub repo **401s**; `snapshot_download` would `RepositoryNotFoundError`). The template was written for the **`spark-hermes-*` harnesses, which ARE genuinely pushed** (verified live **200**: `Orionfold/spark-hermes-{cost-router,profile,vertical-router}`) — `local-knowledge-appliance` is the first **catalog-only** harness (ships as `pip install "fieldkit[arena]"` code that builds its index on-box; no downloadable bundle, by the local-first design). **Fix:** new optional **`catalog_only: z.boolean()`** manifest flag (added to **both** `content.config.ts` copies + set `true` in the `.yaml`); the harness `[slug]/index.astro` branches on it to show `pip install "fieldkit[arena]"` + `fieldkit arena up` instead of `snapshot_download`, and omits the false `codeRepository` from the `SoftwareSourceCode` JSON-LD. The three hermes cards are untouched (their `snapshot_download` is correct). **Verified:** `astro build` **507 pages**, both rendering verifiers pass; rendered HTML confirms the appliance card flipped (no `snapshot_download`, "Published to HuggingFace" count 0) while `spark-hermes-profile` still renders `snapshot_download`. Committed + pushed **`442b331`** (atop `585fd3c`). **`origin/main` tip = `442b331`.** NB the original dogfood entry's parenthetical "`kind: harness`, catalog-only" described intent; the *card* hadn't honored it until this fix.

### 2026-06-03 (Phase-A **local-knowledge-appliance dogfood** — first real end-to-end DRIVE of the shipped arc; found + fixed a real `rag_eval` bug; new `kind: harness` artifact with a MEASURED recall@k)
First real-world use case driving the completed `pane → hands → engine` arc (user-directed program: **A appliance → B bench suite → C RLVR**, C gated on the article series landing + a new-domain-model scout, maybe a *science* stream, + an operator brainstorm). Drove the **visible Arena cockpit over CDP** (`playwright-core connectOverCDP` to the `:9222` Chromium, operator-style) through the full pane→hands flow on **this repo's own corpus** — "the machine managing its own memory": clicked **rebuild** on `/arena/knowledge/` → `reindex` + chained `rag_eval` drained through the **M8** dispatcher → provenance **313/313** backfilled (closes operator-action (a): `ensure_schema` ran on the shared `vectors` pgvector, adding the `source/kind/doc_date/verdict/link` columns) → **recall@k 0.4091 chunk / 0.7273 slug** (44-Q in-repo `qa-eval.jsonl`, cosine-only/GB10) → queried the Second Brain **live through the cockpit** (cited hits + trust tiers) → 2nd `rag_eval` exercised the **M10-6 promotion gate** as a gate (`prior=0.4091, delta=0.0, promote=true`) → **M11 standup** showed RAN 3 · Spend $0.0000/$5.00 · 0 regressed · "no push path". Exercised **M8+M9+M10+M11** (4 of 5 new pieces; the GPU-blocked RLVR engine untouched, as planned). **Bug found + fixed:** `fieldkit.harness.mcp.rag_eval_index` raised `NameError: name 'json' is not defined` on the **real** `rag_eval` drain — it parses the gold set with `json.loads` but the module shipped (since M10) with **no `import json`** (the mock-injected job tests never executed the real tool body). Fixed (one-line import) + regression test (`test_rag_eval_index_parses_gold_jsonl`, infra-free) + CHANGELOG `[Unreleased]` Fixed entry → a future **`v0.20.1`** patch. **Artifact:** `src/content/artifacts/local-knowledge-appliance.yaml` (`kind: harness`, catalog-only, `hf_repo` reserved-not-pushed per the orionfold-arena precedent) renders at `/artifacts/harnesses/local-knowledge-appliance/` with a new **Measured** block (added to the harness `[slug]` template) carrying the **byte-matched** recall@k + 4 bounded `known_drift` entries. 8 operator screenshots staged at `products/local-knowledge-appliance/screenshots/`. **Verified:** build **498 pages** (+1), both rendering verifiers pass, 45 fieldkit tests green (harness+arena/jobs incl. the 2 new). **Phase B SKIPPED (user call):** the 4 verticals (finance/legal/cyber/medical) were evaluated against **third-party** benchmarks (FinanceBench/LegalBench/CyberMetric/MedMCQA, local at `/home/nvidia/data/eval-benches/`), **not** Orionfold-authored datasets like `patent-strategist-bench` — so they can't honestly ship as `Orionfold/*-bench` artifacts (misattribution). User chose skip-B → go straight to articles. **Article decision (user): BOTH, SHIPPED this session:** (1) `product-writer` launch **`products/local-knowledge-appliance/product.md`** (`series: Cockpit`, status published; build-metrics mined honestly — the appliance IS the M10 recall layer [1,013 LOC, 8 tests], driven in **one ~4.2h launch session: 146 turns, 33.2M tokens 97.3% cached, 133k generated, 100% Opus 4.8**; 8 operator screenshots as the feature tour; tagline 111/summary 294); (2) `tech-writer` Second-Brain deep-dive **`articles/the-machine-manages-its-own-memory/`** (stage observability, `also_stages:[agentic]`, `series: Second Brain`, `fieldkit_modules:[memory,arena,harness,eval]`, `book_chapters:[10,11]`; inline flow-pipeline fn-diagram [adapted from the rag-eval figure, passes `verify_svg.sh` — note that script stays **non-executable**, its 12 *business* signature components fail the fn-diagram invariants by design]; 8 explainers incl. the "mock runner tests the dispatcher, not the tool" pitfall; signature `RecallGate` [name only — field-notes signature components aren't migrated to this monorepo, same as all siblings]). Both cross-link each other + the existing `rag-eval-ragas-and-nemo-evaluator`. **Verified:** build **507 pages**, both rendering verifiers green, `verify_product_article.sh`+`verify_article.sh` pass, stats refreshed (51 articles). **Phase A committed** (`9c687c5`, **not pushed** — user said commit-not-push); the two articles + manifest-repoint + stats are a **second commit** (this turn). **Phase C (RLVR) QUEUED** — opens a separate session: scout a new domain model (maybe a *science* stream), operator brainstorm, then vendor the GPU backend (`gpu_seams`). Cockpit left **UP** on `:7866`; the visible CDP browser on `:9222` died mid-session (relaunch via `arena_lifecycle.sh restart --browser` if needed).

<!-- 2026-06-03 fieldkit-v0.20.0-CUT entry (Phase 3 RLVR engine ships: `fieldkit.reward` + `fieldkit.rl`; minor 0.19.0→0.20.0, two new modules, schema unchanged RV-8 `user_version 6`; audit-docs 17/18 + audit-landing 4/4; offline 1222 pass; commit `9a1de39` + tag `fieldkit/v0.20.0`; both install-verifies green; PyPI upload user-authorized; stats 40,474→41,584; `origin/main` tip `d393073`) pruned per "keep ~2 latest" — superseded by the v0.20.1 cut + the GPU-vendored entry above; output live on PyPI + `fieldkit/CHANGELOG.md` [0.20.0]. The two open threads it named (editorial launch + GPU-backend vendoring) are both now progressed: editorial HALF-DONE (the v0.20.1 + editorial entries) and the GPU backend VENDORED (top entry). Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-03 Phase-3-RLVR-rlvr-loop-v1-BUILT entry (`fieldkit.reward` [Reward/RewardAdapter/group_advantage/RewardError — eval-harness-IS-reward RV-2, reuses `lineage.FailureLabel` RV-3] + `fieldkit.rl` [GRPOConfig/RLLoop/RLLoopError — REINFORCE+KL orchestration w/ injected GPU seams RV-1, held-out-ONLY checkpoint selection RV-4, `gpu_seams` raises until `fieldkit[rl]` GPU backend vendored]; `rl_run`/`requant` promoted to `DISPATCHABLE` RV-6, async/overnight-only narrow `POST /api/jobs` allowlist; NO schema bump RV-8 stays 6; suite 1211 pass; `1613899`; §10.1 as-built) pruned per "keep ~2 latest" — now RELEASED in v0.20.0 (the v0.20.0-CUT entry above covers the release) + full as-built lives in NEXT UP item 4 + `_SPECS/rlvr-loop-v1.md` §10.1. Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-03 fieldkit-v0.19.0-CUT entry (M11 autonomous harness ships: `fieldkit.budget` + `fieldkit.arena.scheduler`; minor 0.18.0→0.19.0; schema unchanged AH-9; audit-docs 15/16 + audit-landing 4/4 after adding the `budget` tagline; offline-only 1183 pass; commit `9727c15` + tag `fieldkit/v0.19.0`; both install-verifies green incl. monorepo git-URL; stats 39,414→40,474; `fieldkit-curator` SKILL.md path/URL drift PATCHED same session in `da8dbef`) pruned per "keep ~2 latest" — superseded by the v0.20.0 cut (top entry); output live on PyPI + `fieldkit/CHANGELOG.md`. Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Arena-M11-autonomous-harness-BUILT entry (`fieldkit.budget` [BudgetGovernor/BudgetDecision/SpendDigest/EscalationReason/MemoryEnvelope/check_budget/BudgetError] = the allow/escalate/defer brake, LOCAL_CEILING=33% AH-4, M9 soft-prereq AH-5, store-agnostic duck-typed; `fieldkit.arena.scheduler` [DrainLock stale-pid steal · run_drain_cycle · build_standup, no-push R26]; `drain_jobs(governor=…)` the one cross-module change; `GET /api/standup` never-drains + `/arena/standup/` pane; hook battery pre_commit_guard/post_publish/stop_feedback/secret_scan wired into .claude/settings.json; NO schema bump AH-9 stays 6; suite 1183 pass; §15.7 as-built) pruned per "keep ~2 latest" — now RELEASED in v0.19.0 (the v0.19.0-CUT entry above covers the release); build live in `fieldkit.budget`/`fieldkit.arena.scheduler` + `_SPECS/spark-arena-v1.md` §15/§15.7. Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 fieldkit-v0.18.0-CUT entry (M9 cost plane + M10 recall layer ship together; v0.17.0 skipped; minor bump schema 4→6; audit-docs 14/15 + audit-landing 4/4 after adding cost+memory taglines; offline-only 1153 pass; commit `a97eda6` + tag `fieldkit/v0.18.0`; both install-verifies green incl. monorepo git-URL; stats 37,146→39,414) pruned per "keep ~2 latest" — now superseded by the v0.19.0 cut (top entry); output live on PyPI + `fieldkit/CHANGELOG.md`. Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Arena-M10-recall-layer-BUILT entry (`fieldkit.memory` [MemoryIndex/KnowledgeCard/Provenance/ingest_sources/coverage_report/resolve_qa_set/MemoryError] owns canonical `(slug,chunk_idx)` blog_chunks + provenance card; schema `5→6` reindex_runs+rag_eval_runs CREATE-IF-NOT-EXISTS, pgvector ALTER in ensure_schema R21; reindex/rag_eval/scout_ingest→DISPATCHABLE M10-1 + 3 memory MCP tools; recall@k promotion gate M10-6; single backend M10-9 ask_second_brain→MemoryIndex.query trust-filtered; `/arena/knowledge/` pane; mirror M10-10 rag_eval_runs→PUBLISHABLE/reindex_runs→FORBIDDEN; external RAG assets VC'd M10-12; §14.7 as-built) pruned per "keep ~2 latest" — now RELEASED in v0.18.0; output live in `_SPECS/spark-arena-v1.md` §14/§14.7 + `fieldkit.memory` + `/arena/knowledge/`. Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Arena-M9-cost-plane-BUILT entry (first spec→build crossing; new module `fieldkit.cost` [CostLedger/PriceSnapshot/seed_price_snapshot/cost_per_quality/CostError], duck-typed store-agnostic; schema `user_version 4→5` first ALTER migration R18 round-trip-tested; per-run cost persisted at `add_openrouter_cost`, `seed_session_spend` restart-survival M9-8; mirror M9-7 [`openrouter_price_snapshot`→PUBLISHABLE, per-run cost OFF]; cockpit `$/task`+`$/quality` + `~` estimated marker R20; H6 evidence VC'd M9-10; ledger-not-governor; §13.7 as-built) pruned per "keep ~2 latest" — now RELEASED in v0.18.0 (top entry); output live in `_SPECS/spark-arena-v1.md` §13/§13.7 + `fieldkit.cost`. Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 RLVR-loop-locked-decisions-WRITTEN entry (standalone `_SPECS/rlvr-loop-v1.md` — the 4th/final `_FLOWS` §3 stub, the engine of pane→hands→engine; placement a user-green-lit fork [standalone, not an Arena section — `fieldkit.rl`+`fieldkit.reward` live mostly outside `fieldkit.arena`]; 10 decisions RV-1…10 + 5 risks; two reconciliation wins [reward `failure_class` reuses the shipped `fieldkit.lineage.FailureLabel` 9-class enum; `rl_run`/`requant` pre-drilled in `arena/jobs.py`]; three grounding corrections [RV-1 wrap the hand-rolled ~280-LOC REINFORCE not Unsloth/NeMo-RL; RV-4 held-out-every-≤10-steps hard gate; RV-5 pin vLLM + kill the ≈3.5min/step restart]; ~v0.20.0 gate) pruned per "keep ~2 latest" — output live in `_SPECS/rlvr-loop-v1.md` + `_SPECS/index.md`. Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Arena-M11-autonomous-harness-WRITTEN entry (`spark-arena-v1.md` §15 = M11: 9 decisions AH-1…9 signed off; a `/schedule` cron over the already-built `drain_jobs()`/`check_and_enqueue_regressions()` — M11 adds the *scheduler*, not dispatch; new `fieldkit.budget` governor [`BudgetGovernor`/`BudgetDecision`/`SpendDigest`/`check_budget`, `LOCAL_CEILING=33%`]; M9 cost-plane a SOFT prerequisite AH-5; hook battery 1→N; stage-only morning standup; NO new arena.db schema AH-9; ~v0.19.0 gate) pruned per "keep ~2 latest" — output live in `_SPECS/spark-arena-v1.md` §15 + `_SPECS/index.md` Planned queue. Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Arena-M10-recall-layer-WRITTEN entry (`spark-arena-v1.md` §14: 12 decisions M10-1…12 signed off; promote the pre-drilled `reindex`/`rag_eval`/`scout_ingest` job sockets to DISPATCHABLE; `fieldkit.memory` over the existing `fieldkit.rag.Pipeline` [adopted canonical, resolving the external-`ingest_blog.py` fork]; multi-source provenance card on `blog_chunks`; eval-gated re-index wrapping the on-disk `rag-eval-work/` harness; arena.db `5→6` contingent on M9's `4→5`; 4 open calls signed off "recommended"; ~v0.18.0 gate) pruned per "keep ~2 latest" — output live in `_SPECS/spark-arena-v1.md` §14 + `_SPECS/index.md` Planned queue. Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Arena-M9-cost-plane-WRITTEN entry (`spark-arena-v1.md` §13: 10 decisions M9-1…10 signed off; persist the ephemeral `_compare_cost_usd()`; per-side cost + `tokens_in` on `compare_responses`; public `$/quality-point` on `leaderboard_rows`; `openrouter_price_snapshot` pinned from H6 evidence; schema `4→5` first ALTER-migration R18; new `fieldkit.cost`; ledger-not-governor scope; VC the untracked H6 evidence M9-10; ~v0.17.0 gate) pruned per "keep ~2 latest" — output live in `_SPECS/spark-arena-v1.md` §13 + `_SPECS/index.md` Planned queue. Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 `_FLOWS` spec-stub-prioritization entry (post-M8 write order locked: cost→second-brain→autonomous-harness→rlvr; cross-bets-5/6-feed-Phase-2 rationale) pruned per "keep ~2 latest" — its order is now executing (cost ✅ §13, second-brain ✅ §14) and is live in `_SPECS/index.md` Planned queue + the ▶ NEXT UP block; recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Phase-1-Arena-launch-article entry (first `product-writer` launch since M7; new article `products/arena-control-plane/` `series: Cockpit`, positioning-first, build-metrics ~2.6h sprint / 95.9% cache, 3-shot `/arena/jobs/` tour 2× DPR, cross-links M7 + H4; tagline≤120/summary≤300 gotchas; `606333d`) pruned per "keep ~2 latest" — shipped + pushed; output live in `products/arena-control-plane/` + the §5 series ledger. Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 fieldkit-v0.16.0-CUT entry (full-remediation release via `fieldkit-curator`: tag `fieldkit/v0.16.0` + PyPI, both install-verifies green; remediated CHANGELOG 3-milestone drift [M6/M7/M8] + missing `arena` landing tagline + `audit_landing.py` `fieldkitModules`-indirection regex; version skip 0.13.0→0.16.0 intentional, fresh tag lineage; suite 1129 pass; wheel ships baked `_webui/`; stats LOC 35,446→37,146; `f50267f`) pruned per "keep ~2 latest" — output live in `fieldkit/CHANGELOG.md` + on PyPI. Open thread it left (the `fieldkit-curator` SKILL.md path/URL drift) is now **RESOLVED** — patched 2026-06-03 in `da8dbef` (see the v0.19.0-CUT entry). Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Arena-M8-side-by-side entry (browser-use walk of `/arena/jobs/` found + FIXED two gaps: #1 `eval_rerun` bench-path resolver [filesystem bench registry + `BenchNotRegistered`], #2 regression producer `check_and_enqueue_regressions` + `leaderboard_baseline` table [user_version 3→4] + `POST /api/jobs/check-regressions` + scan button; +11 tests → 1129 pass; browser E2E 0 console errors; `d6ce51a`) pruned per "keep ~2 latest" — output live in `_SPECS/spark-arena-v1.md` §12.7 + `fieldkit.arena.jobs` + the shipped v0.16.0. Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Arena-M8-BUILT entry (all 8 decisions green-lit M8-1…8; new `fieldkit.arena.jobs` dispatcher + store schema 2→3 + 2 new MCP tools + `/arena/jobs/` board; suite 1118 pass) pruned per "keep ~2 latest" — its output is live in `_SPECS/spark-arena-v1.md` §12.7 as-built map, the side-by-side-FIXED entry above, and the now-shipped `fieldkit v0.16.0` (top entries); recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Specs—Arena M8 control-plane milestone WRITTEN entry pruned per "keep ~2 latest" — its output is live in `_SPECS/spark-arena-v1.md` §12 (8 locked decisions M8-1…8, schema, dispatcher/trigger-producer arch, R13–R17) + the as-built §12.7, and it directly grounded the now-BUILT-and-FIXED M8 control plane (top two entries); commits `5f80fe9` + audit follow-up. Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Bet-6 cost-plane entry pruned per "keep ~2 latest" — its output is live in `_FLOWS/the-machine-that-builds-machines.md` (Bet-6 section + §4 table rows + §7 drift bullet, committed `ea3ed0d`); names `fieldkit.cost` + stub `_SPECS/cost-plane-v1.md` (named, not written). Recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Roadmap/spec corpus-harvest entry pruned per "keep ~2 latest" — its output is live (`_SPECS/roadmap-reconciliation.md` + the `_FLOWS` §3 per-phase *Measured grounding* notes + §7 drift bullet) and it directly grounded the now-written Arena M8 spec (top entry); recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Skill-path-drift entry (stats output repointed to `src/data/field-notes/project-stats.json`; `refresh_readme.py` + `tech-writer` `index` mode deleted; LOC KPI simplified to fieldkit-only `35,446`; `project-stats.json` path swept across skills + guides) pruned per "keep ~2 latest" — shipped + pushed (ancestor of `4e5a67b`); recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 Doc-consolidation entry (`_GUIDES/`+`_SPECS/` reorg, INDEX files, `specs/`→`_SPECS/` reference rewire, `handoff/`+`mirrors/`+dead-sync-docs deleted) pruned per "keep ~2 latest"; its outcome is live in CLAUDE.md's doc map + `_FLOWS/the-machine-that-builds-machines.md` §7 + `_GUIDES/index.md`/`_SPECS/index.md`; recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-02 WORKFLOWS-roadmap-resequence entry pruned per "keep ~2 latest" (§3 pane→hands→engine phasing, MCP-harness-already-built correction, Ch-14 fold-in, article-overlay strategy — all now live in `_FLOWS/the-machine-that-builds-machines.md` §1/§3/§5 + the Editorial-overlay open-item above; its "stale uber-corpus memory" sub-note is RESOLVED — the memory now points at the existing archive path); recover via `git log -p HANDOFF.md`. -->

<!-- 2026-06-01 WORKFLOWS.md-authoring entry pruned per "keep ~2 latest" (its roadmap + key-insight content is superseded by the 2026-06-02 resequence; recover via `git log -p HANDOFF.md`. -->

<!-- 2026-05-31 catalog-narrative entry (patent-strategist lanes + 4 older-quant positioning) pruned per "keep ~2 latest"; recover via `git log -p HANDOFF.md`. -->
<!-- 2026-05-29 Orionfold LLC go-live entry pruned per "keep ~2 latest"; recover via `git log -p HANDOFF.md`. -->
