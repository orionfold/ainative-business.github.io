<!--
  Session handoff for ainative-business.github.io (Spark-operated monorepo).
  Convention:
  - Keep this file short: current state, live runtime, build/verify, open items, and ~2 recent decisions.
  - Do NOT append completed history. Prune closed tasks aggressively; full history is recoverable via `git log -p HANDOFF.md`.
  - Last pruned/reset: 2026-06-10 (public-launch session compacted the Advisor proof narrative into the launch summary; receipt-by-receipt history is in git).
-->

# HANDOFF — ainative-business.github.io

> 📌 **PINNED — Arena build discipline.** Any `arena-app/` or `fieldkit.arena` work that changes the cockpit must be built and browser-smoked side by side in the running Arena over CDP: bring the cockpit up in browser-use mode (`arena_lifecycle.sh up --browser` -> `:7866` + CDP Chromium `:9222`), rebake `_webui` after `arena-app/` edits (`fieldkit arena build --repo-root arena-app`), then drive the live panes and confirm the change renders. Live `rl_run` validation stays operator-armed; offline tests + live render/transport smoke are the normal session bar.

> 📌 **PINNED — Arena pipeline discipline.** For Advisor/Arena/fieldkit pipeline work, the visible Arena cockpit is the operating surface and system of record. Use browser-use mode first for lane launch/teardown, active-lane selection, chat/compare/eval observation, jobs, run-context, and screenshots. Do **not** replace Arena operation with headless browser scripts, hidden endpoint batch scoring, or terminal-only API calls. If a needed step is not possible in Arena, perform only the minimum deterministic terminal action and log it as an `AD-AE-*` or `AD-FK-*` dogfood finding.

## Current State

> 📌 **NEXT — v0.34 PREMIUM DESIGN PASS · ROUND 4 — ✅ COMPLETE + SHIPPED + the TUI-demo build is ✅ DONE. Only operator live side-by-side review remains:**
>
> Rounds 1–3 are settled with the operator. **All context — the 6 locked brand decisions (Part E) + the concrete design (Part F) + the BUILT notes (Part B-CONCRETE) — is in `_IDEAS/field-edition-onboarding-experience.md`** (private; gitignored). **The whole v0.34 build order is done AND released; the TUI marketing demo is built (dated entry below).** What's left: (a) **operator live side-by-side review** on the Spark display — now also covering the **welcome-CTA fix** (the "Talk to your Researcher" CTA, white-on-indigo). The headless playwright MCP stalls on the SSE cockpit page, so bring up the visible Chromium via `arena_lifecycle.sh restart --browser` (`:7866`/`:9222`); ~~(b) a fieldkit-curator release~~ — **DONE 2026-06-15: `fieldkit/v0.34.0` on PyPI** (<https://pypi.org/project/fieldkit/0.34.0/>) shipped the onboard TUI + key endpoints + write_ngc_api_key + the rail `[hidden]` fix + the cloud-lane hide + the Compare horizontal-bars + eval-drawer scrollbar fixes into the customer wheel + rebaked `_webui`. ~~**(c) build the TUI marketing demo**~~ — **DONE 2026-06-15 (this session; dated entry below).**
> - **Shipped this session (the full v0.34 journey, ONE big push):** **(1)** settings **OpenRouter-key form** (`OpenRouterKeySettings.jsx` + `GET`/`POST /api/openrouter-key` + `.env.local` upsert helpers) — auto-detect env/`.env.local` or paste+save locally, "Orionfold never sees it", reveals the cloud catalog+spend tile; **(2)** persistent **✦ Welcome back-link** in the top bar; **(3)** flag-gated **`/arena/`→`/arena/welcome/` redirect** (loopback-only, never demo/mirror); **(4)** the **guided `onboard` TUI** (Rich linear flow; `fieldkit.field_edition.onboard` + `field-edition onboard` + `compose.write_ngc_api_key`; bootstrap calls it via `/dev/tty`); **(5)** fixed the rail **`[hidden]` CSS bug** (the cloud spend tile now genuinely hides — a real bug an earlier `.hidden`-only smoke missed; CDP-verified via `offsetParent`); **(6)** **journey snapshots** (9 cockpit panes re-shot live in the new palette: 01,02,03,04,05,09,10,11,14 + new `15-welcome`, `16-settings-cloud-key`, 4 console SVGs `17-onboard-*`); **(7)** **marketing demo rebuilt+redeployed** (new palette + welcome + settings — CSS+page rebuild, no fixture re-record); **(8)** **product article + arena landing** updated (3 new feature entries + onboarding/welcome/cloud-key tour sections; landing now "17 features").
> - **Decisions 7+8** were the seed; both fully built. The cloud-lane hide is a **single data-driven seam (`has_key`), no FE-mode flag** — keyed dev cockpit unchanged, keyless customer hides cloud. Persona stays "your AI Researcher".
> - **Snapshot note — RESOLVED 2026-06-15 (this session):** the 5 deferred deep-interaction shots are now **all re-shot in the new blue-indigo palette** (dated entry below). 08-eval-drawer + 13-eval-bench-drawer needed **no lane** (the eval drawer is data-driven off the bench registry); 06-chat / 07-compare / 12-lanetruth were shot against a **warm Kepler-Q8 lane** armed through LaneTruth (operator-approved) and **torn down after** (box restored to embedder-only). All 19 product screenshots are now new-palette; **the palette migration is fully closed.**
> - **Verified:** fieldkit offline **1768 passed / 16 skipped**; site build **538 pages** + `verify_artifact_rendering`/`verify_field_notes_rendering`/`verify_arena_catalog_sync`/`verify_arena_demo_links` all green (35 screenshots in sync).
> - **The two-zone principle:** narrative surfaces (welcome/first-run/"while you wait"/empty states) get the full premium language; instrument/working surfaces (chat/compare/tables/telemetry) keep the Airtable density, re-skinned blue-indigo. **Now enforced structurally:** the premium idioms live in an opt-in `arena-app/src/styles/premium-narrative.css` that **only narrative pages import** (ArenaAppLayout's own `<style is:global>` carries the cockpit CSS and never imports it → zero bleed); the noise grain is gated to `body[data-zone="narrative"]` (new opt-in ArenaAppLayout `zone` prop). CDP smoke confirmed `/arena/chat/` has no zone, no noise, 0 `.of-surface` bleed.
> - **Arena build/smoke loop — PROVEN this session, reuse it:** after `arena-app/` edits → **(1)** rebake `ARENA_BUILD=1 /tmp/fk/bin/fieldkit arena build --repo-root arena-app` → **(2)** `.claude/skills/arena-lifecycle/scripts/arena_lifecycle.sh restart --browser` (brings up sidecar `:7866` + a **visible** Chromium on CDP `:9222` — **the old "exit-144, operator-only" caveat is SUPERSEDED**: the script detaches Chromium so Claude CAN launch+drive it; [[reference_visible_browser_cdp_attach]] is stale on this) → **(3)** drive with `puppeteer-core` (`browserURL:'http://127.0.0.1:9222'`, `defaultViewport:null`) **run from the repo root** (module won't resolve under `/tmp`), and use **`sharp`** to pixel-sample composited colors (getComputedStyle misses translucent-composite effects — that's how the magenta cast was caught). The operator reviews side-by-side on the Spark display. `!`-prefix still dead → copy-pasteable blocks ([[feedback_terminal_bang_unavailable]]). Reproduce-the-install recipe is in the doc's Part D. Brand grounding: strategy repo `references/{brand-strategy,rigel-product-family}.md`.

### 2026-06-15 — **TUI marketing demo BUILT** (playable `field-edition onboard` replay, inline in the Arena product article) + welcome-CTA contrast fix (this session)

- **The product article's onboarding section now leads with a playable `asciinema-player`.** `products/orionfold-arena/product.md` → "It starts at the install — guided onboarding" embeds a replay of the full guided `field-edition onboard` flow (welcome → preflight matrix → free-NGC-key capture → named download manifest → "while you wait" cards → "your Advisor is warm" CTA). The first `17-onboard-1-preflight.svg` is the **poster / no-JS fallback**; the other three stills stay below as the static walkthrough. **orionfold.com picks it up on its existing clone of the product article — no new route.** A dedicated `/arena/demo/onboard/` route is the deferred sibling option (not built).
- **The cast is faithful, not a live capture.** A live record would need a real NGC key + a multi-minute model pull (too slow/network-bound). `scripts/generate_onboard_cast.py` replays the **real** `fieldkit.field_edition.onboard` renderable builders + the real `PHASES` / `TESTED_MATRIX` into a deterministic ~44s asciicast (only the passing `DoctorReport` + the `▶`/`✓` phase stream are synthesized, both from the shipping tables). Re-run it after any `onboard.py` flow change to keep the cast honest. Output: `products/orionfold-arena/casts/onboard.cast` (+ public mirror `public/products/orionfold-arena/casts/`).
- **Plumbing (clone-portable, gated):** new `::asciinema{src=… poster=… …}` remark directive (`src/lib/products/remark-asciinema.mjs`, runs **before** `remark-explainers` so its directive-neutralizer leaves it alone) → emits a player mount point + poster `<img>`; a client island `src/components/products/AsciinemaCasts.astro` mounts `asciinema-player` (npm, added) on scroll-into-view (IntersectionObserver, autoplay + controls, poster fades on play). The ~180 KB bundle is **gated** into `ProductLayout` only when the body contains a cast (`hasCast = entry.body.includes('::asciinema')`), so it never loads on a cast-less product page. asciinema-player 3.15 uses an **inline Blob worker** → the single chunk has no missing-asset risk. Breakout CSS in `field-notes.css` (`.asciinema-figure`).
- **Verified:** site build **538 pages**; `verify_artifact_rendering` (22) / `verify_field_notes_rendering` (451) / `verify_arena_catalog_sync` (22) / `verify_arena_demo_links` (35 in sync) all green. Browser-confirmed (headless playwright MCP against a static preview): the player **mounts, autoplays, and renders the real frames** (saw the command typing → the "2.84 GB · Q4_K_M, downloading now" manifest → the value cards), poster fades, and the bundle ships **only** on the orionfold-arena page (0 on the four other product pages).
- **Welcome-CTA contrast fix (arena-app).** The filled **"Talk to your Researcher"** CTA on `/arena/welcome/` rendered dark-indigo-on-indigo (invisible). Root cause: the cockpit-wide `html.arena-app a:not(.arena-btn):not(.arena-app__tab)` link-color reset (specificity 0,3,2) out-specified `.welcome__go { color:#fff }` (0,1,0) and repainted the label `--arena-accent`. Fix: added `:not(.welcome__go)` to that reset's exclusion list (the same idiom already sparing `.arena-btn`/nav tabs) so the reset no longer matches the CTA. `_webui` rebaked (confirmed `:not(.welcome__go)` shipped in `build.KBd3pRBd.css`); CHANGELOG `[Unreleased]`. **Couldn't get a live pixel confirm** — the headless MCP stalls on the SSE cockpit page and the visible CDP Chromium (operator-armed) is down; the fix is correct by selector reasoning (the reset no longer selects the element). Flagged for the operator's next side-by-side review.
- **Ship split:** the website (article + cast + player island) is live on the next Pages deploy; the arena-app CTA fix is in fieldkit `[Unreleased]` → ships into the customer wheel on the next fieldkit-curator release + `_webui` rebake. Relay updated (`_RELAY.md` — TUI-demo thread CLOSED for orionfold.com pickup).

### 2026-06-15 — fieldkit **v0.34.0 RELEASED** to PyPI + git tag — the whole Arena Field Edition v0.34 block ships into the customer wheel (this session)

- **`fieldkit/v0.34.0` is live on PyPI** (<https://pypi.org/project/fieldkit/0.34.0/>) and tagged (`181b327`). **Minor bump** 0.33.1→0.34.0 — new public symbols (`fieldkit.field_edition.onboard`: `run_onboard`/`ensure_ngc_key`/`VALUE_CARDS`/`download_manifest` + `compose.write_ngc_api_key`). Ships the entire v0.34 cycle into the customer wheel: the **guided console onboarding TUI**, the Settings **OpenRouter-key form** (`GET`/`POST /api/openrouter-key`), the **keyless cloud-lane hide** rule, the blue-indigo+gold palette + `FIELDKIT_VERSION` wordmark badge, the rail **`[hidden]`** fix, and the two operator-caught UX fixes (**Compare horizontal magnitude bars** + **eval-drawer scrollbar**). The wheel rebakes `_webui`, so the customer cockpit gets all the arena-app changes that had been sitting in `[Unreleased]`.
- **Gates green:** `audit-docs` 17/18 PASS (1 SKIP `cli`, 1 pre-existing arena kwarg WARN — not this release; `field_edition` isn't an audited FIELDKIT_MODULE), `audit-landing` 6/6, **offline suite 1768 passed / 16 skipped / 0 failed**. **Git-source install verify ✓** (0.34.0, `field-edition onboard` command + `onboard`/`write_ngc_api_key` symbols present) and **PyPI install verify ✓** (0.34.0 from the published wheel; onboard imports; wheel carries `onboard.py` + the corpus pack). twine check PASSED both artifacts. **Stats refreshed** (fieldkit 63,840 LOC).
- **Not a website rebuild** (`field_edition`/`arena` aren't in `FIELDKIT_MODULES`; the parked `docs/field_edition.md` is outside the website glob). The website (screenshots + demo + prose) already shipped on the prior two commits this session.

### 2026-06-15 — v0.34 palette migration CLOSED + screenshot-quality pass + Compare horizontal-bars fix (this session)

> Three operator follow-ups in one session: (1) re-shot the 5 skipped interaction screenshots; (2) an Arena Compare UX fix (the per-lane comparison bars were illegibly tiny vertical canvases → now horizontal magnitude bars) + an eval-drawer horizontal-scrollbar fix; (3) a screenshot-quality pass (no scrollbars, reframed the odd-shaped thumbnails). The **arena-app code changes land in fieldkit `[Unreleased]`** (ship on the next fieldkit-curator release + rebake); the **website demo + screenshots + prose are live on the next Pages deploy**.

- **Followed up the screengrabs the round-4 push skipped.** All 5 are now re-shot in the blue-indigo palette and placed in both `products/orionfold-arena/screenshots/` + `public/products/...` (drift-guard green, **35 in sync**):
  - **08-eval-drawer + 13-eval-bench-drawer — no lane needed.** The eval drawer is fully data-driven off the bench registry (`GET /api/eval/benches`), so it renders without a warm chat lane. 08 = drawer open with all 8 bench pills (Patent Strategist default); 13 = the **Orionfold Advisor** bench selected (the count-89 bench — its 89 measured Field-Note rows + 26 family filters, matching the "89 measured rows with family filters" caption). Shot over CDP via the `🧪 Eval prompts` trigger.
  - **06-chat / 07-compare / 12-lanetruth — needed a warm lane (operator-approved arming).** Launched the **`kepler-q8`** recipe through **LaneTruth's guarded form** (system of record) → live on `:8091`, warm 6 s, run anchored (the anchor repointed `local:resident` → the :8091 lane, label honest `model-Q8_0.gguf`). **06** = a real streamed Kepler's-third-law answer ("Orbital period: 93.6 minutes", true ~94.6) with collapsible reasoning + live throughput rail (**TTFT 232 ms**, 9.6 tok/s avg). **07** = Kepler-Q8 **vs Claude Haiku 4.5** (full OpenRouter catalog toggle → picked `anthropic/claude-haiku-4.5`), both answered, framed on the head-to-head **metric cards** (Format/tok-s/TTFT/Tokens/Cost w/ winner ✓) + per-side rubric (A 100% / B 100%) + operator-pick + the **cost card metering the hosted call `~$0.0015` vs local $0**. **12** = LaneTruth clipped to the widget — the lane **ACTIVE · operator-selected** on :8091 + the guarded launch form (teardown-first + anchor-on-warm). **Lane torn down after** via LaneTruth (`:8091 torn down · 9.75 GB freed`); box restored to embedder-only.
- **2 prose honesty fixes in `product.md` (report=reality vs the new shots):** (1) chat TTFT **210 → 232 ms** (the new 06 shot shows 232); (2) the compare paragraph claimed the rubric scored *correctness* — the 07 shot honestly shows a **format check** that "says nothing about which value is *right*", so reworded to say exactly that and cross-ref the eval drawer's gold scoring (the metric-card label is "Format", not "Quality", when not in eval mode — AF-27(c)).
- **Compare metric-card bars: vertical → horizontal (arena-app fix).** The operator flagged the per-lane comparison bars as illegibly tiny — they were 9px-tall vertical peak-bar canvases (`CanvasSpark`) of the session's runs, so a single compare drew one ~4px sliver. Replaced with **horizontal magnitude bars** (A over B, sized to the real values on a shared per-pair scale → longer bar = bigger number, the ratio reads at a glance; winner keeps `✓` + full opacity, honouring lower-is-better for TTFT/cost). `CompareDuel.jsx` (dropped `CanvasSpark` + the `drawBars` import) + `ArenaAppLayout.astro` CSS (`.compare-metric__bars/__bar/__bar-fill` replace `__spark/__canvas`). 07 re-shot against a warm Kepler-vs-Haiku duel showing the new bars. CHANGELOG `[Unreleased]`.
- **Eval-drawer horizontal scrollbar FIXED (arena-app).** The long gold-reference badge (`white-space:nowrap`) + the list's implicit `overflow-x` drew a horizontal scrollbar across the drawer (visible in 08/13). `.eval-drawer__list { overflow-x: hidden }` + `.eval-badge--gold` now wraps (`white-space:normal; overflow-wrap:anywhere`). CDP-confirmed `overflowX=0`. 08/13 re-shot scrollbar-free.
- **Screenshot-quality pass (thumbnails).** Reframed the three odd-shaped shots that thumbnailed as slivers: **03-efficiency-frontier** (was 376×184 tiny → full-res `.lb-frontier` panel, ar 2.6), **11-telemetry-rail** (ar 11.5 → rail + cockpit context, ar 3.2), **12-lanetruth-guarded** (ar 9.0 clip → LaneTruth ACTIVE widget + catalog context, ar 2.2; re-armed the Kepler lane for the active state, torn down after — `9.53 GB freed`). 06-chat verified clean (no scrollbar). All 19 product screenshots now drift-green (35 in sync).
- **Marketing demo rebuilt+redeployed** (`ARENA_DEMO=1` build → `deploy_arena_demo.mjs` → `verify_arena_demo_links.mjs`): the demo's Compare pane + eval drawer pick up the new bars + scrollbar fix (new `build.*.css` + `CompareDuel.*.js` hashes; old `compare-metric__canvas` gone — verified). 40 demo files churned (legit asset-hash propagation). The compare-prose line was updated to describe the horizontal magnitude bars.
- **Verified:** site build **538 pages**; `verify_artifact_rendering` (22) / `verify_field_notes_rendering` (451) / `verify_arena_catalog_sync` (22) / `verify_arena_demo_links` (35 in sync) all green; `_webui` rebaked twice (bars, then +eval-drawer fix). **Box state:** cockpit `:7866` + visible Chromium CDP `:9222` UP (keyed); dogfood pgvector `:5432` + nim-embed `:8001` intact; **no chat lane resident** (Kepler torn down after each arming). **Ship split:** arena-app code (bars + eval-drawer fix) is in fieldkit `[Unreleased]` → next fieldkit-curator release rebakes the customer wheel; website (screenshots + prose + rebuilt demo) is live on the next Pages deploy.

### 2026-06-15 — v0.34 ROUND 4 COMPLETE: full onboarding journey built (settings key form + ✦Welcome link + redirect + guided `onboard` TUI) + journey snapshots + demo rebuild + article/landing — ONE big push (this session)

- **The operator chose to complete the WHOLE v0.34 journey before one big push** (AskUserQuestion: build Track B TUI + add the flag-gated redirect). Eight deliverables shipped together:
  1. **Settings → OpenRouter key form.** `arena-app/.../OpenRouterKeySettings.jsx` + `server.py` `GET`/`POST /api/openrouter-key` + `_upsert_env_local`/`_env_local_defines`/`_mask_secret` helpers. Auto-detects a key in env/`.env.local` (masked + provenance), else paste+save locally → written to the `.env.local` the sidecar loads + set live in `os.environ` (cloud catalog + spend tile appear, no restart) + busts the catalog cache. Privacy: status route never returns the raw value; "Orionfold never sees it". Reveals the v0.34 cloud-lane hide.
  2. **Persistent ✦ Welcome back-link** in the ArenaAppLayout top bar (re-reachable, Decision 7); hidden on the welcome page itself.
  3. **Flag-gated first-run redirect** — `/arena/`→`/arena/welcome/` only when `localStorage of_arena_welcomed` unset; in `<head>` before paint, loopback/LAN-only + never in demo (public mirror + marketing replay untouched). CDP-verified BOTH ways (flag set → stays; cleared → bounces).
  4. **Guided `onboard` TUI** — new `fieldkit.field_edition.onboard` (`run_onboard`/`ensure_ngc_key`/`VALUE_CARDS`/`download_manifest`/renderable builders) + `field-edition onboard` CLI + `compose.write_ngc_api_key` (the missing NGC writer). Rich **linear** flow (not Textual → `curl|sh`/CI-safe): welcome → preflight matrix → NGC capture → narrated phases w/ named manifest + one while-you-wait card per phase → "you are live" CTA + auto-open `/arena/welcome/`. Bootstrap (`get-orionfold.sh`) now calls `onboard` (reads `/dev/tty` under `curl|sh`; `--no-open` headless); `up` stays the documented fallback. 15 tests.
  5. **Rail `[hidden]` CSS bug FIXED.** `.telemetry-rail__cell { display: flex }` overrode the UA `[hidden]{display:none}`, so the cloud spend tile (and the run cell) never actually hid — an earlier `.hidden`-property-only smoke FALSELY passed (report≠reality). Added `.telemetry-rail__cell[hidden] { display: none }`; CDP-verified hidden via **`offsetParent`** (keyless) + shown (keyed).
  6. **Journey snapshots.** Re-shot 9 cockpit panes LIVE in the new palette (01-cockpit, 02-leaderboard, 03-frontier, 04-models, 05-model-detail, 09-command-palette, 10-lab, 11-rail, 14-proof-cards) + NEW `15-welcome.png`, `16-settings-cloud-key.png` (keyless paste form, tile hidden), + 4 console SVGs `17-onboard-{1-preflight,2-ngc,3-manifest,4-cta}.svg` (Rich `save_svg`). Placed in `products/orionfold-arena/screenshots/` + `public/products/...` (MD5 drift-guard green, 35 in sync). **Not re-shot (honest):** 06-chat/07-compare/08-eval-drawer/12-lanetruth/13-eval-bench — need a warm Advisor lane / FE-corpus / drawer states the data-empty dev cockpit can't cheaply reproduce; keep the prior palette (follow-up w/ a seeded lane).
  7. **Marketing demo rebuilt+redeployed** (`ARENA_DEMO=1` build → `deploy_arena_demo.mjs` → `verify_arena_demo_links.mjs` all-green). New palette + welcome + settings flow in via the CSS+page rebuild (no fixture re-record — palette is CSS-only, cloud-gating is server-side). 44 demo files churned (legit new-palette assets).
  8. **Product article + arena landing.** `products/orionfold-arena/product.md`: +3 feature entries (welcome / cloud-key / guided onboarding) + a v0.34 evolution paragraph + new tour sections (onboarding 4-SVG walkthrough, welcome, cloud-key). Arena landing heading fixed to `d.features.length` ("17 features") so it matches the grid (build block `feature_count:14` stays the honest launch metric).
- **Verified:** fieldkit offline suite **1768 passed / 16 skipped** (+20 tests); site build **538 pages**; `verify_artifact_rendering` (22) / `verify_field_notes_rendering` (451) / `verify_arena_catalog_sync` (22) / `verify_arena_demo_links` (35 screenshots in sync) all green. Cockpit + visible Chromium left UP, **keyed**, on `:7866`/`:9222` for the operator's live review.
- **Ship vehicle:** the fieldkit code lands in `[Unreleased]` (onboard + key endpoints + write_ngc_api_key + the rail fix + the cloud-lane hide) → a **fieldkit-curator release** (minor bump — new public symbols) ships it into the customer wheel + rebakes `_webui`. The website changes (article/landing/demo/screenshots) are live on the next Pages deploy from this push.

### 2026-06-15 — v0.34 ROUND 4 step 4: Arena cloud-lane hide rule (Decision 8) — 337-model dropdown + "$0 spend" tile gated behind a wired OpenRouter key (this session)

- **The cloud-lane hide rule is built on a single data-driven seam — `has_key` (the env `OPENROUTER_API_KEY`), no new "Field Edition mode" flag.** A keyless founding-25 customer no longer sees 337 OpenRouter cloud models they can't run (or a `$0.00` cloud-spend tile) on first-run; a keyed box (the dev cockpit) is **unchanged**. This sidesteps the step-3 redirect dilemma (`/arena/` is the shared dev cockpit): the gate keys off real key-presence, so it Just Works on the operator's daily driver without an FE-mode toggle.
- **Server (`fieldkit/src/fieldkit/arena/server.py`):** `api_compare_options` now sends **empty** `openrouter` + `openrouter_groups` arrays (and **skips the catalog fetch + its curated fallback entirely**) when no key — *the fallback was the real leak: `_openrouter_models_for_ui()` returned curated cloud models even keyless.* The chat + compare model dropdowns then render only the "Local · Spark" optgroup via their **existing** per-group `.length > 0` guards (zero `ChatLane.jsx`/`CompareDuel.jsx` edits), and the "all N" toggle drops out (its `laneOptions.all.length > 0` guard). `_build_payload` carries a new **`openrouter_enabled`** telemetry flag.
- **Rail (`arena-app/src/components/arena/TelemetryRail.astro`):** the OpenRouter spend cell (`#telemetry-rail-cost`) starts `hidden`; the SSE reveal `hidden = (t.openrouter_enabled === false)` shows it only on a keyed box. Absent flag (demo replay / legacy payload) → shown, so the **marketing demo is unaffected**.
- **Decision 8 part (a) "Models pane = Advisor-only" needs NO new code** — on a real customer box the repo_root pin (v0.33.1) means `local` = just their resident Advisor (no monorepo article models). Verified reasoning; not re-implemented.
- **CDP smoke (puppeteer-core, cache-disabled) — both states green:** **keyed** (`:7866`, normal dev cockpit) → 3 OpenRouter optgroups + "all 337" toggle + spend cell visible (`$0.0694`) — **regression: unchanged**. **keyless** (relaunched from `/tmp` with an empty `--repo-root` so neither cwd nor root carries `.env.local` — the server auto-loads `.env.local`, `server.py:7647`) → dropdown **only "Local · Spark"**, **0** OpenRouter groups, **no** "all N" toggle, spend cell **hidden**; live API `has_key:false, openrouter:0, catalog_size:0`. Verified on `/arena/chat/` + `/arena/compare/`. **Box restored to keyed** after the smoke (cockpit pid 544442, 337 models; CDP `:9222` up).
- **Verified offline:** `fieldkit/tests/arena/` **531 passed** (+3: keyed compare-options shape now sets a key; new keyless gated-path test; new `openrouter_enabled` telemetry test). `_webui` rebaked (141 → 36 baked). **Lands in fieldkit `[Unreleased]`** (server.py) + baked `_webui` (TelemetryRail.astro) → ships on the **next fieldkit-curator release**. No website rebuild (arena.* isn't in `FIELDKIT_MODULES`; `_webui`/`dist` gitignored).
- **Deferred (operator-gated):** the **"+ Add a cloud lane (OpenRouter key)" affordance** (Decision 8 names it) — NOT built; there's no in-cockpit key-entry form (`settings.astro` is only cloud-eval guardrails), so it needs a key-entry-path decision. Without it a keyless customer simply sees their Local Advisor (honest, not broken).

### 2026-06-15 — v0.34 ROUND 4 steps 2+3: premium-narrative CSS ported + `/arena/welcome/` first-run surface built + honest `?prefill=` (this session, committed `c770101`)

- **Step 2 — premium-narrative idioms ported as a NEW opt-in stylesheet.** Created `arena-app/src/styles/premium-narrative.css` (it `@import`s `design-tokens.css` so any narrative page gets tokens+idioms in one import, layout-independent): `.of-surface` / `.of-pressable` / `.hero-gradient-text` (near-verbatim — arena-app already shares the marketing `--color-*`/design-token names), the **noise grain** (gated to `body[data-zone="narrative"]`), `[data-animate]` scroll-reveal, **+ a new `.of-chip`** (gold-dot pill; Decision 6 named it but it's absent from the local source, so authored in-idiom). **Why a new file, not arena-app's `global.css`:** the cockpit + all `/arena/` routes use **ArenaAppLayout**, whose own ~5,600-line `<style is:global>` carries the cockpit CSS and does **NOT** import `global.css` (that's BaseLayout/field-notes only, and those pages are pruned out of `_webui`). An opt-in stylesheet only narrative pages import is the correct two-zone unit — the cockpit never sees it → **zero bleed by construction.** Transparent mixes use `in srgb` (hue-stable, the step-1 magenta lesson); oklch only for two-real-color mixes. `StarWave`/the marketing `BusinessConstellation` NOT ported (the latter is business-semantic — ainative product/outcome nodes + logo, wrong for Arena).
- **Step 3 — `/arena/welcome/` built (Decision 7).** New page `arena-app/src/pages/arena/welcome.astro`: ArenaAppLayout with **`hideRail`** + a new opt-in **`zone="narrative"`** prop (the noise hook — added to `ArenaAppLayout.astro`), a **fresh abstract Orion gold-star cover** (decorative, no business semantics), the locked Part F copy (eyebrow `ORIONFOLD ARENA · FIELD EDITION` → navy→blue→gold `.hero-gradient-text` "Welcome to your Arena" → "Meet your AI Researcher." → 182-sources/647-chunks body), the `.of-surface` "try asking" card with three `.of-chip` teach-tags, indigo CTA "Talk to your Researcher →" + "Skip to the cockpit". The three prompts route into `/arena/chat/?prefill=<q>` — **an HONEST pre-fill path added to `ChatLane.jsx`**: fills the composer + focuses, does **NOT** auto-send (the existing `?prompt=` auto-send path is untouched). First-run flag = `localStorage of_arena_welcomed` (MVP).
- **CDP smoke all-green** (puppeteer-core from repo root, cache-disabled, 2× DPR, `sharp` pixel-sample): welcome → rail hidden, `data-zone=narrative`, noise on; CTA gradient `#296cd8→#1f54b8`, `.of-chip` dot `#c99414` gold, `.of-surface` border oklch hue 259 (no magenta), hero region dominant `rgb(248,248,248)` clean. **Two-zone verified:** `/arena/chat/` has no zone, no noise, **0 `.of-surface` bleed**, rail present. **Prefill verified:** composer fills `"ping honest prefill"`, URL scrubs, **0 messages sent**.
- **Verified:** arena-app astro build clean (141 pages); `_webui` rebaked (36 pages) + welcome survives the prune + serves live at **`:7866/arena/welcome/`**. **Cockpit + visible Chromium likely still UP** (sidecar `:7866` + CDP `:9222`) for the operator's live side-by-side review — `arena_lifecycle.sh down --browser` when done. `_webui`/`dist-arena` gitignored (no artifact churn). Dogfood pgvector `:5432` + nim-embed `:8001` untouched.
- **Operator live side-by-side review of `/arena/welcome/` still pending** (headless render-smoke is the offline bar; the visible-browser review is the higher bar). **Deferred step-3 tail** (operator-gated; see pinned block + Part F): cockpit-index first-run redirect, top-nav trim, persona proper-name. **NEXT = step 4** (Models pane Advisor-only + cloud-lane hide).

### 2026-06-15 — v0.34 design pass: ROUND 3 (concrete design) CLOSED + ROUND 4 step 1 (palette migration) STAGED & offline-verified (this session)

- **Round 3 (concrete design) is done.** Continued the locked brainstorm: took the two gating decisions with the operator via AskUserQuestion (concrete wireframe previews), then wrote the full concrete design into **`_IDEAS/field-edition-onboarding-experience.md` Part F** (private/gitignored). **Decision 7** — first-run lands on a NEW dedicated, self-dismissing **`/arena/welcome/`** premium surface (constellation cover + hero-gradient "Welcome to your Arena / Meet your AI Researcher" + body grounded in real numbers (182 sources / 647 chunks) + 3 try-these prompts routing into Chat pre-filled, the 3rd teaching honest refusal; CTA "Talk to your Researcher →"). **Decision 8** — canonical FE catalog = **Advisor-only** (+ "add a cloud lane" affordance); cloud-lane hide rule: the 337-model OpenRouter dropdown + "$0 spend" rail tile stay hidden until a key is wired. Part F also locks the greeting copy, the 6 "while you wait" value cards, and the round-4 build order.
- **Round 4 step 1 — palette migration (the headline visual move) DONE + CDP-smoked + COMMITTED `5f1aabb`.** Retargeted the arena-app brand accent **spark-orange → Orionfold blue-indigo primary + DGX-gold secondary** (Decision 6 exact values) across all three token locations: `design-tokens.css` (`--accent`/`-ink`/`-soft` → indigo `#296cd8`/`#1f54b8`/`#dbe7fb`; **added** `--gold`/`-ink`/`-soft` + `--orbit-ink` `#142643`), `ArenaAppLayout.astro` (`--arena-accent*` → indigo), `global.css` (`@theme` + `:root` `--color-primary*` → indigo). **Scoped to the brand accent** — traffic-light status ramp, categorical `--svg-accent-orange`, bronze medal untouched.
- **Live CDP side-by-side review with the operator drove 5 follow-on fixes (`5f1aabb` + version badge `143508c`):**
  1. **Cooled `--arena-warn`** `#B87503` (red-amber) → `oklch(0.60 0.11 84)` (gold-amber) for hue cohesion — **Arena-local only**, the shared `--amber-ink` §3 contract (self-health system-of-record) is untouched. The telemetry rail's warn cells (configured-lane DRIFT badge etc.) read cohesive now.
  2. **Magenta/violet cast on translucent neutral panels FIXED** (operator: "telemetry panel background faded red/violet"). Root cause: `color-mix(in oklch, <neutral>, transparent)` drags near-neutral hues toward magenta (transparent = powerless hue-0) — a *composited* effect getComputedStyle can't see (only the pixels show it). Converted **80** `…in oklch, X, transparent)` → **`in srgb`** (alpha-scale, hue-stable); the **58** legit two-real-color oklch mixes preserved. Rail bg went `rgb(242,240,241)` (magenta) → `rgb(239,241,244)` (clean cool).
  3. **Hardcoded spark-orange `rgba()` glows** (the hex-grep blind spot — `rgba(247,101,59,…)` literals on the active nav tab + logo-mark drop-shadow) → token-derived indigo; 5 `rgba(255,180,80,…)` warn-washes → cooled-warn `color-mix`.
  4. **Dropped the colored left-edge stripe** (the "AI-designed tell") on `.build__card` / `.build__gate` / `.build__bench` + cockpit `.evidence-card`; state reads from in-card dots/chips/glyphs, the **active** build stage gets a subtle uniform accent highlight instead of a stripe.
  5. **Wordmark version badge now tracks the fieldkit version** (was a hardcoded `v0.2` that drifted from the 0.33.1 package). `arena build` (`webui.py`) injects `FIELDKIT_VERSION` into the astro env; `ArenaAppLayout` renders it (`'dev'` fallback for a standalone arena-app build). **fieldkit change (`143508c`) → lands in `[Unreleased]`; ships on the next fieldkit release** (the wider arena `dist`/`_webui` is gitignored, so the badge updates in the customer wheel only when fieldkit-curator cuts a release that rebakes).
- **Verified:** `arena-app` astro build clean (140 pages, 5× rebakes); `_webui` rebaked + cockpit CDP-smoked each round (pixel-sampled with `sharp`); 0 thick colored left-edges + 0 orange `rgba()` remain; active-tab glow confirmed indigo. **NOTE for next session: the cockpit + visible Chromium are likely still UP** (`arena_lifecycle.sh restart --browser`, sidecar `:7866` + CDP `:9222`); `down --browser` when done. Dogfood pgvector `:5432` + nim-embed `:8001` untouched. `dist/`/`_webui` gitignored (no artifact churn).

### 2026-06-15 — fieldkit **v0.33.1 RELEASED** to PyPI + git tag — the first-customer install is now ONE-SHOT (AD-FK-ε / AD-AE / repo_root leak CLOSED) (this session)

- **`fieldkit/v0.33.1` is live on PyPI** (<https://pypi.org/project/fieldkit/0.33.1/>) and tagged (`1f3d96f`). **Patch bump** 0.33.0→0.33.1 — bug fixes to existing modules, no new public symbol. Closes the three onboarding gaps the end-to-end first-customer run surfaced; v0.33.0 reached all-green but forced a re-run and landed the cockpit idle. **Gates green:** `audit-docs` 17/18 (1 SKIP `cli`, 1 pre-existing arena kwarg WARN — not this release), `audit-landing` 6/6, **offline suite 1748 passed / 0 failed / 16 skipped** (+5 regression tests). Git-source install verify ✓ (0.33.1, field-edition CLI present) and PyPI install verify ✓ (0.33.1; published wheel confirms AD-AE — `lane_ports=[8080,8091,8000]`, embedder excluded). twine PASSED both artifacts; wheel carries `ingest.py` + the corpus pack. **Stats refreshed** (LOC 63,277 / fieldkit 62,980).
  - **AD-FK-ε (High) — FIXED.** `up.resident` now **health-polls** the serving lane (`:{lane.port}/v1/models`, ~150 s budget, the pattern `sidecar` uses for `:7866`) instead of probing once. The lane container starts in `stack` but llama-server needs ~60–90 s to load the 2.84 GB GGUF → a single probe deterministically FAILed on a cold lane and forced a re-run. Honest timeout fix (`docker logs of-advisor-lane` + re-run).
  - **AD-AE (Med) — FIXED.** `lanes.lane_ports()` now **excludes the infra ports** (`launcher.infra_ports()` — the Cortex embedder on `:8001`, the same set the ONE-LANE guard already treats as not-a-chat-lane; lazy import keeps launcher→lanes acyclic). The embedder answered `/v1/models` so it was enumerated as a phantom 2nd chat lane → `resolve_active_lane` returned `"ambiguous"` → cockpit idle. Now the lone Advisor lane auto-resolves (`source="discovered"`) and the cockpit lands warm. **Fixes every launch path, not just the installer.** `FK_ARENA_INFRA_PORTS=` (empty) still exempts nothing (escape hatch).
  - **repo_root leak (Med) — FIXED.** `up.sidecar` now pins the cockpit's `--repo-root` to a fresh customer-owned `~/.orionfold/arena-root` (created) instead of letting `arena up` default it to the CWD → launching the installer from a dev monorepo can no longer leak that repo's artifacts/articles/leaderboard/models into the customer cockpit; honest first-boot empty state regardless of CWD.
- **Files:** `field_edition/up.py` (resident poll + `_lane_healthy` + `_cockpit_command(repo_root)` + sidecar arena-root pin), `arena/lanes.py` (`lane_ports` infra exclusion); +5 tests across `test_field_edition.py` / `test_lanes.py`. Not a website rebuild (field_edition isn't in `FIELDKIT_MODULES`).

### 2026-06-15 — FIRST-CUSTOMER INSTALL driven end-to-end (operator + headless Arena): reaches all-green but **NOT one-shot**; 3 onboarding gaps found → brainstorm queued (this session)

- **Drove the REAL `get-orionfold.sh` bootstrap end-to-end** on a pristine sister `HOME=/home/nvidia/of-fe-firstboot2`: `fieldkit[arena]==0.33.0` pulled fresh from PyPI into its **own venv** (the true customer path — confirmed `Successfully installed … fieldkit-0.33.0`), dev-signed `file://` license (`OF-FE-2026-0042`, founding-25, self-verified), NGC key seeded to the sister `~/.nim/secrets.env`, dogfood pgvector/embedder stopped so compose owned `5432`/`8001`. Then drove the cockpit **headlessly (Playwright over `:7866`)** for the visual verification (the visible CDP browser wasn't up; Claude can't launch it — exit 144).
- **What worked — AD-FK-α/β/γ all held + verify all-green.** `doctor` PASS (5/5 matrix). `up` ran **matrix→bundle→pull→stack→ingest→sidecar** clean: NGC key sourced (no compose error), **647 chunks ingested offline**, **cockpit auto-started + health-polled on :7866**. `verify` → **every gate green**, incl. **`cortex` PASS** — the first **customer-path** proof that the v0.32.3 refusal-scorer fix (+ this session's Part 1 `preflight` port) holds on a *fresh* box (the gate that false-FAILed 5/5 in the prior sim). Cockpit chat: grounded query → retrieved 3 real sources → `Citations: [artifact_orionfold_arena]`; refusal probe → *"does not support… Citations: []"*. TTFT ~440 ms, ~77 tok/s. **Retrieve→ground→generate→cite loop is live + correct.** Cockpit honesty held (it *warned* "more than one lane is live and none is selected" + showed the grounding provenance chip).
- **The headline: it is NOT one-shot, and the first cockpit screen confuses.** Three gaps (full detail + fix sketches in `_IDEAS/field-edition-onboarding-experience.md` Part A):
  - **AD-FK-ε (High):** `up.resident` probes `:8091` **immediately** after `stack`; llama-server needs ~60–90 s to load the 2.84 GB GGUF → **deterministic FAIL on a cold lane → customer must re-run `up`** (the re-run sailed through — `up` is re-entrant). Fix: health-poll `:8091` with a timeout, the exact pattern `up.sidecar` already uses for `:7866`.
  - **AD-AE (Med):** the **NIM embedder (`:8001`) is discovered as a selectable *chat* lane** → cockpit shows "2 lanes", active-lane resolves **"ambiguous"**, lands **idle** → customer must hand-pick the Advisor lane. Fix: discovery excludes/flags embedding endpoints → `resident` can **auto-arm** the single chat lane → cockpit lands warm.
  - **repo_root leak (Med, partly sim artifact):** `up.sidecar` resolved `repo_root` from **CWD**, so launching from the monorepo leaked dev data (22 artifacts / 67 articles / full leaderboard / 8 models / 337 OpenRouter models) into the "customer" cockpit. A real customer has no monorepo, but `sidecar` should **pin `repo_root`**. Minors: GGUF pull not resumable across `up` runs; bootstrap prints `installed: present`; HF_TOKEN unauth warning.
- **Operator's directive: improve the onboarding experience materially BEFORE more install runs** — (B) split fast `pip install` + a redesigned guided **TUI** (NGC-key capture, legible download manifest, "while you wait" value cards, narrated phases that *wait*), (C) Arena **first-run** (land warm on Chat, auto-arm, welcome panel, trim dev clutter / cloud lanes). All seeds written to **`_IDEAS/field-edition-onboarding-experience.md`** → **next session opens with an interactive scoping brainstorm** (see the pinned block above).
- **Box restored:** FE stack `down` + cockpit killed by-PID + FE pgdata volume removed + sister home/venv/license deleted + Playwright closed/cleaned; **dogfood `pgvector` + `nim-embed-nemotron` restarted, healthy** (corpus intact, 328 `blog_chunks` in `db=vectors`; clean restart, no port-conflict corruption this time). Ports: `8091`/`7866` free, `5432`/`8001` = dogfood. **Reproduce-the-run recipe** in `_IDEAS/…onboarding-experience.md` Part D.

### 2026-06-15 — AD-FK tail CLOSED: `preflight._score_output` re-broadened to the live-gate refusal vocab + regression test (this session)

- **Port fidelity restored.** `scripts/orionfold_advisor/preflight.py`'s `_score_output` (the frozen-receipt generator) now carries the same refusal vocabulary as the live first-boot gate `fieldkit.field_edition.advisor._REFUSAL_WORDS` — added the v0.32.3 broadening (`"does not include"`, `"unsupported"`, `"out of scope"`) the cortex false-FAIL fix introduced on the live side. They had diverged; a regenerated receipt would otherwise have scored differently from the live gate. Inline comment ties the two together.
- **New colocated regression test** `scripts/orionfold_advisor/test_preflight.py` (2 tests, mirrors the advisor-side test): the 4 valid refusal phrasings the Q4_K_M lane actually emitted all score `passed`, a non-refusal still fails, and every word in the live gate's `_REFUSAL_WORDS` is recognized by preflight (catches future divergence). `/tmp/fk/bin/python -m pytest scripts/orionfold_advisor/test_preflight.py -q` → 2 passed; field_edition + preflight server suites 179 passed.
- **Receipt re-freeze deferred (operator-gated, operator chose "leave deferred").** Broadening can only *increase* refuse-credit, so published receipts aren't wrong — they'd differ only on a row whose refusal used one of the 3 new words. Regenerate only when a receipt is reissued (needs a served lane to score live). **This is the last AD-FK-* item — closed.** (Memory: [[feedback_keep_scorer_local_until_reuse]] — `preflight._score_output` is the local copy; the promoted one is `advisor.score_output`.) Not a fieldkit-package change (preflight is a repo script) → no version bump, no website rebuild, no release.

### 2026-06-15 — fieldkit **v0.33.0 RELEASED** to PyPI + git tag — AD-FK-α/β/γ CLOSED: `up`/bootstrap is now a real unattended `curl … | sh` (this session)

- **`fieldkit/v0.33.0` is live on PyPI** (<https://pypi.org/project/fieldkit/0.33.0/>) and tagged (`d97668f`). **Minor bump** 0.32.3→0.33.0 (new `fieldkit.field_edition.ingest` submodule + the `field-edition ingest` CLI command + public symbols). **Gates green:** `audit-docs` 17/18 PASS (1 SKIP `cli`, 1 pre-existing arena kwarg WARN — not this release), `audit-landing` 6/6, **offline suite 1743 passed / 0 failed / 16 skipped** (164 field_edition). Git-source install verify ✓ (0.33.0, pack ships + sha-matches, `ingest` CLI present) and PyPI install verify ✓ (same, from the published wheel). twine check PASSED both artifacts; wheel carries `ingest.py` + the 986 KB corpus pack. **Stats refreshed** (`efca34d`, LOC 63,169 / fieldkit 62,872). No website rebuild (field_edition isn't in `FIELDKIT_MODULES`; the parked `docs/field_edition.md` is outside the website glob). The α/β/γ code shipped in `cebcf04`; this release finalizes the CHANGELOG.
- **The 3 unattended-orchestration findings from the first-customer sim are fixed** — the gaps that forced me to hand-drive the sim's `stack`/`ingest`/`sidecar` phases.
  - **AD-FK-α (NGC key) — FIXED.** New `compose.read_ngc_api_key()` (env → `~/.nim/secrets.env`) + `compose.compose_env()` inject `NGC_API_KEY` into the subprocess env for **every** `docker compose` call (`up.stack`, `down`, `repair` — α broke all three, not just `up`). `up.stack` now refuses up front with a named fix when no key is resolvable (vs a cryptic Docker error); `down`/`repair` use a harmless placeholder when teardown doesn't need a real key. `get-orionfold.sh` also sources `~/.nim/secrets.env` (belt-and-suspenders; Python side is authoritative — it sourced the box's real key in the smoke).
  - **AD-FK-γ (sidecar) — FIXED.** `up.sidecar` now **starts** the cockpit: spawns `fieldkit arena up --no-open` detached (`start_new_session`, log → `~/.orionfold/cockpit.log`, pid → `cockpit.pid`), health-polls `:7866/healthz` until ready, fails honestly on early-exit/timeout. Re-entrant (returns immediately if already healthy). Console script resolved off `sys.executable`'s sibling (venv/pipx-robust; `fieldkit` has no `python -m` entry).
  - **AD-FK-β (ingest) — FIXED (wheel-vendored, operator-chosen).** New `fieldkit.field_edition.ingest` + an `ingest` phase in `up` (after `stack`): `matrix→bundle→pull→stack→**ingest**→sidecar→resident→[verify]`. The Advisor demo corpus rides the wheel as a **sha-pinned frozen pack** `data/advisor-corpus-pack-v01.jsonl.gz` (986 KB gz, 182 sources, `CORPUS_PACK_SHA=e2f4a2d64ada`, built by `scripts/field_edition/build_advisor_corpus_pack.py`). `up.ingest` chunks→embeds→upserts it **offline** into `advisor_corpus_v01`, reproducing the exact **647 chunks** the recall@5 0.977 proof measured (verified pure-path). Idempotent (non-empty corpus left as-is); honest `PhaseError` if embedder/pgvector down. Also `fieldkit field-edition ingest [--force]` (manual hatch). **Wheel over HF/GitHub:** the bootstrap already pulls the wheel, so the pack costs zero extra fetches / no auth / offline — inverse of the 2.6 GB GGUF (too big to vendor).
- **⚠️ Proof-control note — the pack is a DELIBERATE FROZEN SNAPSHOT, not repo-derived.** The fieldkit **CHANGELOG is itself a corpus source** (`doc_fieldkit_changelog_md`) AND a **gold source for recall row `advisor-operator-recommendations-0077`** + appears in a curveball packet — so it can't be excluded, and editing it (e.g. documenting this very change) drifts a repo-rebuild. So the builder's `--check` validates the **shipped artifact** (decompress + parse + sha == runtime pin), NOT a repo round-trip. The shipped pack froze the pre-edit CHANGELOG (recall still finds the source — `source_recall` is about *which* source, not exact text). **A re-freeze is a deliberate `build_advisor_corpus_pack.py` run (no flag) gated on re-running EVERY bench** — and fieldkit-curator should re-freeze + re-pin if it ships a CHANGELOG/docs change that materially moves the corpus (new release-step coupling to note).
- **Gates:** **164 field_edition tests** (was 142; +22 for α/β/γ), **full offline suite 1743 passed / 0 failed / 16 skipped**. Wheel build confirmed it carries `ingest.py` + the 986 KB pack (added `data/*.jsonl.gz` to the wheel glob). `field-edition up --dry-run` renders the new `ingest` phase in the plan; `--check` OK. `sh -n` clean.
- **➡️ Remaining (deferred, operator-gated):** **re-broaden `preflight._score_output`** to match the v0.32.3 advisor-scorer broadening (port fidelity; re-freezing published receipts is operator-gated) — the only AD-FK-* tail left (now shippable any time; v0.33.0 already out).

> 📌 **NEXT — Field Edition license path is END-TO-END LIVE; founding-25 is sellable. Only the §9 update-channel tail remains (Website lane, no Spark dependency).** The launch loop closed this session (relay `Website→Spark` 🟢 "WE ARE LIVE", 2026-06-13): pay → prod-signed token-less license emailed → `curl getarena.orionfold.com … sh` → `load_license()` verifies against the now-embedded prod key (v0.32.2) → Arena up. Remaining tail, none weekend-critical: (1) **§9 hosted signed-matrix feed** so `LiveUpdateChannel.fetch_latest` can pull a *new* signed proven matrix (last M3 piece — images are signed + cosign-verifiable now; only the feed is missing; lives in the Website peer's `entitlement-fetch`). (2) Website-owned: `entitlement-fetch` (revocable short-TTL re-mint) + re-vendor `license-conformance-v1.json` from `fieldkit==0.32.1` into their mirror CI (test-fixture refresh, not blocking). ~~(3) optional `hermes` verify gate~~ — **DONE this session (2026-06-13g): the LAST unwired §8 gate is wired LIVE + PASSING; the entire first-boot `verify` battery is now wired.** `/tmp/fk` needs `respx fastapi uvicorn sse-starlette aiosqlite httpx build twine` (+ `mcp` for the hermes gate / harness suite — `pip install -e fieldkit[dev,arena,harness]`) for the full suite + build (drifts out of the minimal venv).

### 2026-06-15 — fieldkit **v0.32.3 RELEASED** to PyPI + git tag (ships the §8 hermes gate LIVE + the cortex refusal-scorer fix) (this session)

- **`fieldkit/v0.32.3` is live on PyPI** (<https://pypi.org/project/fieldkit/0.32.3/>) and tagged (`e8b1aae`). **Patch bump** 0.32.2→0.32.3 — no new module/public symbol, two `field_edition` first-boot changes: (1) **Added** — the optional §8 **`hermes` gate wired LIVE** (the last unwired first-boot gate; `LiveGateRunner.hermes` drives the fieldkit-as-MCP server over real stdio, lane-safe capabilities probe, passes stack-independently — from the 2026-06-14 session, was sitting in `[Unreleased]`); (2) **Fixed** — the **cortex grounded refusal-scorer false-FAIL** found by today's first-customer sim (broadened `_REFUSAL_WORDS`; published Advisor curveball 85.7%/9-9 unchanged, re-verified live). **This closes AD-FK-δ** — a customer's `verify --hermes` no longer hits the old stub, and the cortex gate no longer false-FAILs on the shipped lane.
- **Gates + verifies all green:** `audit-docs` 17/18 PASS (1 SKIP `cli`, 1 pre-existing arena kwarg WARN — not this release), `audit-landing` 6/6, **offline suite 1721 passed / 0 failed / 16 skipped** (142 field_edition tests). Git-source install verify ✓ (0.32.3, refusal-fix present) and PyPI install verify ✓ (same, from the published wheel). twine check PASSED both artifacts. **Stats refreshed** (LOC 62,491 / fieldkit 62,194).
- **➡️ Remaining (deferred, logged): ~~the 4 orchestration findings~~** — **AD-FK-α/β/γ CLOSED this session** (see the 2026-06-15 entry above; `up` is now a real unattended `curl|sh`, in `[Unreleased]`). **AD-FK-δ** was closed by this v0.32.3 release. Only **re-broaden `preflight._score_output`** remains (port fidelity; re-freezing published receipts is operator-gated). None block selling — they're first-boot UX hardening.

### 2026-06-15 — FIRST-CUSTOMER HAPPY-PATH SIM RUN END-TO-END: all-green receipt reached + **a launch-bug scorer false-FAIL found & FIXED**; 4 orchestration findings logged (this session)

- **Drove the REAL `get-orionfold.sh` bootstrap end-to-end on a pristine box** (sister `HOME=/home/nvidia/of-fe-firstboot`, fresh `fieldkit[arena]` venv, dev-signed sample license via `file://`, operator stopped dogfood pgvector/embedder so compose owns the ports). **Wall-clock: bootstrap start → running container stack ~8 min** (GGUF pull was ~2 min, not the projected 10); a clean first-boot is **~10–12 min — comfortably under AC-1's 60 min** for the 4B path. **Final receipt: `ok=True`, 4 pass / 0 fail / 1 skip** (`fieldkit`+`advisor` 85.7%/9-9 +`cortex` recall 0.977 + grounded 16/16 & cite 95.7% +`lane` PASS; `hermes` skipped). Receipt at `$OF_HOME/.orionfold/receipts/verify-20260615-061953.json`.
- **⭐ LAUNCH BUG FOUND + FIXED (operator-approved spot-fix): the §8 cortex grounded-contract gate deterministically false-FAILed on a healthy box (5/5 real `verify` runs).** Root-caused: the Q4_K_M lane refuses **correctly** but in ≥2 phrasings (temp-0 llama-server is not bitwise-deterministic), and `advisor._REFUSAL_WORDS` recognized `"does not support"` but **not** `"unsupported"`/`"does not include"`. So probe `advisor-missing-source-refusal-0096` (*"…scope is unsupported. Citations: []"*) scored as a refusal-hygiene miss → all-or-nothing 16/16 → cortex FAIL, even though recall (0.977) + citation (95.7%) passed and the model was behaving correctly. Proof: the same probe refuses cleanly in **6/6 of my isolated reproductions** and the scorer passes it; the real CLI failed it 5/5 (the `_lane_chat`-scored output used the unrecognized wording). **Fix:** broadened `_REFUSAL_WORDS` with the lane's actual decline vocabulary (`"does not include"`, `"unsupported"`, `"out of scope"`). Since `refusal_ok` is consulted **only** for expected-`refuse` rows, answer/route scoring is untouched and adding words can only *increase* refuse-credit (the 9/9 floor can't drop). **Re-verified LIVE: advisor 85.7%/9-9 UNCHANGED (same misses 0005/0009/0011); cortex now PASS 16/16; full `verify` `ok=True` on 2/2 reruns.** +1 regression test (the 4 valid refusal phrasings the lane actually emitted) → **142 field_edition tests, all pass**. CHANGELOG `[Unreleased]` `### Fixed` entry added. **Working-tree changes (uncommitted, operator gate): `advisor.py` + `test_field_edition.py` + `CHANGELOG.md`.** Lands in `[Unreleased]`; a fieldkit-curator release publishes it. **NOTE: this diverges the field_edition scorer from its `scripts/orionfold_advisor/preflight._score_output` port — preflight (frozen-receipt generator) should get the same broadening for fidelity (deferred; re-freezing published receipts is operator-gated).**
- **4 orchestration findings (operator decision: LOG NOW, fix in a focused session — they touch `up.py`'s phase machine / the bootstrap):**
  - **AD-FK-α (High) — NGC key never wired.** `get-orionfold.sh` doesn't source `~/.nim/secrets.env` and `compose.render_env` omits `NGC_API_KEY`, yet compose needs `${NGC_API_KEY:?…}` → a real `curl|sh` customer **fails at the `stack` phase**. `compose.py:308` comment *claims* the bootstrap sources it (it doesn't). Fix: bootstrap sources `~/.nim/secrets.env`, or `up` reads it and writes `NGC_API_KEY` into the bundle `.env`. (I exported it in the sim driver to proceed.)
  - **AD-FK-β (High) — no corpus-ingest phase.** FE pgvector boots **empty**; `up` has no ingest/seed phase, so the cortex gate can't pass on a fresh box. The cortex gate's own fix-text (`verify.py:534`) *claims* `up` "brings up + ingests `advisor_corpus_v01`" — it doesn't. I ingested manually via `scripts/orionfold_advisor/score_recall_live.py` (647 chunks, recall 0.977). Fix: add an `ingest` phase to `up` (ship + ingest the Advisor demo corpus pack), or detect empty-corpus and emit a "run ingest" fix.
  - **AD-FK-γ (High) — `sidecar` phase doesn't start the cockpit.** `up`'s `sidecar` phase only health-polls `:7866` and raises if absent → `up` hard-stops; `curl|sh` can't complete unattended. I started `fieldkit arena up --no-open` manually. Fix: `up` should start the pipx cockpit itself (the code's own M2 TODO).
  - **AD-FK-δ (Med) — shipped wheel's `verify --hermes` errors.** PyPI **v0.32.2** (what the customer installs) still has the OLD hermes stub ("needs the live Hermes harness — M2"); the wired+passing hermes gate is in `[Unreleased]`, **unreleased**. A customer passing `--hermes` hits a false error. Fix: cut the fieldkit release shipping the wired hermes gate (and now the scorer fix) before launch.
  - Minor: bootstrap prints `installed: present` (its `fieldkit --version` probe fails → fallback); the bundle `.env` uses `OF_*` var names but `MemoryIndex` reads `SECOND_BRAIN_PG_DSN`/`EMBED_URL` (aligns only by default).
- **➡️ Stack LEFT UP for operator visual verification** (operator asked to "bring up the new install arena in browser"): the customer-install cockpit is serving at **http://127.0.0.1:7866/arena/** (fresh `arena.db` → empty leaderboard/models, honest first-customer state; the FE lane `:8091`/embedder `:8001`/pgvector `:5432` are the compose stack). **Claude can't launch the visible browser from Bash (exit 144) — operator opens the URL on the Spark display.** **Teardown when done:** `HOME=/home/nvidia/of-fe-firstboot … fieldkit field-edition down` (or `docker rm -f of-advisor-lane of-embedder of-cortex-db`) + kill the cockpit (PID in `cockpit.log`), then **restart dogfood: `docker start pgvector nim-embed-nemotron`**. Sister folder `/home/nvidia/of-fe-firstboot` can be deleted.

### 2026-06-14 — Arena Field Edition M2 step 4: the optional §8 **`hermes` gate is wired LIVE + PASSING** — the LAST unwired first-boot verify gate is discharged (this session)

- **The entire §8 first-boot `verify` battery (`fieldkit`/`advisor`/`cortex`/`lane`/`hermes`) is now wired LIVE.** `LiveGateRunner.hermes` (was an honest-error stub: "needs the live Hermes harness") drives the **`fieldkit`-as-MCP server** — the exact surface a Hermes harness drives — with a **real MCP client over stdio** (`initialize` → `list_tools` → `call_tool`), confirming one read-only tool round-trip (`spark_inference_envelope`) returns. **Live-smoked through the real CLI:** `fieldkit field-edition verify --hermes` → `hermes status: pass`, note "MCP round-trip ok — called `spark_inference_envelope` over stdio (15 fieldkit tools exposed); tool result returned".
- **Design decision (defensible, report=reality):** the probe is a **capabilities lookup — no GPU, model, disk, or network** — so the gate is **lane-safe** (honors the one-lane invariant: the resident Advisor lane the other gates proved warm is untouched) and **deterministic**, completing in <1 s (well inside the §8 ~30 s budget). Like the Cortex recall-half, it does **not** need the operator-armed serving lane, so it PASSES stack-independently. The **brain-driven full Hermes agent run** (a 30B MoE picking the tool) stays the heavier `hermes-harness-v1` §H4 milestone — NOT a first-boot gate, because a resident 30B brain would violate the one-lane budget at verify time.
- **Honest failure modes (never a vanity pass):** honest `error` with the install fix when the `fieldkit[harness]` MCP extra is absent; honest `error` when the stdio transport/server fails; empty/errored tool result → a real FAIL. The MCP round-trip is an **injectable seam** (`_mcp_tool_roundtrip`) so the pure floor is unit-testable without the SDK or a subprocess.
- **Tests:** 5 new (round-trip passes / empty-result fails / missing-extra errors honestly / transport-failure errors honestly / a **real end-to-end stdio MCP round-trip** against the live `fieldkit` server) → **141 field_edition tests** (was 136); `test_live_runner_bench_gates_error_honestly` dropped `hermes` (no longer stack-dependent). **Full collectable fieldkit suite 1720 passed / 0 failed / 16 skipped** (heavy-dep skips only). CHANGELOG `[Unreleased]` + parked `fieldkit/docs/field_edition.md` (§8 now "all-green") updated. **fieldkit source change → lands in `[Unreleased]`; a fieldkit-curator release publishes it. No version bump, no website rebuild.** Strategy: spec §8 + §12 updated (M2 step 4).

### 2026-06-13 — Arena marketing demo: the **SFT + Reward tabs are no longer empty** — both render full feature visualizations (this session)

- **Closed the two blank tabs the operator flagged on `orionfold.com/arena/demo/{sft,reward}/`** (the website clones this repo's `public/arena/demo/` demo bundle and adds its own chrome). Both panes were rendering their "No run yet" empty state because the recorded demo fixtures carried `{available:false}` stubs for `/api/sft-progress` + `/api/reward-signal` (recorded against a box with no active SFT/reward run). **Now both tabs render the full feature**, grounded in **real** evidence (DEMO ribbon already discloses "simulated data drawn from past real runs"):
  - **SFT tab** — a completed NeMo p65 LoRA SFT-init run: loss curve 0.792→0.471 (anchored on `evidence/orionfold-advisor/advisor-sft-headroom-v0.2.json`), iter 285/285 100%, peak GPU mem 33.8 GB, 11 checkpoints, feed-health strip + provenance chip. Run-history dropdown switches a full run ↔ a 10-iter smoke.
  - **Reward tab** — the eval-is-reward lineage from **3 real AV-10 preflights** (`evidence/astrodynamics/av10-preflight{,-8192,-sft}.json`): default shows the SFT-merged run that **clears the gate** (boxed 100% · reward 86% · truncation 0% · GATE PASS · 38/6/0/0 buckets · 44-row per-prompt table). The dropdown flips back to the pre-SFT Qwen3-8B baselines where **AV-R1 truncation fires** (88% truncation, red alarm, GATE HOLD, 7/8 truncated) — a live demo of the silent-no-learning the gauge exists to catch.
- **How:** edited the demo source `arena-app/public/arena-demo/{fixtures.json,boot.js}`, rebuilt the `ARENA_DEMO=1` bundle + ran `scripts/deploy_arena_demo.mjs` → regenerated deployed `public/arena/demo/`. The run-history dropdowns send `?source=<file>`, which the fetch-shim ignored; **taught `boot.js` to honor `?source=`** by serving per-run reports from new `fixtures.sft_reports` / `fixtures.reward_reports` maps (else the default/latest stub). Sanitized the `/home/nvidia/data/...` model path off the reward report → friendly labels (`Kepler-Astrodynamics · p65-NeMo LoRA (SFT-merged BF16)`).
- **Verified live** (headless browser against a local static-serve of `public/`): both tabs fully populate; reward dropdown switch confirmed flipping the truncation alarm. `scripts/verify_arena_demo_links.mjs` all green (691 links, **fixtures.json leak-scan clean** — the path sanitize held). Committed only the 4 meaningful files (source + deployed `boot.js`/`fixtures.json`); reverted the incidental rebuild HTML-hydration churn (island JS assets were byte-identical, referenced by stable paths — HTML didn't need regenerating). **Website picks the change up on its next clone.**

### 2026-06-13 — 🟢 Arena Field Edition is LIVE on BOTH sides — founding-25 sellable (Website peer closed the loop on this session's v0.32.2) (this session)

- **The license thread is closed.** The Website/commerce lane shipped `fulfillLicense` to prod (`ae0a521` + storefront `af671f7`): a `license` checkout now records the sale (founding-25 cap + 23505 idempotency) → draws `OF-FE-2026-NNNN` (gap-free seq RPC; first sale = `OF-FE-2026-0001`) → builds the **token-less** `orionfold.license/v1` (claims + 12-mo term, no `registry`) → **signs with the prod seed** → self-verifies → upserts `fe_entitlements` → mints a 7-day signed URL from the private `field-edition` Supabase bucket and emails the buyer the working `curl -fsSL https://getarena.orionfold.com | OF_LICENSE_URL='<signed>' sh` one-liner.
- **This session's `v0.32.2` is what closed it** — the Website peer's relay calls it out explicitly: embedding `of-license-prod-2026` in `TRUSTED_KEYS` means a founding-25 buyer's prod-signed license now `load_license()`-verifies on a released box. **End-to-end, both sides, no remaining blocker to selling.**
- **`getarena.orionfold.com` host is LIVE + verified** (`HTTP 200`, `text/x-shellscript`) — a Cloudflare Worker proxying the released `get-orionfold.sh`. The `c667d4c` rename to `getarena.orionfold.com` (operator decision) made the edge string-rewrite an exact self-healing no-op; domains aligned, nothing to undo.
- **No Spark action outstanding.** Their follow-ups (`entitlement-fetch`, the §9 hosted signed-matrix feed, re-vendoring the conformance vector) are all Website-lane, none blocking sales.

### 2026-06-13 — fieldkit **v0.32.2 RELEASED** to PyPI + git tag — PROD license signing key PROVISIONED (off `PROD_KEY_PENDING`) (this session)

- **`fieldkit/v0.32.2` is live on PyPI** (<https://pypi.org/project/fieldkit/0.32.2/>) and tagged (`3f56e26`). **Patch bump** 0.32.1→0.32.2. Embeds the **production Ed25519 license pubkey** for `of-license-prod-2026` (`LQVkEw+cetZGkstWJSdKoxOF/kuCrCgmGADaFi/yyDc=`) in `field_edition.license.TRUSTED_KEYS`, flipping it off the `PROD_KEY_PENDING` sentinel. Ops generated the keypair + delivered the public half (relay 2026-06-13, Website→Spark); the private seed stays in the commerce plane (Supabase secret `LICENSE_SIGNING_SEED_B64` + offline ops-vault backup, never on the box). **This was "the one Spark dependency blocking live license verify" for the weekend launch** — a prod-seed-signed `orionfold.license/v1` now verifies on a released box; rotation stays additive.
- **What changed:** the one-line `TRUSTED_KEYS` embed + docstring reword; `verify_signature` against the prod slot is now gated by **real Ed25519 verification** rather than the "not provisioned" guard. The honesty test flipped: `test_prod_key_pending_is_honest` → `test_prod_key_is_provisioned` (asserts the slot is a real 32-byte key, not the sentinel, and a bogus sig fails on verification).
- **Gates + verifies all green:** `audit-docs` 17/18 PASS (1 SKIP `cli`, 1 pre-existing arena kwarg WARN), `audit-landing` 6/6, **offline suite 1712 passed / 0 failed / 19 skipped** (136 field_edition tests, unchanged count). Git-source install verify ✓ (0.32.2, prod slot provisioned) and PyPI install verify ✓ (same, from the published wheel). twine check PASSED both artifacts. **Stats refreshed** (LOC 62,314 / fieldkit 62,017).
- **Also this session — installer domain renamed** (cosmetic) to the operator's final choice **`getarena.orionfold.com`** across `deploy/field-edition/bootstrap/get-orionfold.sh` + `README.md` (`943bde8` first set `arena.orionfold.com`, then `c667d4c` corrected to `getarena.orionfold.com` per the operator). `sh -n` clean; the Website peer's Cloudflare Worker now serves it live (`HTTP 200`).
- **➡️ Mac action (already in flight per relay):** `fulfillLicense` signs token-less with the prod seed; with the pubkey now embedded + the wheel shipped, real prod-signed licenses **verify on a box**. Also: re-pull the vendored `license-conformance-v1.json` from `fieldkit==0.32.1` (token-less full-license case, sha changed) and re-green the mirror CI.

### 2026-06-13 — fieldkit **v0.32.1 RELEASED** to PyPI + git tag (OPEN-1: license `pull_token` now optional) (this session)

- **`fieldkit/v0.32.1` is live on PyPI** (<https://pypi.org/project/fieldkit/0.32.1/>) and tagged (`9fb74bc`). **Patch bump** 0.32.0→0.32.1 — a schema *relaxation*, not a new module. Closes the OPEN-1 decision (**public images + weights-are-the-moat**, relay `Website→Spark` 2026-06-13): the `orionfold.license/v1` schema drops the mandatory GHCR `pull_token`. A v1 license is now **claims + term only** — `registry`/`pull_token` are **optional**, `License.registry` is `None` for a current license, `License.pull_token` returns `""`. The detached Ed25519 signature still covers whatever the payload contains, so **older token-bearing licenses keep verifying** (back-compat, explicitly tested).
- **What changed:** `field_edition/license.py` (`Registry` fields default-empty + lenient `from_obj`; `parse_license` treats `registry` as optional; docstrings + the parked `docs/field_edition.md` reworded off the "pull_token = the entire DRM" framing). The vendored `data/license-sample.json` **and** the relay sample `_IDEAS/field-edition-license-sample-v1.json` re-signed token-less; the shared `data/license-conformance-v1.json` `full-license-founding25` case re-frozen token-less via `build_license_conformance_vector.py` (`--check` round-trips, dev-key sigs re-froze).
- **Gates + verifies all green:** `audit-docs` 17/18 PASS (1 SKIP `cli`, 1 pre-existing arena kwarg WARN — not this release), `audit-landing` 6/6, **offline suite 1712 passed / 0 failed / 19 skipped** (was 1710; +2 license tests — token-less-validates + legacy-token-bearing back-compat → **136 field_edition tests**, was 134). Git-source install verify ✓ (0.32.1, token-less parse → `registry=None`) and PyPI install verify ✓ (0.32.1, same). twine check PASSED both artifacts; wheel carries the re-frozen `license-conformance-v1.json` + `license-sample.json`.
- **Stats refreshed** (`a2ca612`, LOC 62,305 / fieldkit 62,008) + home "At a glance" current.
- **➡️ Mac action (relay filed):** `fulfillLicense` simply **stops populating `registry`** — the token-less `fulfillLicense` build is now matched by a shipped verifier (v0.32.1). Its CI mirror re-canonicalizes + re-signs each conformance case against the **new** vector (the `full-license-founding25` case + the file sha changed). No other Mac change.

### 2026-06-13 — Advisor-GGUF HF card now features **Q4_K_M as the default variant** (closes the GA "HF card Q4_K_M" item) (this session)

- **The `Orionfold/Advisor-GGUF` card + the website artifact catalog now feature Q4_K_M as the recommended/default lane.** The Q4_K_M weights were already published (rev `dad6d1e5`, 2.6 GB) but every card surface still showed **Q8_0 only**; this refresh fixes that across **all three surfaces**: (1) the **HF README** (pushed, HF commit `2bafff72`), (2) the **website artifact manifest** `src/content/artifacts/advisor-gguf.yaml` (+ synced arena-app copy), (3) the **baked Arena cockpit `_webui`** (rebaked + mirror — `_webui` is gitignored/CI-regenerated, so no committed diff).
- **Real measurements, not fabricated.** A Q4_K_M lane was already resident on `:8091` (operator-left-up, byte-identical to the published artifact), so tok/s was **measured on it** (6 runs, median **~70.0 tok/s** — vs Q8_0's published 42.0). Quality is **byte-identical to Q8_0**: curveball-v0.2 85.7% (18/21 scored==strict), refusals 9/9, **same 3 misses** (0005/0009/0011) — confirmed from `evidence/orionfold-advisor/advisor-curveball-v0.2-q4km.json`. Size 2.6 GB via the card's own `{GiB:.1f} GB` formatter.
- **Surgical edit, not a full regen.** The live card diverges from the manifest (card siblings = `Kepler-GGUF`, manifest siblings = `advisor-bench`), so a `publish_quant` regen would have clobbered hand-state — I edited the README directly (title one-liner, Spark-tested table +Q4_K_M row, Variants table, How-to-run defaults → Q4_K_M) and updated the manifest fields (`variants`/`spark_tokens_per_sec`/`vertical_eval`/`recommended_variant` + the positioning copy). Gates green: `verify_arena_catalog_sync` (22 byte-identical), `verify_artifact_rendering` (22 pages), `verify_field_notes_rendering` (451 explainers); astro build 538 pages. **Reverted** an unrelated 181-line `arena-mirror/leaderboard.json` churn that `fieldkit arena mirror` pulled from live arena.db (publishing leaderboard data is a deliberate op, not a card-refresh side effect). Shipped `8a2091b`.
- **Lane note:** the `:8091` Q4_K_M lane was **torn down at end of session** (killed PID 544631 by-PID — `pkill -f 'llama-server.*8091'` self-matched its own shell wrapper, exit 144, so kill-by-PID; port dead, ~12 GiB reclaimed). Embedder `:8001` + pgvector `:5432` left untouched/up.

### 2026-06-13 — fieldkit **v0.32.0 RELEASED** to PyPI + git tag (ships the whole Field Edition surface) (earlier)

- **`fieldkit/v0.32.0` is live on PyPI** (<https://pypi.org/project/fieldkit/0.32.0/>) and tagged. Minor bump 0.31.0→0.32.0 (new `fieldkit.field_edition` package + `cosign`/`license` modules → minor per Decision M). This release ships everything the Field Edition session built: the §7–§9 installer/orchestration CLI (`doctor`/`up`/`verify`/`down`/`repair`/`update`/`rollback`), the §8 first-boot eval gates (advisor/cortex-recall/cortex-grounded/lane, live-measured), the **AC-7 license file** (`license.py` + conformance vector), and **§9 cosign verification** (`cosign.py` + wired `LiveUpdateChannel.verify_signature`).
- **Gates + verifies all green:** `audit-docs` 17/18 PASS (1 SKIP `cli`, 1 pre-existing arena kwarg WARN — not this release), `audit-landing` 6/6, **offline suite 1710 passed / 0 failed / 19 skipped** (skips = torch/mcp/matplotlib/great_tables/jupytext heavy deps + `--spark`). Git-source install verify ✓ (0.32.0, CLI + `field-edition` surface) and PyPI install verify ✓ (0.32.0, `field-edition doctor` runs). twine check PASSED both artifacts; wheel carries all 15 field_edition modules + the 4 `data/*.json` (curveball/recall/conformance/sample).
- **Note for the next release:** `/tmp/fk` needed `respx fastapi uvicorn sse-starlette aiosqlite httpx build twine` reinstalled to collect the full suite + build (the minimal venv had drifted) — the 1710 count needs those present. `field_edition` is **not** in `FIELDKIT_MODULES`, so `cosign`/`license` need no `docs/api/` pages (the parked `docs/field_edition.md` is the field-edition doc).
- **Stats refreshed** (`51fccde`, LOC 62,258 / fieldkit 61,961) + home "At a glance" current.

### 2026-06-13 — §9 `LiveUpdateChannel.verify_signature` WIRED to real cosign verify (closes the cosign follow-on) (this session)

- **The §9 update channel's signature gate is now real, not a stub.** New **`fieldkit.field_edition.cosign`** module: `verify_matrix()` filters a fetched proven matrix to its digest-pinned `ghcr.io/orionfold/*` images (skips upstream pgvector + the NVIDIA NIM embedder) and runs `cosign verify --key` against a **pinned public key** `PROVEN_MATRIX_COSIGN_PUBKEY` (key-based; byte-matched to the committed `deploy/field-edition/cosign/proven-matrix.pub` — a test enforces no drift), raising an actionable `CosignVerifyError` on a bad/absent signature or a missing cosign. `LiveUpdateChannel.verify_signature` calls it (signed-flag check first, then cosign); the cosign subprocess is an **injected `runner` seam** so the control flow is unit-testable without the binary. Same pin-a-committed-pubkey pattern as `license.TRUSTED_KEYS`.
- **Proven LIVE end-to-end**, not just fake-tested: `LiveUpdateChannel().verify_signature(matrix)` with the **real** cosign binary verifies the **actually-signed** lane image (`sha256:93993cc2…`) against the pinned key → OK. (`fetch_latest` still raises honestly — the *hosted release feed* to fetch a *new* matrix from is the remaining M3 piece; the images are signed + verifiable now.)
- **Tests:** 10 new (ref-filter to ours-only · pass/bad-sig/no-orionfold-images · ignore-tlog flag threads through · missing-cosign honest · LiveUpdateChannel unsigned-reject / cosign-pass / cosign-fail→UpdateError · pubkey constant byte-matches the committed pin) → **134 field_edition tests** (was 124); full field_edition suite green. `cosign` exported from `field_edition.__init__`; CHANGELOG `[Unreleased]` + parked `docs/field_edition.md` §9 section updated. **fieldkit source change → lands in `[Unreleased]`; a PyPI release (fieldkit-curator) publishes it. No version bump this commit, no website rebuild.**
- **➡️ NEXT (§9 remaining):** the **hosted release feed** so `fetch_latest` can pull a *new* signed matrix (the last M3 piece — a published signed-matrix manifest the channel reads). Then the full update path (fetch→verify→apply→gate→receipt) is end-to-end live. Separately: a fieldkit release to ship this wiring; embed the prod license pubkey (`PROD_KEY_PENDING`, waits on ops/OPEN-2).

### 2026-06-13 — cosign signing of the proven-matrix lane image SIGNED + verified (§9/M3) — key-based (Fulcio network-blocked) (this session)

- **The §9 cosigned-transport plane is live for the lane image.** Operator armed it this session: `ghcr.io/orionfold/llama-server-cuda13@sha256:93993cc2…` is **signed** (Rekor tlog index `1810763259`, signature pushed to GHCR) and **verifies green** against the committed pin (`./verify-proven-matrix.sh` → `OK signed by the pinned key`, exit 0). Setup at `deploy/field-edition/cosign/{sign-proven-matrix.sh, verify-proven-matrix.sh, README.md}` (public). **Not a first-boot blocker** — pulls work unsigned; this hardens the §9 *update* path.
- **PIVOT keyless→key-based (the first arm surfaced a hard network block).** First (keyless) attempt failed: `fulcio.sigstore.dev` (the Sigstore keyless CA) is **network-blocked on the Spark box** — answers 443 in plaintext (`tls: first record does not look like a TLS handshake` / OpenSSL `wrong version number`), persistent, single GCP IP, no proxy. Probed the rest of Sigstore: **Rekor + the TUF root are reachable (200)** — only Fulcio is intercepted. So **reworked to key-based signing** (a long-lived key — cosign's default ECDSA P-256 — still uploading to the public Rekor log). The **public key is committed + pinned** (`proven-matrix.pub`), the same pin-a-committed-public-key pattern this repo uses for the (Ed25519) `license.TRUSTED_KEYS` (different algorithm, same idea; operator chose this via AskUserQuestion over CI-keyless / unblock-Fulcio-and-wait). Drop-in swap back to keyless if the network is ever opened.
- **Scope (derived live from `fieldkit`, so it never drifts from `compose.py`):** signs only **Orionfold-built, GHCR-hosted, digest-pinned** images. Today exactly **one** — the CUDA-13 lane `ghcr.io/orionfold/llama-server-cuda13@sha256:93993cc2…`. Correctly **skips** pgvector (upstream) + the NIM embedder (NVIDIA NGC); **auto-includes** `cortex-embedder` at v1.1. Verified the fieldkit-derived set is byte-identical to the embedded fallback.
- **`sign-proven-matrix.sh`** defaults to **DRY-RUN** (smoked green); `--sign` arms it: generates the key pair into `~/.orionfold/cosign/` (encrypted by `COSIGN_PASSWORD`) on first run, publishes the public half to `proven-matrix.pub`, key-signs each image, uploads to Rekor (`OF_NO_TLOG=1` for a fully self-contained sign). **`verify-proven-matrix.sh`** runs `cosign verify --key proven-matrix.pub …` — the operator's confirmation AND the check the installer's `LiveUpdateChannel.verify_signature` mirrors at M3 (currently an honest `UpdateError`; wiring it is a fieldkit change gated on the signatures existing first). Both `sh -n`-clean; cosign v2.4.1 installed.
- **Key custody:** the private key (`~/.orionfold/cosign/cosign.key`) + its `COSIGN_PASSWORD` must go to the **orionfold secret store** (same custody as the prod license seed) — **never committed**; only `proven-matrix.pub` is committed. GHCR write auth as `orionfold` was used (the `.sig` is pushed to the same repo). **No version bump / no website rebuild / no fieldkit release.**
- **➡️ NEXT:** a small fieldkit change wires `LiveUpdateChannel.verify_signature` to `cosign verify --key proven-matrix.pub` (flips the honest `UpdateError` → live → M3 update channel goes live; gated on the sig existing, now true). `cortex-embedder` gets signed the same way once built+pinned at v1.1. The other open license item — embed the prod pubkey (`PROD_KEY_PENDING`) — still waits on ops/OPEN-2.

### 2026-06-13 — `get.orionfold.com` bootstrap script DELIVERED (Spark half of the install entry point; DNS/hosting is the Website peer's lane) (this session)

- **Authored the first-boot install entry point — `deploy/field-edition/bootstrap/get-orionfold.sh` + `README.md`** (public, released code). This is the Spark half of license-workflow §5.2 (`curl get.orionfold.com | sh`): the missing bridge between "signed-URL delivered" and `fieldkit field-edition up`. The license *format* + the `verify` gates were already done; this is what a founding-25 customer actually runs.
- **Lane split confirmed (operator-checked mid-session):** **DNS for `get.orionfold.com` + hosting the script (Supabase + CDN) + minting the per-customer signed URLs + the order-page snippet are the Website/Mac peer's lane** — I own the *content* only (forced seam, same as the license file: Spark owns format/validator + now the bootstrap script; Website owns issuance + delivery + hosting). Not blocked on DNS — handed over via relay for the peer to host.
- **What the script does** (POSIX `sh`, dumb + offline-honest): fetches the signed license **first** (most time-sensitive → fail-fast on expiry) → `~/.orionfold/license` `chmod 600`; optional Supabase weights (`OF_WEIGHTS_URL`, unset for v1 — weights stay on HF, `up` pulls them); installs `fieldkit[arena]` into a **self-contained venv** (`~/.orionfold/venv`) that **sidesteps DGX OS PEP-668 "externally-managed"** with no sudo/apt/`--break-system-packages` (prefers existing `fieldkit`/`pipx` if present); runs `field-edition doctor` (§7 gate) → `field-edition up`. **No entitlement check of its own** — Supabase gated at mint time; fieldkit Ed25519-verifies the license **offline**. Full env contract + Website hosting notes in the README.
- **Smoke-tested** (not a build artifact — shell + md, outside the website glob and the fieldkit package, so **no version bump, no website rebuild, no fieldkit release**): `sh -n` clean; guard (missing `OF_LICENSE_URL` → order-page hint), bad/expired-payload rejected **before** any install (license-first ordering), license drops `-rw-------`, venv resolution + `OF_SKIP_DOCTOR`/`OF_SKIP_UP` exit cleanly. The smoke caught + fixed the original `pip install --user` PEP-668 failure (→ venv path) and the install-before-license-fetch ordering (→ license-first fail-fast).
- **Relay filed** (Spark→Mac, `[needs Mac action: DNS + host + order-page snippet]`). **No commit/push yet** — working-tree changes ready (the two new `deploy/` files + HANDOFF + beacon + private relay); operator gates the main push.
- **➡️ NEXT on this track (all still Spark-owned, unblocked vs. blocked):** *unblocked* — cosign-keyless sign the GHCR lane image (§9/M3, needs interactive GitHub OIDC → operator-armed); optional `hermes` verify gate. *Blocked-on-peer* — embed the prod pubkey (waits on ops sending it; `PROD_KEY_PENDING`); fold final SKU names into the known-sets (waits on Mac).

### 2026-06-13 — Distribution exploration (Spark-owned): NGC/Supabase **read STARTED + delivered** — NGC is brand-only, entitlement stays Supabase+license (this session)

- **Took the Mac→Spark distribution reassignment and delivered the go/no-go read** (the pre-GA exploration, not a weekend blocker). **Supabase CLI installed + authed on the `orionfold` account** (v2.106.0 arm64 at `~/.local/bin/supabase` → `~/.local/share/supabase/{supabase,supabase-go}`; auth token in `~/.supabase/`). Confirmed the prod project **`orionfold` / ref `lgnmmcxvwdnusvfpguvf`** (us-east-1, pg17) already runs the commerce spine: Edge Functions `create-checkout-session` v13, `stripe-webhook` v9 (where `fulfillLicense` is scaffolded), `customer-portal`, `order-status`.
- **NGC go/no-go = NO-GO as a transport/entitlement win; conditional-GO as a GA *brand* play only.** The headline finding: the "customer reuses their own NGC key to pull our lane image" rationale **is mostly false** — a customer's NGC key only authorizes the *public* Catalog + orgs they're a member of, **not** our private `nvcr.io` namespace (pull is membership-gated). The only key-reuse path is the public NGC Catalog (free → can't gate paid; and itself gated behind **NGC Software Partner Network** editorial approval). **Inception ≠ publishing rights.** → recommendation flips **GHCR-now → GHCR-stays**; NGC Catalog only as an optional additive public mirror for credibility. Moots the User→Org conversion *as an NGC prerequisite*.
- **Supabase entitlement v1.1 = concrete shape authored, grounded in the live project** (extension of the existing spine, not new infra): an `fe_entitlements` table + one private `field-edition` Storage bucket (weights+bootstrap+license, signed-URL only) + extend `fulfillLicense` (sign Ed25519 / write row / deliver first signed-URL bundle) + a fast-follow `entitlement-fetch` fn (re-mint short-TTL signed URLs after live Stripe-status check). Slots **behind** the AC-7 license file: Supabase = online delivery + revocable gating; license = offline enforcement; GHCR stays the image registry (Supabase never in the OCI path). cosign = GHCR Sigstore-keyless at M3.
- **All findings written to two private specs** (gitignored — paths only): `_SPECS/arena-field-edition-distribution-v1.md` (→ **draft.2**: §7 NGC go/no-go, §8 Supabase v1.1, §9 cosign, Sources) **and a new `_SPECS/arena-field-edition-license-workflow-v1.md` (draft.1)** — the license **generation + customer assignment + validation** lifecycle + the forced ownership seam (Website owns issuance/assignment/delivery/revocation; Spark owns the format + offline validator; §6 conformance vector prevents canonicalize byte-drift; §9 OPEN-1 = the public-images/weights-are-the-moat decision that touches the shipped license schema). Registered both + the main FE spec in `_SPECS/index.md`. **Two Spark→Mac relay entries filed** (distribution `acted`; license-workflow `needs Mac alignment`). Strategy repo pushed (`92741ed` distribution, `5673409` license-workflow). **No public-repo code change** (read/strategy only; HANDOFF + beacon are the only public churn).
- **License conformance vector DELIVERED (this session, Spark side of license-workflow §6).** Frozen `fieldkit/src/fieldkit/field_edition/data/license-conformance-v1.json` (built by `scripts/field_edition/build_license_conformance_vector.py`, `--check` round-trips) — 4 cases stressing the cross-language canonicalization traps (recursive key sort, UTF-8 non-ASCII, scalars/no-floats, full license), each with exact `canonical_utf8`/`canonical_sha256_12`/dev-key `signature_b64`. New `test_license_conformance_vector_matches_live_verifier` → **124 field_edition tests** (was 123). CHANGELOG `[Unreleased]` + parked `docs/field_edition.md` updated. No version bump, no website rebuild. **Mac action:** add the mirror assertion to `fulfillLicense`'s CI (re-canonicalize + re-sign each case vs the vector; dev seed + pubkey are in the file).
- **➡️ NEXT on this track:** (Mac/commerce) wire the conformance vector into `fulfillLicense` CI; create the private `field-edition` bucket; stash the prod Ed25519 seed as a Supabase secret (pairs with `PROD_KEY_PENDING`); decide license-workflow OPEN-1 (public-images vs GHCR-token-DRM); pick `entitlement-fetch` auth. (Spark) cosign-keyless wiring at M3.

### 2026-06-13 — Arena Field Edition M2: first-boot `verify` receipt **NEAR-ALL-GREEN** (`lane` + Cortex grounded gates LIVE) + **AC-7 v1 license file delivered** (this session)

- **The launch-critical milestone: `fieldkit field-edition verify` now renders 4 PASS / 0 fail / 0 error / 1 skip (`ok=True`), live-smoked end-to-end on the box in ~86 s.** Two more §8 generation-half gates went from honest-error stubs to measured-live PASS against the warm resident stack (lane `:8091` + NIM embedder `:8001` + pgvector `:5432`, all up). The receipt flipped from this morning's "2 pass / 2 error / 1 skip" → **`fieldkit`+`advisor`+`cortex`+`lane` all PASS, `hermes` skip**. Only `hermes` (optional, `--hermes`) remains unwired. This is exactly the near-all-green launch receipt the weekend launch wanted (answers the Mac→Spark TIMELINE-COLLAPSED receipt-state question with the *upgraded* answer).
  - **`lane` gate (warm-resident) — operator decision taken.** Resolved the flagged §6-vs-§8 fork (AskUserQuestion → **warm-resident**): first-boot `verify` proves the resident lane is reachable + serves one generation, and does **NOT** tear the warm default down (§6 zero-swap common path). `_assess_lane` floor reworked to `launched + generated` (dropped `torn_down`); "teardown clean" is now the `down`/`repair` lifecycle gates' job. Unreachable → honest `error`; reachable-but-blank-gen → real FAIL (report=reality). Live-smoked PASS in 0.2 s.
  - **Cortex grounded-contract half — no new freeze.** New **pure** `fieldkit.field_edition.grounded` **reuses the already-blessed frozen recall probes** (operator chose this over freezing a new bench) + a faithful port of the Advisor grounded prompt (`_system_prompt`/`_user_prompt`/context-block builder). For a **deterministic stratified slice** of the frozen set (every refuse + route row + first 3 answer rows/family = 39 probes), the lane answers over **live-retrieved** context (the full retrieve→ground→generate→cite loop via `MemoryIndex.query`), scored by the **shared** `advisor.score_output`. `contract_pass` = all refuse rows refuse (hygiene, 16/16) **AND** answer/route citation rate ≥ `GROUNDED_CONTRACT_FLOOR` (0.80, the Advisor precedent). `LiveGateRunner.cortex` now measures **both** halves + merges metrics. **Live-smoked: recall@5 0.977 + grounded citation 19/23 (82.6%) + refusals 16/16 → contract PASS in 56 s.** Verified the 4 citation misses are **genuine model misses** (gold WAS in context; model cited a sibling spec/product) — not scorer or retrieval artifacts. The stale "embedder image digest mismatch" failure-`fix` text was reworded (closes the cosmetic wart flagged last entry).
  - **⚠️ Honest fragility note (for operator review):** the grounded citation margin is thin — 82.6% vs the 0.80 floor over only **23 cite rows** (15 answer + 8 route); one more flipped citation = 78.3% < floor → FAIL. Deterministic at temp 0 (reproducible run-to-run), so it's a *floor-margin* fragility, not run-noise. A `answer_per_family` knob widens the sample (slower gate) if a firmer margin is wanted later — same call as the recall set's `--per-family` option. Floor mirrors the blessed Advisor 0.80; kept as-is.
- **AC-7 v1 license file DELIVERED — `fieldkit.field_edition.license` (Spark owns the format + verifier; Mac/ops owns the signing commerce server + DNS).** Closes the Website→Spark open relay ask. Offline-verifiable JSON file (`~/.orionfold/license`): a `payload` of claims + a detached **Ed25519** `signature` verified locally against a pubkey embedded in `TRUSTED_KEYS` (no license-server round-trip — AC-7). The paid boundary is `registry.pull_token` (a GHCR read token the signature **binds** to the license — that token is the entire DRM; revocation = rotate it). **Signing contract** = `canonical_bytes()` (compact, recursively key-sorted, UTF-8, **no floats**), which Mac's `fulfillLicense` must match byte-for-byte (Python + JS port + keygen recipe + a valid sample `_IDEAS/field-edition-license-sample-v1.json` are in the relay). `load_license()` parses → verifies → enforces term. **Prod key `of-license-prod-2026` = `PROD_KEY_PENDING`** — ops generates the keypair + sends the public half, I embed + ship (the one remaining action to issue real licenses). Dev-only key (seed `bytes(range(32))`) signs the vendored `data/license-sample.json` so the schema self-validates. `cryptography` → the `arena` extra.
  - **`tier`/`edition`/`entitlements` = SOFT known-set (operator decision), NOT a strict enum.** They're descriptive, not security-bearing, so the installer recognizes known values (`KNOWN_TIERS`/`KNOWN_EDITIONS`/`KNOWN_ENTITLEMENTS`) for display/telemetry and logs a one-line "treating as generic" on anything new — **never rejects**. Keeps SKU velocity decoupled from the installer release cadence + avoids an older installer hard-failing a newer edition. Unknown entitlements ignored, not rejected. A new SKU just needs a Stripe price + a `fulfillLicense` mapping. **Open back to Mac:** send the final SKU names → I fold the recognized ones into the known-sets (cosmetic; unrecognized works either way).
- **Tests:** 14 new across both deliverables → **123 field_edition tests** (gates: lane floor + live pass/unreachable/empty-gen, grounded probe-selection/blocks/excerpt/messages/scorer/hygiene, cortex both-halves/refusal-regression/lane-down; license: canonical-bytes, sample-validates, sign/verify round-trip, tamper/unknown-key/prod-pending/expired/missing-file, soft-known-set no-reject + clean-known). Hardened 2 pre-existing env-dependent live tests to a **dead-port config** (deterministic regardless of box lane state). Full collectable fieldkit suite **1294 passed** (only the 3 documented env-arena failures — webui no-cache, grounded-receipts endpoint, real-SIGTERM; none touch `field_edition`). CHANGELOG `[Unreleased]` + parked `fieldkit/docs/field_edition.md` updated. **No version bump, no website rebuild** (fieldkit-internal). Near-all-green receipt preserved at `evidence/orionfold-advisor/field-edition-verify-near-all-green-2026-06-13.json`. **Shipped:** public wheel `0440b71` (gates) + `e19345e` (license) + `67451c7` (soft known-set); strategy `2c528e0` + `0bd6de0` (relay/spec/sample).
- **Stack left UP** (operator-armed): lane `:8091` + embedder `:8001` + pgvector `:5432` all up at handoff (tear the lane down via `pkill -f 'llama-server.*8091'` if needed).
- **➡️ NEXT:** (1) optional `hermes` gate (last generation-half — the MCP `fieldkit` tool round-trip; only verify gate still unwired). (2) **License: embed the prod pubkey** once ops sends it (flips `PROD_KEY_PENDING` → real key, unblocks issuing real licenses) + fold final SKU names into the known-sets. (3) Remaining GA items below (cosign-sign the GHCR image; `get.orionfold.com` bootstrap + DNS — the bootstrap script is Spark's lane, the license-file *format* is now done; HF card Q4_K_M feature; advisor-manifest `_FLOWS/` re-freeze). (4) **Distribution exploration — now Spark-owned** (see dedicated note below).

> 📌 **OPEN — Distribution exploration (GHCR vs NGC vs Supabase) is a SPARK action** (Mac→Spark reassignment, relay `[open]` 2026-06-13 "distribution exploration … reassigned to Spark"; was the mis-tagged `strategy-4` website delegate). **Pre-GA hardening track — NOT a weekend-launch blocker** (GHCR-now stands). I already authored `_SPECS/arena-field-edition-distribution-v1.md`, so the exploration follows from it. **Deliverable to Mac (next relay, no deadline): a short go/no-go read** on — (a) **NGC (`nvcr.io`) GA feasibility**: what an Inception/partner NGC-org allocation requires (gating + timeline), and confirm the "customer reuses their existing NGC key for the lane image" story holds end-to-end (migration cost = one re-push + one digest re-pin in `compose.py` per §4 — confirm that survives contact); (b) **Supabase signed-URL entitlement v1.1 sketch**: the per-customer, Stripe-status-tied, revocable URL layer for GGUF weights + bootstrap + license issuance (§3) — minimal v1.1 shape, and how it slots *behind* the license file `verify` already validates (the signed URL delivers license + weights; the file is what the installer then checks); (c) **cosign signing** of the lane image (§9/M3, registry-independent — fold into wherever the image lands). **Stays operator/Flows (consumes my findings, NOT mine to decide):** the `orionfold` GitHub **User→Org** conversion vs skip-straight-to-NGC, and the final GA registry call (made *after* my NGC read — if GA is NGC, the Org conversion may be moot per §5). Already ack'd in the relay; queued behind the launch-critical gates.

### 2026-06-13 — Arena Field Edition M2: the §8 **Advisor gate is now measured LIVE** (first generation-half gate wired + live-smoked) (earlier this session)

- **The §8 first-boot `verify` battery gained its first generation-half gate.** Before this session only `fieldkit` (import/version/matrix) and the Cortex **recall-half** were live; `advisor`/`lane`/`hermes` + the Cortex gen-half were honest-error stubs. The **`advisor` gate is now real, live, and PASSING** — `fieldkit field-edition verify` scores the resident lane against a frozen curveball set and renders `Advisor → pass · 85.7% · refusals 9/9`. Receipt split is now **2 pass / 0 fail / 2 honest-error / 1 skip** (`fieldkit`+`advisor` PASS · `cortex` recall-half live 0.977 / gen-half pending · `lane` pending · `hermes` skip) — a real advisor PASS where there used to be a stub.
  - **Vendored frozen curveball set (operator-blessed public freeze).** New `fieldkit.field_edition.advisor` ships `data/advisor-curveball-mini.json` — the **21 scored prompt packets behind the published 85.7% Q4_K_M run**, sha-pinned `CURVEBALL_SET_SHA=7218d717a6fa`, in the wheel via the `data/*.json` glob, built by `scripts/field_edition/build_advisor_curveball_set.py` (`--check` round-trips). **Why freeze packets, not regenerate:** each packet's BM25-retrieved context is baked into its message, and the public corpus manifest grows with every article — proven this session (a regen now picks a different Source 5 than the 13:12 run). So the gate ships the **exact packets immune to corpus drift**, not a re-derivation. Load-time sha check raises on out-of-band edits.
  - **Pure scorer = faithful port** of `preflight._score_output` (citation integrity, refusal hygiene, routing, thinking-leak, private-state-leak). **Regression-locked against the REAL captured outputs** (not just fakes): scoring the committed `*-q4km.results.jsonl` reproduces 18/21 with **zero verdict mismatches** vs the frozen receipt — the exact 3 safe-direction misses (`0005` over-refusal, `0009`/`0011` citation-correct route-prefix).
  - **`LiveGateRunner.advisor` wired:** replays the packets through the lane (`127.0.0.1:8091/v1`, OpenAI-compat, `enable_thinking=False`, temp 0, 700 tok), scores, applies the floor (curveball ≥80% + refusals 9/9); unreachable lane → honest `error`, never a vanity pass.
- **Live-smoked (operator-armed lane):** operator served the staged Q4_K_M (`/tmp/orionfold-stage/advisor-gguf/model-Q4_K_M.gguf`) on `:8091` via the host `llama-server`; the gate scored **85.7% (18/21), refusals 9/9, misses 0005/0009/0011 in 28 s** — **byte-identical to the frozen receipt** even on the May-12 host build (the CUDA-13-image build delta was a non-issue at temp 0). Full `verify` CLI run confirmed the whole receipt; a copy preserved at `evidence/orionfold-advisor/field-edition-verify-advisor-live-2026-06-13.json`; dogfood `~/.orionfold/receipts/` cleaned.
- **Tests:** 9 new (sha-pin load + drift-raise + pure scorer citation/refusal/route + thinking-leak + aggregate floor + length-mismatch + the live gate reproduces the published 85.7% from captured outputs + refusal-regression fails the floor + unreachable-lane honest error) → **101 field_edition tests**; full collectable fieldkit suite **1282 passed** (only the 3 documented env-arena failures — webui no-cache, grounded-receipts endpoint, real-SIGTERM; none touch `field_edition`). CHANGELOG `[Unreleased]` + parked `fieldkit/docs/field_edition.md` reconciled. **No version bump, no website rebuild** (fieldkit-internal). The 85.7% proof receipt was committed into `evidence/` (it lived only in `/tmp`).
- **➡️ NEXT (remaining generation-half gates, same pattern):** wire **`lane`** (LaneTruth launch→generate→teardown — note the open §6-vs-§8 spec Q: warm-resident default vs "teardown clean"), the **Cortex grounded-contract half** (citation integrity + refusal hygiene over the lane), and optional **`hermes`** — each lights up the same way against the live lane → flips the receipt toward near-all-green. **Known cosmetic wart:** the Cortex gate's failure `fix` text still says "embedder image digest mismatch" even when the recall-half is passing 0.977 and only the gen-half is pending — reword when wiring the gen-half.
- **Lane left UP** (operator-armed `:8091`) at session handoff unless the operator tears it down (`pkill -f 'llama-server.*8091'`); embedder + pgvector up (12 h).
- **📥 INBOUND Mac→Spark ask arrived mid-push (relay `[open]`, `ainative-business-website/_RELAY.md` 2026-06-13 "Website→Spark — orionfold.com FE storefront"):** the orionfold.com FE storefront is wired (catalog + 3 Stripe SKUs + edition-split + velocity copy, uncommitted/operator-gated). Their **one open ask = the v1 license-file schema** the installer's `verify`/bootstrap validates — exact fields, how the GHCR pull token is packaged, which key signs the **Ed25519** signature + where the public key lives in the installer (+ a sample valid license file). They've scaffolded `fulfillLicense` in `stripe-webhook` and need this to hand-issue founding-25 licenses this weekend. This is the **AC-7 license/bootstrap lane** (Spark builds the bootstrap script + license-file format; Mac/ops owns DNS + the commerce server) — a distinct launch task from the verify gates. Not yet started.

### 2026-06-13 — Arena Field Edition M2: the full proven matrix is PUBLISHED + fully digest-pinned (earlier this session)

- **The launch artifacts are live.** The Field Edition proven matrix now resolves entirely by digest — `unpinned_images()` returns `[]` for the default config and `fieldkit field-edition up --dry-run` renders an **all-`@sha256` Compose bundle**. A live `up` can now pull + run the whole stack.
  - **Q4_K_M Advisor published + pinned.** Quantized the promoted v0.2 BF16 → F16 GGUF (in the nemo container) → **Q4_K_M** via the box's `llama-quantize` (the correct path; requant-from-Q8_0 is disabled in llama.cpp). **Quality-gated on a live lane *before* publish** (the right gate, not a vanity pass): served the Q4_K_M and scored the frozen **curveball-v0.2** (21 rows) via `preflight.py --reasoning-mode off` → **85.7% (answer 7/8, route 2/4, refuse 9/9)** — byte-identical to the Q8_0 baseline, **same 3 safe-direction misses** (0005 over-refusal; 0009/0011 citation-correct route-prefix). Clears the §8 Advisor floor (≥80% + refusals 9/9); the heavier quant did not regress the default. Published to **`Orionfold/Advisor-GGUF`** (rev **`dad6d1e5`**, classic-LFS — xet was blocked by a root-owned `~/.cache/huggingface/xet`), pinned in `LaneConfig.gguf_revision`.
  - **CUDA-13/SM121 lane image built + smoked + pushed.** New reproducible multi-stage Dockerfile `deploy/field-edition/llama-server-cuda13/` (pins llama.cpp `856c3ad`/b9128, CUDA 13.0, SM121). Build needed a **forced CUDA driver-stub link** (`-L .../stubs -lcuda`; ggml didn't emit `-lcuda` and the build container has no driver — SONAME `libcuda.so.1` resolves to the Toolkit-injected driver at runtime, stub never bundled). Smoked: serves the Q4_K_M, `/v1/models` 200 in ~3 s (kernels baked, no cold JIT), `/v1/chat/completions` → `finish_reason=stop` with correct `reasoning_content`/`content` split. Pushed to **`ghcr.io/orionfold/llama-server-cuda13:0.1`** (**`sha256:93993cc2`**), pinned in `LaneConfig.image`.
  - **NIM embedder = the v1 default** (operator decision): digest-pinned, recall-proven (the exact embedder behind recall@5 0.977); the open embedder moves to **v1.1** (`--open-embedder` / `with_open_embedder`). Rationale: the FE ICP already runs NGC, so the NIM dep is near-zero friction; AC-2's substance (offline *operation*, no phone-home) holds. `up.pull` wired (resumable `hf_hub_download`, pin-by-revision; `REV_PENDING` sentinel). **92 field_edition tests**, full suite 1272 passed (only the 3 documented env arena failures). Shipped **`06df6f7`** (wiring + NIM-default + Dockerfile) + **`d31423e`** (publish + pins).
- **GHCR auth learned (memory saved `reference_publish_auth_surfaces`):** HF token = `Orionfold` (ready, as always); git push = `manavsehgal` `repo` scope (works); **GHCR container push is new** — `orionfold` is a separate GitHub **User** account (not an org), so `ghcr.io/orionfold/*` packages need **orionfold's own token** (`manavsehgal` got `permission_denied: create_package`). Resolved via `gh auth login` as a 2nd account (orionfold, `write:packages`, device flow) → `docker login` → push, then `gh auth switch -u manavsehgal` back for git.
- **Distribution decision deferred to pre-GA (discussed):** images **GHCR-now → NGC-for-GA** (Inception alignment + reuse the customer's existing NGC auth); **weights + license + bootstrap → Supabase signed URLs** (per-customer, revocable — the commerce/entitlement layer the registry shouldn't be); **convert `orionfold` GitHub User → Organization** (a personal account is weak for a paid product). Registry (transport) and entitlement (commerce) are separate concerns — Supabase is disqualified as the image registry (not OCI) but is the right home for weights/license.
- **⏩ TIMELINE COLLAPSED — full-public launch THIS WEEKEND** (Mac→Spark relay pulled this session, `a4f669d`): the operator superseded the M0–M4 / ~08-21 chain — launch is *this weekend*, website + marketing executing in parallel. No engineering re-scope; the v1 stack is unchanged. Today's proven-matrix publish is squarely on that critical path. **Answered Mac's single open launch question** (the first-boot receipt state — see the Spark→Mac relay entry) and **dropped a real `verify` receipt** into the share (`_IDEAS/field-edition-verify-receipt-sample.json`). Current receipt: `fieldkit` PASS · `cortex` honest-error (recall-half live ✓ 0.977) · `advisor`/`lane` honest-error · `hermes` skipped.
- **➡️ NEXT SESSION (the guided live `up` — operator-armed, item 1 of GA): I drive, the operator brings up the box.** Sequence: operator confirms the NGC embedder + pgvector are up and brings up the pipx Arena cockpit; I run `fieldkit field-edition up` (now resolves the whole stack by digest) → then **wire + live-smoke the four generation-half verify gates** (#5: advisor PASS is already proven at 85.7%, cortex full PASS, lane launch→gen→teardown — open spec Q: §8 lane gate "teardown clean" vs §6 resident-stays-warm) → produce the **near-all-green** launch receipt. This is what flips the weekend receipt from 1-pass to mostly-green.
- **Remaining GA items after that:** **cosign-sign** the GHCR image (§9/M3 signed update channel — *not* a first-boot blocker; pull works unsigned); **`get.orionfold.com` bootstrap + DNS + Ed25519 license server** (AC-7); the HF card update (feature Q4_K_M); the advisor-manifest `_FLOWS/` re-freeze (a local gitignored compat symlink `_FLOWS/the-machine-that-builds-machines.md → _GUIDES/...` unblocks `preflight` for now).
- **Distribution strategy recorded** (this session) at `_SPECS/arena-field-edition-distribution-v1.md` (private): registry ≠ entitlement; **GHCR-now → NGC-for-GA** (images), **Supabase signed URLs** (weights + license + bootstrap), **convert `orionfold` GitHub User → Org** (relay TODO for strategy). Pre-GA, not a launch blocker. `reference_publish_auth_surfaces` memory saved (HF=Orionfold ready, git=manavsehgal, GHCR needs orionfold's own token).

### 2026-06-13 — Arena Field Edition M2 step 1: the §8 Cortex gate's recall-half is now measured LIVE (vendored frozen set + pure scorer) (earlier this session)

- **The first M2 *infra* piece is code-complete AND live-smoked** — the Cortex gate's recall-half no longer reports a blanket "not wired" stub; it measures retrieval recall for real against the running Cortex stack. This is the one bench `LiveGateRunner` gate that does **not** need the operator-armed serving lane (the embedder + pgvector are enough), so it lit up this session.
  - **New `fieldkit.field_edition.recall` (the pure half):** a **vendored frozen recall set** `field_edition/data/cortex-recall-mini.json` — a deterministic projection of the already-frozen Advisor recall bench (source sha `3220b8e799cd`) down to the recall fields (query + gold source_ids + family/split/behavior), sha-pinned `RECALL_SET_SHA=97ce168b851d`, ships in the wheel via the existing `data/*.json` glob; built reproducibly by `scripts/field_edition/build_cortex_recall_set.py` (`--check` round-trips). Plus a **pure** `score_recall_set()` (source_recall@5 over answerable rows, refusal rows excluded; retrieval injected → unit-tests with a fake). A **load-time sha check raises on out-of-band edits** to the shipped set (proof-control discipline).
  - **`LiveGateRunner.cortex` wired:** builds the retrieval closure over `MemoryIndex.query` (pgvector dense, source-deduped, pool=80) against `advisor_corpus_v01` and scores the vendored set. **Live-smoked: `recall@5 = 0.977 over 87 rows, 2 misses, in 2.7 s`** — byte-identical to the `score_recall_live.py` proof, well under the §8 ~1–2 min budget. The gate still reports an **honest non-pass**: the recall number is real and surfaced in the receipt detail + note, but the grounded-contract generation half (citation integrity + refusal hygiene) needs the serving lane (M2) — never a vanity pass on recall alone.
- **FREEZE blessed (operator-approved this session):** shipped the **full 87-row frozen slice, not a sub-sample**. Design finding recorded in the builder docstring: the live floor (0.95) sits only ~0.027 above 0.977 — a ~2.3% natural miss rate vs a 5% tolerance, so sub-sampling to a true "mini" set makes the floor **fragile** (small-N quantization swings 1–2 misses past 0.95). "Mini" = small vs a customer corpus, not a bench sub-sample. A `--per-family N` sub-sample knob is a documented future option if a faster gate is wanted (accepting that fragility).
- **Tests:** 8 new (sha-pin load + drift-raise + pure scorer perfect/miss/k/empty + live cortex recall-measured-with-honest-pending + below-floor-honest + unreachable-errors-with-fix via a fake `MemoryIndex`) → **86 field_edition tests** (was 78). Full collectable fieldkit suite **1267 passed** (only the 3 documented pre-existing/environmental arena failures — webui no-cache, grounded-receipts endpoint, real-SIGTERM; the 6 collection errors are missing-dep imports in the minimal `/tmp/fk` venv). CHANGELOG `[Unreleased]` extended; parked doc `fieldkit/docs/field_edition.md` verify section updated (still outside the website glob — M4 card deferred). **No version bump, no website rebuild** (fieldkit-internal). **Shipped to origin** (`058ce7b`).
- **What remains for the Cortex gate to fully PASS (unchanged, operator/hardware-gated):** the **grounded-contract generation half** (citation integrity + refusal hygiene) needs the operator-armed serving lane (the published+pinned Q4_K_M Advisor rev). The recall-half is done. Stack left as found (cortex up; lane + cockpit down).
- **Strategy repo synced (this session):** pulled (`2bb4ae4`) → updated the private spec to record the recall-half discharge (§8 "Implemented (M2 step 1)" note + §5/§7 Cortex row + §12 log entry) and pushed (`7df08c9`); Spark→Mac relay entry filed. **Closed the Mac→Spark `[open]` §10 playbook copy item** — swapped both product-level "studio" → "AI lab" (title + lead-in) in the spec's §10 playbook draft per the operator brand-term rule ("studio" = Orionfold the company; the product is "a private AI lab"); relay thread flipped to closed, pushed (`9bb3604`). **No open cross-box items.**

### 2026-06-13 — Arena Field Edition: the full §7+§9 installer CLI surface COMPLETED (down/repair/rollback/update no longer stubs) (this session)

- **The four remaining `field-edition` commands are now real code, not milestone stubs** — the `_SPECS/arena-field-edition-v1.md` §7 + §9 CLI surface is implemented end-to-end. Same pure-core / thin-I/O split as `doctor`/`up`/`verify`; each command **fails honestly** at the boundaries that still need M2/M3 infra (the unbuilt GHCR images, the unpublished signed channel) rather than stubbing the whole body.
  - **`down`** (new `fieldkit.field_edition.down`, §7/AC-6 uninstall): pure `plan_down()` (removed vs. preserved) + injectable `DownExecutor`. Default `down` removes containers+network but **preserves** the Cortex pgdata volume, model store, and `arena.db`; `--purge` drops those (explicit opt-in). Cockpit is a pipx host process → its uninstall is printed, not self-destructed. Idempotent on a clean box. **Live-smoked:** default `down` ran green and left `arena.db` untouched.
  - **`repair <advisor|cortex|lane>`** (new `fieldkit.field_edition.repair`, §8 failure-UX hatch): force-recreates one component's container(s) (re-pull pinned image), re-pulls model weights if it owns any, re-runs **only that component's §8 gate**, prints a fresh honest receipt-line. Pure `plan_repair()` + injectable executor + the existing `GateRunner`. **Live-smoked:** unknown component errors cleanly.
  - **`update` / `rollback`** (new `fieldkit.field_edition.update` over new `fieldkit.field_edition.proven_matrix`, §9 channel): fetch→cosign-verify→apply→§8 gate→receipt with **automatic rollback** to the prior matrix (retained on disk via `save_current` current→previous rotation) on apply/gate failure; `rollback` = manual escape hatch. The signed GHCR channel is the **only external boundary** — `LiveUpdateChannel` raises an honest `UpdateError` ("no published channel yet — M3"). **Live-smoked:** `update` aborts honestly, `rollback` reports nothing-to-restore.
- **Tests:** 26 new (78 field_edition total, was 52) — `down` plan/run/purge/teardown-failure + CLI; `repair` plan/known+unknown/repull/recreate/gate-fail; `proven_matrix` round-trip/fingerprint/rotate/rollback; `update` no-channel/already-current/apply-green/auto-rollback-on-gate-fail/auto-rollback-on-apply-fail + `rollback` restore/nothing-to-restore + the live channel is honest. Full collectable fieldkit suite **1259 passed** (the only 3 failures are the documented pre-existing/environmental arena tests — webui no-cache header, grounded-receipts endpoint, real-SIGTERM timing — none touch `field_edition`). CHANGELOG `[Unreleased]` extended; parked doc `fieldkit/docs/field_edition.md` CLI-surface section rewritten (still outside the website glob — M4 card deferred). **No version bump, no website rebuild** (fieldkit-internal).
- **What this does NOT do (genuinely operator/hardware-gated, unchanged):** build+push+digest-pin the 2 GHCR images; publish+pin a Q4_K_M Advisor-GGUF rev; the operator-armed live serving-lane smoke; the proof-control **freeze** of the vendored mini recall set. The installer *orchestration* is now code-complete; the live operations light up as that infra lands. **Shipped to origin** (`f3b8edc`); strategy repo pushed (`6aa8077`, after a rebase that also absorbed the Mac-side AC-2 reword — the "Platform API serves" clause is dropped from §3 AC-2 since the Platform is out of scope).

### 2026-06-13 — Arena Field Edition M2: owed live-lane recall re-run DISCHARGED + M1 surface re-confirmed on the box (this session)

- **Discharged the §1-Cortex "live-lane recall re-run owed per build-manifest"** (spec line 77): scored the frozen Advisor recall bench through the **live** Cortex stack (pgvector `:5432` + `nim-embed-nemotron` `:8001`, both up 9 h; `advisor_corpus_v01` intact 646 chunks / 183 slugs) via `score_recall_live.py --skip-ingest` → **source_recall@5 = 0.977, gate PASS** over 87 answerable rows — **above the §8 Cortex gate floor (≥0.95)**, byte-identical metrics to the prior receipt (only the `generated` date advanced 06-09→06-13; predictions byte-identical, deterministic retrieval). **This confirms the Cortex gate's published floor is satisfiable on the actual running embedder image — de-risks M2.** Spec line 77 updated to record the discharge (private repo).
- **Re-confirmed the shipped M1 installer surface healthy on the box** (no code change — read-only dogfood): `field-edition doctor` green (DGX OS 7.4.0 / driver 580.159.03 / CUDA 13.0 / Docker 29.2.1 / nvidia-ctk 1.19.1 — matches the re-pinned matrix); `field-edition verify` → **fieldkit gate green** (import + version 0.31.0 + matrix), the three bench gates **honest-error** (advisor/cortex/lane "M2 — needs the live stack + pinned Q4_K_M"), hermes skipped, receipt written. Test receipts cleaned from `~/.orionfold/receipts/`.
- **Honesty note logged for the M2 wiring:** the Cortex gate's current stub error says *"not yet wired to the live Cortex stack"* — but the stack **is** up and recall@5 was just measured against it. The real M2 blockers for that gate are (a) **vendoring a frozen mini recall set into the shipped package** and (b) the **serving lane** for the grounded-contract generation half (citation-integrity + refusal-hygiene). Both are operator/spec-gated (proof-control freeze discipline) → not wired blind this session. Recall *floor* now proven; only the package-vendor + lane-smoke remain for that gate.
- **No code shipped.** Evidence date-bump (`rag-recall-v0.1-cortex.json`) + HANDOFF + beacon only; spec note private. Stack left as found (cortex up, lane + cockpit down).

### 2026-06-13 — Advisor v0.3 packet-contract work: diagnosed, then PAUSED behind Arena Field Edition (operator directive, this session)

- Started the v0.3 step-1 packet-contract fixes; produced a **refined, evidence-grounded diagnosis** (classified all 51 answer rows of the latest frozen-pack receipt + live-probed the retrieval stack, which is up: pgvector + embedder on `:8001`). **Headline: the packet-contract levers are smaller/harder than the documented framing** — GE-2 is mostly model-paraphrase-vs-verbatim-scorer (only 2/8 are genuine excerpt drops), GE-3 is a real dense-retrieval misranking wall (11/12 gold-in-pool-but-ranked-out; naive RRF/widen-k recover ~1–3 with regressions), and ~29/51 failures are model behavior → the real headroom is **training calibration (step 4)**, not the packet. Full diagnosis preserved in Open Items → "Advisor / Arena" so it isn't re-derived.
- **Operator paused this work and prioritized ALL Arena Field Edition tasks first for next session.** Confirmed (against `verify.py`) that **v0.3 is NOT a Field Edition release prerequisite**: the §8 verify gate floors the Advisor on curveball-v0.2 ≥80% (v0.2=85.7% ✓) + refusals 9/9 ✓ and Cortex on recall@5 ≥0.95 (0.977 ✓) + a citation-integrity smoke flag — none is the grounded-pack pass rate v0.3 improves. v0.3 is a model-quality uplift for the §9 update channel; soft coupling only (which Advisor rev becomes the Q4_K_M default).
- **No code shipped for v0.3** — diagnosis only; the retrieval stack was read-only (no DB/lane mutations). The generation lane (`:8091`) + sidecar are down; embedder + pgvector remain up (8 h).

### 2026-06-13 — Arena Field Edition: §7 tested matrix re-pinned to the 7.4.0 clean-wipe target (earlier this session)

- **Closed the "re-pin before M2" flag** (the one decoupled, autonomous M2-prep item): `TESTED_MATRIX` in `field_edition/doctor.py` carried the original 7.2.3-era spec baseline while the dogfood box has moved on. Probed the live box — **DGX OS 7.4.0** (the running `DGX_SWBUILD_VERSION`; **7.5.0** is the pending OTA), **driver 580.159.03**, CUDA 13.0, Docker 29.2.1, nvidia-ctk 1.19.1 — and re-pinned the version-min baselines to the actual clean-wipe target: `dgx_os` 7.2.3→**7.4.0**, `driver` ≥580.95.05→**≥580.159.03** (CUDA stays 13.0; Docker/Container-Toolkit stay `present`-checks, no version baseline). **Minimum-version semantics unchanged** — a newer base still passes and the report still surfaces drift; `doctor` now runs **green live at `tested ≥ 7.4.0`** matching the box exactly.
- **Tests:** `_all_green_probes()` moved to the new baseline; the `newer-than-tested-passes` test bumped to genuinely-newer values (7.6.0 / 590.10.05) so it stays meaningful → **52 field_edition tests green**. Reconciled the stale `7.2.3` parentheticals in the parked `fieldkit/docs/field_edition.md` + CHANGELOG `[Unreleased]` doctor entry, and the docstring example. **No version bump, no website rebuild** (fieldkit-internal; the parked doc isn't globbed).
- **Spec (private):** §7 matrix line + §7.1 prerequisites + the §7 "Implemented" drift note moved off 7.2.3 onto 7.4.0; packaging-rationale "7.2.3" generalized to "7.x"; §12 decision-log entry added. Strategy repo pushed `44639ed`.
- **Shipped to origin** (`d08f8e3`, pushed this session). The two remaining `TESTED_MATRIX` consumers (`up.py`/`verify.py` matrix gate) inherit the new baseline automatically. **M2 sequencing note unchanged:** the next Field Edition increment is the live `up`/`verify` infra (build/push the 2 GHCR images + publish+pin a Q4_K_M Advisor rev), still largely operator-armed.

### 2026-06-13 — Arena Field Edition M1: `fieldkit field-edition verify` (§8 first-boot eval gate) shipped + live-smoked (earlier this session)

- **`verify` built — the §8 eval gate, as a pure-verdict + always-on-receipt machine.** New public module `fieldkit.field_edition.verify` with the five §8 gates (`fieldkit · advisor · cortex · lane · hermes`). Same pure-core / thin-I/O split as `doctor`/`up`: a `GateRunner`/`LiveGateRunner` **measures** each component (the only I/O) returning raw `GateOutcome` metrics; pure `assess_gate()` applies the **published floors** (Advisor curveball-v0.2 ≥80% + refusals 9/9 · Cortex recall@5 ≥0.95 + grounded contract · lane launch→generate→teardown · fieldkit import+version+matrix) and `evaluate_gates()` builds the `VerifyReport`; `VerifyReport.receipt()` renders the receipt dict **clock-free** (the writer stamps time) → fully unit-testable with a fake runner. **Always emits the receipt — pass OR fail** (the honest-receipt invariant) to `~/.orionfold/receipts/verify-latest.json` + a timestamped archival copy. Every failing gate names **component + gate + fix** (§8 failure UX). CLI: `--json`, `--hermes`.
- **`up --verify` wired (collapses §7 steps 2–3):** `up.py`'s `verify` phase now runs `run_verify` as its final phase and raises a `PhaseError` (with the receipt path) on a failing gate, replacing the prior "next increment" stub.
- **Live-smoked on this box:** `fieldkit field-edition verify` ran green on the **`fieldkit` gate** (import + version 0.31.0 + the doctor matrix all green) and **honest-`error`** on the three bench gates (advisor/cortex/lane — each "not yet wired to the live stack — M2", no vanity pass), hermes **skipped** (not enabled); receipt written, exit 1 (honest). `--json` echoes the on-disk receipt (carries `generated_at`). Test receipts cleaned from `~/.orionfold/`.
- **Tests:** 20 new (pure floors per gate + `evaluate_gates` pass/fail/error/skip + always-on receipt + the live `fieldkit` gate is real + bench gates error honestly + `run_verify` writes latest+archival even on failure + the verify CLI pass/fail/json) → **52 field_edition tests green**; full fieldkit suite **1233 passed** (the only failures are 3 pre-existing/environmental arena tests — webui no-cache header, grounded-receipts endpoint, real-SIGTERM timing — confirmed failing on clean HEAD with my changes stashed; none touch `field_edition`). CHANGELOG `[Unreleased]` extended; parked doc `fieldkit/docs/field_edition.md` updated (verify section + CLI surface; still outside the website glob — M4 card deferred). **No version bump, no website rebuild** (fieldkit-internal; the parked doc isn't globbed).
- **Shipped to origin** (`3eee448`, pushed this session) — the full `doctor`+`up`+`verify` M1 installer surface is now public in the wheel source.
- **What M2 lights up:** the four bench `LiveGateRunner` methods (advisor/cortex/lane/hermes) build on `fieldkit.eval` + the frozen benches once the live Field Edition stack + the pinned Q4_K_M model are up — the verdict/receipt/CLI are done and tested **now**, the bench measurements drop in as the infra lands (same staging as `up`'s live phases).

### 2026-06-13 — Arena Field Edition M1: `fieldkit field-edition up` (Compose bring-up) shipped + live-smoked (this session)

- **`up` built — the §7-step-2 bring-up, as a checkpointed re-entrant phase machine.** Two new public modules in `fieldkit.field_edition`: **`compose.py`** (pure `render_compose(config)→dict` for the digest-pinned Docker Compose bundle — three services: pgvector `of-cortex-db` + open embedder `of-embedder` + a llama.cpp CUDA-13/SM121 lane `of-advisor-lane`, model store mounted **read-only**, GPU reserved on the CUDA services, db healthcheck-gated; Arena cockpit is **not** in compose — it's the pipx `[arena]` process per §5) and **`up.py`** (phases `matrix→bundle→pull→stack→sidecar→resident→[verify]`, checkpoint to `~/.orionfold/state.json` after each, re-run **resumes from the last good phase**). Same pure-core / thin-I/O split as `doctor`: pure `plan_remaining()` + an injectable `Executor` (LiveExecutor shells to Docker/HF/cockpit; tests use a fake).
- **Pin discipline (the §9 "proven matrix" honesty):** `ImagePin` + `unpinned_images()`. **pgvector is digest-pinned** (`sha256:7d400e34…`, the box's arm64 pull — re-pin the multi-arch manifest-list digest at launch); the two **not-yet-built** Orionfold images (open embedder `ghcr.io/orionfold/cortex-embedder`, lane `ghcr.io/orionfold/llama-server-cuda13`) carry `PIN_PENDING`, and the `stack` phase **refuses to launch against them with a named fix** (no cryptic registry 404).
- **Live-smoked on this box:** `field-edition up --dry-run` runs the real matrix gate (green) + writes `~/.orionfold/compose.yaml` + `.env`, and the rendered bundle **validates against Docker's own `docker compose config`** (caught + fixed a real schema bug: GPU `capabilities` must be a flat `["gpu"]`, not nested). Re-run correctly **skips done phases**; a real (non-dry) `up` resumes from `pull` and **fails honestly** at the first phase blocked on unbuilt infra (clear fix, exit 1). CLI flags: `--dry-run`, `--force`, `--verify`, `--nim-embedder` (BYO-NGC-key embedder vs the open default).
- **Tests:** 18 new (compose render + pin discipline + pure phase planning + re-entrancy via a fake executor + the unpinned-images stack refusal + CLI dry-run) → **32 field_edition tests green**; core fieldkit suite **1213 passed** (the only failures are `fastapi`-missing arena tests in the minimal `/tmp/fk` venv — none touch `field_edition`). CHANGELOG `[Unreleased]` extended; parked doc `fieldkit/docs/field_edition.md` updated (still outside the website glob — M4 card deferred). **No version bump, no website rebuild** (changes are fieldkit-internal; the parked doc isn't globbed).
- **Committed to local `main`; public push pending operator go.** (The `doctor` module is already public at origin from last session, so exposure is no longer the concern — this is just the normal solo-blog "push when ready" gate.) `git push origin main` ships this `up` commit.
- **Known M1 gaps blocking a *live* end-to-end `up` (all expected, M2 work):** the open-embedder image + the CUDA-13 llama.cpp lane image aren't built (GHCR), there's no cosign/proven-matrix yet, the resumable HF GGUF pull isn't wired (Advisor-GGUF ships **Q8_0 only**; §5 default is Q4_K_M — needs a published+pinned rev), and the §8 `verify` gate isn't built. The orchestration + bundle are done and tested **now**; the live run lights up as those land.

### 2026-06-13 — Arena Field Edition M0 LOCKED + Platform reconciled to "separate product" (earlier this session)

- Pulled `orionfold/strategy`: two new Mac→Spark relay entries. **M0 is locked (2026-06-19)** — Flows accepted all §11 verdicts (AC-1 ≤60 min scoped to the default 4B path · AC-4 visible-swap counter as an integrity win · AC-3 precursor-at-v1 · §4 no-reinstall step-up) and the §13 dates (M4 launch-assets handoff ~08-21, ~5.5 wk before the 09-30 backstop). **Spark is clear to build toward M1 alpha (~07-17), after the Advisor v0.3 road.**
- **Operator correction (2026-06-13): the AI Native Platform is a *separate product*, not a Field Edition v1.1 bundle** — not in the stack at any version (v1 = Arena + Advisor + Cortex + fieldkit + quants + Hermes); its capabilities feed **Arena's** roadmap (agentic + Hermes) instead. Reconciled the Spark lanes in `_SPECS/arena-field-edition-v1.md` (→ v1.0-draft.4): §5 manifest + §11 verdict row "DEFERRED to v1.1" → "out of scope — separate product"; §11 step-up + §13 closing repointed; §12 logged; flagged the residual §3 AC-2 "Platform API serves" back to Flows (their lane). Closed the relay thread (Spark→Mac). Private repo pushed `b1fb0de`; public repo unaffected (spec is a private symlink).
- **M1 build STARTED — `fieldkit field-edition doctor` shipped + verified live on this box.** New public `fieldkit.field_edition` package (the §7 installer surface), on its own track independent of the Advisor v0.3 road. Live today: the support-matrix check (DGX OS / driver / CUDA / Docker / Container Toolkit) — pure `evaluate_matrix()` over a thin probe layer, `--json`, **minimum-version** semantics (newer-than-tested passes, too-old fails with a named fix), 14 tests. `up`/`verify`/`down`/`repair`/`rollback`/`update` are milestone-marked stubs (surface discoverable via `--help`). **Ran green live: DGX OS 7.4.0 / driver 580.159.03 / CUDA 13.0 / Docker 29.2.1 / nvidia-ctk 1.19.1.** **Matrix-drift finding:** the dogfood box has moved to DGX OS 7.4.0/7.5.0 vs the §7 7.2.3 tested baseline (the §9 churn) — re-pin before M2. CHANGELOG `[Unreleased]` Added entry; public docs *card* deferred to M4 launch (doc page parked at `fieldkit/docs/field_edition.md`, outside the website glob, with a wire-it-at-launch banner). Build green 538 pages; core fieldkit tests pass. **Public push of the new module is HELD for operator go** (pre-launch commercial-product code into the open wheel — consistent with §4/§9 but a publishing-timing call). Private spec updated (§7 + §12).

### 2026-06-12 — Private-by-construction reorg (`_SPECS`/`_GUIDES` private, `_FLOWS` retired) + Arena Field Edition spec lanes filled (this session)

- **`_SPECS` + `_GUIDES` are now private gitignored symlinks into `orionfold/strategy`** (joining `_IDEAS`), and the **`_FLOWS/` stream is retired** — `the-machine-that-builds-machines.md` moved into `_GUIDES/` (folded into its index). Public repo: 37 specs + 7 guides untracked → symlinked → gitignored; `_FLOWS/` deleted from tree+git. Doc-map repointed in **CLAUDE.md / AGENTS.md / CODEX-CC.md** (machine map + invariants now cite `_GUIDES/the-machine-that-builds-machines.md`). Privacy is now **structural — only released code is public, no per-push scrub**. Public commits `6669235` (_SPECS) + `a97928e` (_GUIDES/_FLOWS); private `c2f1e5a` + `912e6c6`. Build-safe (nothing globs these dirs; all remaining mentions are provenance comments or frozen evidence/transcript data).
- **`_RELAY.md` = the new cross-box mailbox** (private repo, per-project, append-only newest-first; pull at session start / push at session end — the contract that supersedes the public-PR seam). The 2026-06-12 Mac→Spark **Arena Field Edition handoff is CLOSED**: item 1 (_SPECS symlink) · item 2 (privacy flag — already scrubbed by the migration; the unsloth files were never tracked at scrubbed-main) · item 3 (engineering lanes) · item 4 (report-back) all done.
- **Arena Field Edition spec lanes filled** — `_SPECS/arena-field-edition-v1.md` **v1.0-draft.3** (private `f06f65e`), §5–§11 + §13. Decisions baked in (operator-confirmed): **pipx + Docker Compose hybrid** packaging (rides preinstalled DGX OS 7.2.3 Container Toolkit, dodges the aarch64+CUDA-13 wheel desert) · **GGUF-default / NIM-optional-BYO-key** (clean AC-2 offline, no NGC login / no $4.5k-yr NIM wall) · **eval-gated cosign-signed pinned-matrix update + auto-rollback** (not Watchtower) · **AI Native Platform deferred to v1.1** (Mac-origin, untested on Spark; doesn't block the evidence-tier step-up) · **AC-4 countered** (warm 4B default + honest visible load state; 30B cold swap = 2–5 min) · **AC-3 = precursor receipt at v1**. §13 milestones shifted **+2 wk** off the Advisor v0.3 critical path → M4 launch-assets handoff **~2026-08-21** (before the 09-30 falsification backstop). **Open for Flows:** review the §11 verdicts; one decision — must AC-1 "≤60 min" include a vertical beyond the default 4B (changes the staging design)?

### 2026-06-12 — Orionfold GitHub migration EXECUTED (this session)

- IP repos moved to the Orionfold entity account: history scrub (filter-repo, leak erased) → repo transfer → `_IDEAS` to private `orionfold/strategy` repo (symlink on Spark, clone on Mac at `~/orionfold/strategy`) → link sweep (this-repo URLs + stale fieldkit PyPI metadata that still pointed at archived `ai-field-notes`) → DNS tail driven over CDP in the operator's logged-in browser (`ainative.business` **verified** on the orionfold account; `www` CNAME → `orionfold.github.io`, propagated; apex 200 / www 301). Details in **"Orionfold GitHub migration"** under Open Items below; full runbook at `_IDEAS/orionfold-github-migration-plan.md` (private repo).
- **Mac side reconciled back (same day):** `mac-handoff.md` executed in full and deleted (monorepo clone reset to scrubbed main + symlinked). Mac's Consolidation Program C1–C6 evolved `orionfold/strategy` into a **dual-role repo** — Orionfold's design-studio state at root (Mac-owned, agency-cockpit-rendered, read-only context for Spark) + per-project `_IDEAS/` share folders (Spark writes only there). **Spark rules live in the strategy README's "evolved since the Spark bootstrap" section — re-read it when in doubt.** C4 also transferred `ainative` → orionfold (Mac clone now `~/orionfold/ainative`; 8 apply-*/stats/deck skills updated Mac-side `fd55e84`); Spark followed through with the live-site link sweep Nav/Footer/CTAs/about/llms*/humans → `orionfold/ainative` (`f7ec6d2`, build green). **Operator rulings (final): `spark-skills`, `ai-field-notes`, `stagent-io-redirect` stay on `manavsehgal`** — their links stay as-is; `webmemo` moves someday (unbound).

### 2026-06-12 — Orionfold State of the Union report (strategy milestone, earlier session)

- **Report shipped to `_IDEAS/` (gitignored — these pointers are the record):** `_IDEAS/orionfold-state-of-the-union-2026-06-12.md` (~5.5k-word analyst/investor edition) + `.pdf` (9-page print edition, brand-styled, 6 hand-authored SVG charts from real repo numbers, rendered via headless snap chromium from the kept `.html` source) + `_IDEAS/sotu-2026-06-research/` (8 web-research briefs over the open NVIDIA stack with full URL lists). Method: 3 Explore reconstructions of the Advisor/Cortex/Arena/fieldkit/editorial history + an 8-agent Workflow research fan-out (spark-owner, train/fine-tune, quantize/serve, evaluate, RAG, agents/harness, open-models, Inception/market).
- **Headline findings:** every researched journey independently lands on the same five open-stack white spaces — no lane manager, no end-to-end lifecycle, **no frozen-bench/eval-receipt layer** ("the layer doesn't exist"; Open LLM Leaderboard retired, no successor), no grounded retrieval+citation+refusal contract (the NVIDIA RAG Blueprint doesn't even run on Spark — amd64-only containers since Feb 2026), no agent run-receipt ledger — and they map 1:1 onto Arena/fieldkit/benches/Cortex/Control-Plane. Funded eval players (Braintrust $80M, LangSmith $125M) are cloud SaaS, structurally unable to follow the local buyer. Honest traction read: 24 GSC-indexed / 69 GA4 users/7d / $0 revenue — every demand claim is untested inference until a first paid motion exists.
- **Strategy adopted in the report (category: "the evidence layer for local AI"):** Tier-1 directions D1 Advisor Kit (productized bring-your-own-corpus governed-advisor build, $7.5k–$25k band), D2 eval-receipt standard + paid certification (`fieldkit receipt verify`), D3 Arena open-core + org governance tier; Tier-2 D4 vertical advisor packs, D5 NemoClaw evidence-bridge receipt, D6 playbook-gravity content sprint. 90-day plan starts by **finishing the queued Advisor v0.3 road first** (unchanged, see below), then offer surface → receipt spec → content sprint → decide-with-data at week 12 (pick ONE of D1/D2/D3). Consistent with and extending `_IDEAS/unsloth-compete-partner-analysis.md`.

### 2026-06-12 — Operator-armed lane session: AD-FK-1 + AD-FK-2 LIVE-CONFIRMED (earlier session)

- **AD-FK-1 confirmed against the live guard**: guarded LaneTruth launch of the promoted recipe `nemotron3-nano-4b-sft-v02-q8` **with the Cortex embedder up** (`:8001` answering before, during, and after) — no `refused:lane_resident`, lane warm in **4 s**, run anchored on warm, teardown-first left OFF. The `docker stop nim-embed-nemotron` workaround is officially retired. Wire-verified: Advisor v0.2 GGUF serving on `:8091` (resident, no drift), embedder untouched.
- **AD-FK-2 live resident smoke DONE** (the deferred half of `39fbe98`): two interactive eval turns driven through the visible cockpit chat on the resident lane — `cg-troubleshoot-0003` (cortex-grounded, **live retrieval**, grounded_contract **1.0**) + `advisor-cited-factual-qa-0003` (advisor-bench, advisor_contract **1.0**, exact citation, no thinking leak). Both persisted with **`lane_id=local:nemotron3-nano-4b-sft-v02-q8` · `cross_vertical=0`** (eval_scores rows 182/183 — vs the pre-fix `local:resident`/`1` rows directly above them in the same table), and the **AF-28 live island now renders both** in the DEFAULT view: advisor-bench group + the "Grounded (live Cortex)" tier, each `nemotron3-nano-4b-sft-v02-q8 · 100% · just now`. Interactive resident-lane grades reach the leaderboard for the first time.
- **AD-FK-3 (NEW, display-only)**: EvalPromptDrawer bench pills tag every bench `CROSS` on the resident lane — client `benchForLane(scopeLanes)` is the resolver AD-FK-2's server fix deliberately left alone, so the lane's OWN benches read as cross-vertical in the drawer while persisted truth is correct. Logged with fix directions in `_IDEAS/arena-smoke-v2-features.md` (session 6). No code changed this session — pure live confirmation.
- Box had rebooted again (~06:00); operator pre-rebuilt `/tmp/arena-venv` + brought the sidecar up before the session; this session added the CDP Chromium (`up --browser`) and the lane.
- **FIRST RECEIPT ON THE FROZEN PACK** (operator asked for a thorough grounded smoke, same session): `run_pack.py --mode both` over all 60 frozen rows → `evidence/grounded-eval/results/2026-06-12-1423-*/` — grounded **11/60 (18.3%)** vs ungrounded 9/60 (15%), lift +3.9% answer rows, **0 per-row flips vs the 2026-06-11-2219 draft receipt** (the bench is stable run-to-run on this lane). Failure classes on the 51 grounded answer rows, precisely classified: **22 GE-1 over-refusals** (gold retrieved, declined with `Citations: []` — lookup 10 · synthesis 6 · troubleshoot 4 · howto 2), **15 attempted-but-failed** (9 of which cite the RIGHT source and answer on-topic but miss the verbatim key-facts matcher — the near-miss band closest to passing; 6 also fail citation), **12 GE-3 retrieval misses** (howto 6 · troubleshoot 4 · lookup 2), 2 PASS. Ungrounded arm passes ONLY the 9 refusal rows — zero answer rows pass without retrieval. Receipt lands on `/api/grounded/receipts` (cockpit grounded tier) as the first frozen-pack run.

### 2026-06-12 — AD-FK-1 FIXED: one-lane guard exempts infra ports (earlier session)

- **AD-FK-1 FIXED**: the AE-R13 ONE-LANE guard no longer counts the Cortex embedder (`nim-embed-nemotron`, `:8001`) as a resident lane. New `launcher.infra_ports()` (default `(8001,)` — the port the runtime telemetry already names "NIM embedder"; env `FK_ARENA_INFRA_PORTS`, set EMPTY to turn the exemption off): `launch_lane` filters infra ports out of the resident set AND the `teardown_first` sweep (the kill chain never points at a docker-published port), and `teardown_lane` refuses them up front with new typed reason `infra_port`. Memory safety unchanged — `oom_envelope` still gates on real MemAvailable; if the target port itself is infra-exempt and busy, `port_busy` still refuses. 6 new tests (exemption, mixed-roster refusal message, teardown_first reaps only chat lanes, direct-teardown refusal, env-off restores strict guard); fieldkit suite **1576 green**; docs audit 17/18 PASS (same pre-existing kwarg WARN); CHANGELOG [Unreleased] `### Fixed` entry; `docs/api/arena.md` AE-31 section documents the exemption.
- **Live wiring smoked** (sidecar recycled via lifecycle script, new pid): dispatched `lane_teardown {port: 8001, confirm: true}` through the live jobs API → failed row `refused:infra_port — … never the lane kill chain`, embedder untouched (still answering). One deliberate refused-teardown job row now sits on the jobs board as the smoke receipt. **The next operator-armed lane session no longer needs the `docker stop nim-embed-nemotron` workaround** — guarded LaneTruth launch should work with the grounded-chat stack up (that session also carries the deferred AD-FK-2 live resident smoke).

### 2026-06-12 — cortex-grounded v0.1 FROZEN (operator gate passed) + AD-FK-2 fixed (earlier session)

- **`cortex-grounded-v0.1` FROZEN** (`c30faa5`): operator reviewed the 60-row draft + GE-1/GE-2/GE-3/AD-FK-2 and approved; validator re-run 0 failures, draft renamed → `evidence/grounded-eval/cortex-grounded-v0.1.jsonl`, receipt stamped **sha12 `c4da31a8897e`** — byte-identical to the 2026-06-11-2219 both-arm run receipt, so that 60-row receipt IS the frozen pack's first receipt. `benches.py` already preferred the frozen filename (rows now load `split: frozen`, verified 60/60); `run_pack.py` default + `verify_pack.py` usage + `grounded-eval-synth` SKILL.md repointed — **new rows go ONLY to `cortex-grounded-ext.jsonl`**. The pre-registration gate for Advisor v0.3 training is now satisfied on the grounded side (curveball-v0.3 freeze still pending on the curveball side).
- **AD-FK-2 FIXED** (`39fbe98`): server now resolves resident-shaped lanes (`''`/`local`/`local:resident`) to their recipe id at score time (new `_resident_lane_sid` in `server.py` — matches the resolved resident's gguf against `~/.fieldkit/arena/lane-recipes.json`; exact-path beats basename; **ambiguous → refuses to guess**, several recipes share `model-Q8_0.gguf`) and recomputes `cross_vertical` server-side for bench rows via new `benches.lane_matches_bench` (membership test — `bench_for_lane()==bench_id` was ALSO wrong for models declared by both `advisor-bench` AND `cortex-grounded`: grounded compare grades were always cross-vertical even with a concrete lane id). Applied at both persistence sites (chat score + compare eval block); free-prompt judge rows keep the client's flag; **historical rows untouched** (live db shows the pre-fix `local:resident` rows for advisor-bench/medmcqa/cortex-grounded only in the cross-vertical view — data-honest). 11 new tests, fieldkit suite **1570 green**; CHANGELOG [Unreleased] entry under `### Fixed`; docs audit 17/18 PASS (kwarg WARN pre-existing).
- **Box rebooted since last session**: all `/tmp` venvs were gone (rebuilt `/tmp/arena-venv` with `fieldkit[dev,arena]` editable — it doubles as the lifecycle script's sidecar venv); cockpit brought back via `arena_lifecycle.sh up` (new fieldkit code live, transport smoke green). **Full live resident smoke of AD-FK-2 deferred** to the next operator-armed lane session — needs the `:8091` lane and a guarded launch is blocked by AD-FK-1 while the embedder is up.

### 2026-06-11 — Grounded Eval v1 Phases 3+4 BUILT + live-smoked

- **Phase 3 (UI) shipped + CDP-smoked**: (a) **gold-vs-actual chips** — shared `GroundedSources.jsx` (ChatLane turns + CompareDuel side cards) renders the live retrieval receipt with a `✓ gold retrieved / ✗ gold missed` badge, gold-marked source chips (◆) and dashed `◇ not-retrieved` gold chips, so a retrieval miss is visible before scoring; (b) **forced-toggle indication** — `list_benches()` now carries `live_retrieval: true` (grounded only; other payloads unchanged), picking a grounded row renders the Cortex toggle checked+disabled with "· forced" + honest title and a `🧠 live retrieval` badge on the eval chip (the old "replays frozen packet" copy stays for packet benches); (c) **±Cortex ablation duel** — `CompareRequest.retrieval_a/retrieval_b` (free prompt only, eval mode wins): packet built ONCE and replayed on every flagged side, receipt rides each grounded side's `start_{a,b}`, score event carries `retrieval_ablation` and the UI shows per-side 🧠 A/B toggles + an "A grounded · B ungrounded" banner. Live smoke: grounded chat row scored 1.0 with gold chips; same-lane A-on/B-off duel — A answered with exact citation, B fabricated a "Spark 2.0 model" (grounding lift made visible).
- **Phase 4 (receipt + leaderboard tier) shipped**: `scripts/grounded_eval/run_pack.py` (deterministic; builds every packet live via `cortex_chat.build_packet`, scores through the SAME `grounded_contract` scorer via `score_eval_prediction`, `--mode both` runs the ±Cortex pair in one harness; hard-aborts if Cortex dies) → receipts under `evidence/grounded-eval/results/<run>/` (results.jsonl + summary.json: per-journey, per-component, grounding lift). New read endpoint `GET /api/grounded/receipts` + leaderboard **"Grounded (live Cortex)" receipt tier** (`GroundedReceiptPanel.jsx`: pack sha + corpus pill, per-arm pass rates, lift, journey bars, component line) + friendly grounded heads in `BenchGroupTable`/`EvalBenchLive` (`groundedBenchDisplay`) — grounded numbers never share a rollup with frozen-packet groups. Arena suite **512 green** (+5); `_webui` rebaked, sidecar restarted, CDP-verified; demo rebuilt/deployed/links-verified.
- **FIRST FULL RECEIPT (draft pack, 4B-SFT-v0.2 lane, both arms)**: grounded **6/20 (30%)** vs ungrounded 4/20 (20%) → **lift +10% overall / +12.5% answer rows**. Per journey (grounded): refusal 4/4 · troubleshoot 2/4 · lookup 0/5 · howto 0/4 · synthesis 0/3. Per component: retrieval_hit **14/16** ✓ (retrieval is fine) · citation 3/16 · key_facts 2/16. Decomposition: **9 of 14 failing answer rows are GE-1-class over-refusals** ("private or live operator state" on operator-journey phrasing) — the known safe-direction drift now QUANTIFIED per journey; 5 are wrong/partial answers (incl. GE-2 excerpt-window class). Runner results match the live cockpit turns row-for-row (cg-troubleshoot-0003 passes both surfaces). This is the v0.3 over-refusal lever case, measured.
- **AD-FK-2 (NEW dogfood finding)**: interactive chat/compare eval grades on the resident lane persist with `lane_id='local:resident'` and `cross_vertical=1` (client `benchForLane('local:resident')` can't resolve the resident's model → bench), so the AF-28 live island (default `include_cross_vertical=false`) never shows interactive advisor-bench/cortex-grounded grades — pre-existing (advisor-bench rows have it too), surfaced while wiring the grounded tier. Canonical receipts are unaffected (the offline runner is the evidence path).
- **Pack filled to ALL quotas (operator "continue" same day)**: `/grounded-eval-synth` batch loop authored 40 rows (lookup +10 · howto +8 · synthesis +9 · troubleshoot +8 · refusal +5) → **60 rows, validator 0 failures, quotas exact**; every `key_facts` value grep-verified verbatim in the gold body before writing, frozen-curveball collisions designed around (89 bench questions reviewed), SFT tags applied (51/60 in-SFT at source level; the 9 out-of-SFT rows are exactly the refusal rows). **Authoring deviation flagged**: all 9 refusal rows are out-of-corpus/fabricated-premise — the strict `grounded_contract` refusal gate requires empty citations, so spec §4's *superseded* sub-kind (where cite-current is correct) would punish correct behavior; superseded probes should join the ext set once the scorer learns refuse-or-cite-current.
- **60-row both-arm receipt (4B-SFT-v0.2)**: grounded **11/60 (18.3%)** vs ungrounded 9/60 (15%), lift +3.9% answer rows. Per journey (grounded): refusal **9/9** · troubleshoot 2/12 · lookup 0/15 · howto 0/12 · synthesis 0/12. Decomposition: **22/51 answer rows over-refuse (GE-1 at scale)** + **NEW GE-3 — 12/51 retrieval misses at k=3, concentrated in howto/troubleshoot** (command-shaped/symptom-shaped queries don't share vocabulary with article prose — exactly the J2/J4 failure mode; retrieval_hit 39/51). Receipt: `evidence/grounded-eval/results/2026-06-11-2219-*/`. The pack is doing its job: the v0.3 levers (over-refusal class + retrieval k/query-expansion for operator phrasings) are now quantified per journey.
- Freeze stays operator-gated (spec §9): review the 60-row draft + GE-1/GE-2/GE-3 + AD-FK-2, then `verify_pack.py --freeze` renames draft → `cortex-grounded-v0.1.jsonl` with sha12 receipt.

### 2026-06-11 — Grounded Eval v1: spec + generation pipeline + 20-row pilot pack (earlier session)

- **New spec `_SPECS/grounded-eval-v1.md` (ACTIVE, operator-decided)** — the missing eval layer between the frozen packet benches (model-only) and `rag_eval_runs` (retrieval-only): **live** query → pgvector → `cortex_chat.build_packet()` → answer → citations scored as one unit, organized around 5 operator-journey archetypes (lookup / howto / synthesis / troubleshoot / refusal; multi-turn research = v1.1). Decisions locked with operator: hybrid generation (deterministic miner + session-model authoring + Arena re-validation), deterministic `grounded_contract` gate (retrieval_hit + citation integrity + key_facts + hygiene) w/ judge advisory only, frozen `cortex-grounded-v0.1` + living ext set, same-lane **±Cortex ablation duel** in compare, `eval_scores` reuse — **no arena.db schema change**. Pack contract is corpus-generic (customer-corpus story: bring docs → ingest → eval pack → grounded leaderboard).
- **Phase 1 BUILT**: `scripts/grounded_eval/mine_candidates.py` (deterministic journey-tagged seeds; 1,066 candidates over the 182-source manifest → /tmp scratch) + `scripts/grounded_eval/verify_pack.py` (mechanical gates: schema, gold-ids-in-manifest, key-facts verbatim-verifiable in gold bodies, title-echo 4-gram, near-dup Jaccard, evaluator-hint phrases, `--apply-sft-tags`, `--freeze` sha12+receipt) + new skill `.claude/skills/grounded-eval-synth/SKILL.md` (claude-corpus-synth pattern, journey quotas, batch loop).
- **Pilot pack authored & validated**: `evidence/grounded-eval/cortex-grounded-v0.1.draft.jsonl` — 20 rows (lookup 5 · howto 4 · synthesis 3 · troubleshoot 4 · refusal 4), validator 0 failures, every fact verified verbatim against gold bodies, frozen-curveball collision-checked (all 89 existing bench questions reviewed; new rows use distinct angles/sources). NOTE: `--apply-sft-tags` marked all 16 answer rows in-SFT at *source* level (the SFT cited-factual family covered the whole manifest) — source-overlap won't discriminate OOD on this corpus; question-novelty is the OOD signal.
- **Phase 2 BUILT + LIVE-SMOKED same session (operator "continue")**: `cortex-grounded` BenchSpec (`fmt="grounded"`, **`live_retrieval=True`** — the first bench with NO packet receipts; eval mode builds the packet through the live Cortex stack at send time and forces retrieval, Cortex down = hard SSE error) + deterministic **`grounded_contract`** scorer (`score_eval_prediction(..., retrieval=receipt)`: retrieval-hit + citation-integrity incl. cited⊆retrieved + key-facts contains/regex/numeric + hygiene; lost receipt → `retrieval_hit` *unknown*, flagged in `why`) + in-memory turn→receipt map feeding `/api/chat/score` + compare eval rows build the packet ONCE and replay it on both sides (fair A/B; receipt on `start_a` w/ gold-vs-actual `retrieval_hit`). 13 new tests, arena suite **507 green**; sidecar restarted via lifecycle script; bench live in the cockpit drawer (pill + journey family filter worked unmodified).
- **4 live eval turns driven through the visible cockpit UI (CDP)** — the loop proven both directions: `cg-troubleshoot-0003` (vLLM tool-call flags) **PASS 1.0** (retrieval ✓ · citation ✓ · facts 2/2 ✓; exact flags + `hermes` parser + exact citation). Three 0.0 rows decompose diagnostically — **two real findings, neither a wiring bug**:
  - **GE-1 — Advisor over-refusal on operator-journey phrasing**: symptom-led/first-person rows (`cg-troubleshoot-0001`, `cg-lookup-0005`) retrieved the right gold source (retrieval ✓) but refused as "private operator or live runtime state". The known safe-direction drift, now *measurable by journey class* — feeds the queued v0.3 over-refusal lever (freeze curveball-v0.3 + grounded v0.1 first).
  - **GE-2 — packet excerpt window drops measurement facts**: `cg-lookup-0003` (cyber mini-eval) retrieved the gold article at rank 1 (dist 0.57) but the 900-char query-centered excerpt picked the *methods* sentence (max term overlap) and dropped the results table → the model's refusal is contract-correct; the failing component is the packet contract. Invisible to `rag_eval` (recall 0.977 ✓) AND the curveballs (their gold facts are prose theses near query-matching sentences) — exactly the layer this bench exists to see. Fix directions: weight measurement-shaped sentences in excerpt selection, or use the retrieved chunk text instead of re-excerpting the article body, or widen k/chars — ANY change is an OA-NV-8-style packet swap → re-gate before adopting. NOTE: `_strip_markup` removes fenced code blocks, so `howto` rows whose facts live only in fenced commands can never pass — pick prose/inline-code facts when authoring.
- **OPERATOR GATE next**: review the draft pack (+ spec + GE-1/GE-2). Then **Phase 3/4**: UI polish (gold-vs-actual chip render from the receipt fields now shipped, forced-toggle indication, free-prompt per-side ±Cortex ablation toggles), offline runner `run_pack.py` (canonical receipts + `--no-retrieval` grounding-lift pair), grounded leaderboard display tier; quotas to 60 rows via `/grounded-eval-synth` before freeze. Spec basename is proof-control → `EXCLUDED_SPEC_BASENAMES` at next corpus build; `evidence/grounded-eval/` must never be ingested into any retrieval corpus.

### 2026-06-11 — Orphan-screenshot audit executed: 23 orphans cleared, 5 reused, kepler positioning + 01-cockpit retake (this session)

- **Audit swept all image surfaces** (products canonical+public, `public/screenshots/`, `arena-app/public/`, article screenshot/asset dirs, evidence dirs minus vendored repo-snapshots/run post-states) with a basename cross-reference against every page/source file: **23 orphans found, now 0 on every surface**.
- **5 placed — new "What the board grew into" section on `products/arena-control-plane/`**: the e2e-smoke-era 3120×2000 shots of untoured surfaces (`04-build-spine`, `08-settings-guardrails`, `09-jobs-guardrails-rl`, `10-reward-gauge`, `11-standup`) now tour the post-launch growth (3-kind dispatch row, vertical build spine, eval-is-reward gauge, cloud-eval guardrails, stage-only standup) — each ≥75-char alt + italic caption per the audit bar; the "Get it" roadmap paragraph updated to point at the landed section. `verify_product_article.sh` PASS.
- **18 deleted** (git rm, dual copies in lockstep): 3 superseded control-plane duplicates (`05-chat`/`06-compare-duel`/`07-leaderboard` — fresher equivalents live in the orionfold-arena tour; 07 showed the 2026-05-28 pre-Advisor leaderboard), cortex `08-catalog-page.png` (a screenshot of the website's own artifact page — circular), `arena-app/public/dgx-rig.jpg` (zero references), and 13 stale `public/screenshots/` shots from old ainative product releases no docs page cites.
- **Kepler positioning gap closed**: canonical `kepler-gguf.yaml` gained `positioning:` (headline/problem/use_cases/audience drawn from the published gate-before-the-gpu numbers — 88.6% held-out, 84.1% curveball, gate-cascade story); byte-copied to arena-app (drift guard 22/22), `_webui` rebaked, CDP-verified live: catalog card shows the headline instead of the bare slug, detail page renders it.
- **`01-cockpit.png` retaken** against the live cockpit (3840×1848 @2x via `browser.newContext({deviceScaleFactor:2})` — note: a raw CDP `Emulation.setDeviceMetricsOverride` on an existing page does NOT survive into `page.screenshot`, first attempt came out 1x): now shows post-Path-A counts (artifacts 22 · articles 67 · benches 4 · runs scored 78) with the Advisor v0.2 lane resident and run-context armed. Both dual copies synced.
- Verify chain green: 538 pages, both render verifiers, catalog sync 22/22, demo rebuilt/deployed (33 product screenshots in sync — count dropped from 37 by the 4 product-side deletions), links verified.

### 2026-06-11 — Arena hardening batch: 3 of 4 queued actions + mirror-default fix shipped

- **Item 1 — catalog drift guard SHIPPED**: new `scripts/verify_arena_catalog_sync.mjs` (every `src/content/artifacts/*.yaml` must byte-match its `arena-app/` copy; orphans + missing both fail; exit = drift count) — runs beside the render verifiers, added to the CLAUDE.md verify chain; currently 22/22 in sync. `hf-publisher` SKILL.md Step 7 post-push tail now leads with "sync manifest → arena-app + rebake + mirror + run the verifier".
- **Item 2 — stale-HTML trap killed at the source**: `fieldkit.arena.server` `_webui` mount now serves HTML with `Cache-Control: no-cache` (new `_webui_static_files()` StaticFiles subclass; ETag keeps revalidation a cheap 304; hashed `/assets/*` untouched). Wire-verified live post-restart: HTML carries `no-cache`+etag, JS asset carries no cache-control. The CDP `Network.setCacheDisabled` workaround is no longer needed for post-bake verification.
- **Item 3 — frontier head-tag honest**: leaderboard SSR now applies the island's quality filter (`ppl != null || evalScore != null`) to `frontierPoints` and counts models from surviving groups. CDP-verified live: head tag reads **6 models · 25 builds** (was 7·30). NOTE: the queue entry projected 26 — miscount; `spark-hermes-profile` has **5** quality-less tok/s lanes, not 4 (verified by script over the manifests; nothing else dropped).
- **Mirror-default hardened** (the recurring `src/data/arena-mirror/` leftover): `import_artifacts(write_mirror=)` defaults **False**; CLI flag is now opt-in `--mirror` (`--no-mirror` kept as the off form). New test pins the default; `fieldkit/docs/api/arena.md` kwarg table updated; CHANGELOG [Unreleased] carries both behavior changes under `### Changed`.
- Arena suite **495 passed** (+2 new tests); `_webui` rebaked + sidecar restarted via lifecycle script; demo rebuilt/deployed/verified (691 links ok, head tag honest in the demo bundle too).
- **Item 4 — Kepler tok/s DONE (operator-armed lane swap, same session)**: operator tore down the `:8091` Advisor lane in LaneTruth; measured all 4 variants with the p65 precedent (`llama-bench -ngl 99 -p 64 -n 64 -r 2`, tg average): **Q4_K_M 33.06 · Q5_K_M 28.06 · Q6_K 24.6 · Q8_0 21.07 tok/s** (monotonic, no Q8 anomaly — consistent with kepler being a chat-tune). Raw receipts at `/home/nvidia/data/quants/Kepler/tok-bench-2026-06-11/`. Manifest + arena-app sync (drift guard 22/22) + rebake; CDP-verified frontier chart now **7 models · 29 builds** with kepler in the legend (verified with a PLAIN reload — the no-cache fix works). Advisor lane relaunched **through the guarded LaneTruth UI** (recipe `nemotron3-nano-4b-sft-v02-q8`, anchor-on-warm, ACTIVE in 6 s), wire-verified serving the v0.2 GGUF.
- **Frontier chart flagship treatment (operator-directed, same session)**: `advisor-gguf` now renders as a violet diamond (`--violet`, drawn above the Pareto line via a uPlot draw hook) with a matching legend swatch; flagship excluded from the color cycle + 6th cycle color (`--red`) added → finance-chat/advisor collision gone and all 6 non-flagship models collision-free. Bonus fix: sweet-spot rings had a background-filled interior that occluded the dot they highlight — now transparent. CDP-verified + demo redeployed (`f8c5aa6`).
- **Leaderboard advisor display layer (operator-directed, same session)**: the `advisor_contract` group now renders FIRST in the bench section (above even the AF-28 live island) with a friendly head ("Orionfold Advisor — refusal-floor contract", metric "frozen OOD curveballs") and translated lane names + pills (◆ flagship / promoted lane / superseded / baseline / teacher + per-row "frozen OOD · curveball v0.x" gate pills) — raw receipt lane ids stay visible as the mono sub-line (report=reality). Mechanics: new `advisorBenchDisplay`/`advisorLaneDisplay` in `leaderboard-format.mjs` (matcher also covers a future live `cockpit:advisor_contract` tier), group table extracted to `BenchGroupTable.astro` so the flagship renders above the island without markup duplication, pill + flagship-accent CSS in ArenaAppLayout.
- **Marketing screenshots refreshed for the changed surfaces (operator-directed, same session)**: retook `products/orionfold-arena/screenshots/{02-leaderboard,03-efficiency-frontier,04-models}.png` against the live cockpit (fresh emulated tab, 1920×924 @2x, no scrollbar — emulation renders none; 02 reframed to the advisor-first bench section, sticky-chrome offset measured in-page at 217 px). Public copies synced (37 in sync per demo verifier); the arena landing (`src/pages/arena/index.astro`) picks 03 up by filename. `product.md` alt + caption + body prose updated for 02/03 to describe the flagship display layer (pills, friendly names, raw ids kept) and the violet diamond. **Screenshot-text audit PASS**: all 21 product shots (14 arena + 7 advisor) carry ≥75-char alts + italic captions. Shots 01/05–11 (cockpit home, detail, chat, compare, palette, lab, telemetry) intentionally NOT retaken — their surfaces didn't change this session, though 01-cockpit shows pre-Path-A counts (see queued audit below).
- **AD-FK-1 (NEW dogfood finding, from the relaunch)**: the AE-R13 one-lane guard counts the Cortex **embedder** (`nim-embed-nemotron`, `:8001`, OpenAICompat) as a resident lane — `refused:lane_resident` on every chat-lane launch while the grounded-chat stack is up, and `teardown_first` is NOT a safe out (it tears down **every** discovered lane; pointing `teardown_lane()` at a docker-published port is untested). Past launches only worked because the embedder happened to be down. Workaround this session: operator `docker stop nim-embed-nemotron` → guarded launch → `docker start` (back at 200). Real fix queued in Open Items.

### 2026-06-11 — Advisor on the leaderboard (Path A executed) + Advisor-bench manifest shipped

- **Path A runbook executed end-to-end** (was QUEUED operator-approved): new deterministic rollup `scripts/orionfold_advisor/leaderboard_rollup.py` (transform-only; reads the 6 curveball results.jsonl receipts, refusal basis = strict-passed among refuse rows to match the published `curveball2-compare` "3/9"; bench shas self-verified against the frozen pins `122bcd619e9d`/`4b6cac85e41f`; tok/s read from the canonical manifest, not hardcoded) → `articles/the-refusal-floor-is-trainable/evidence/advisor_contract_results.json` (6 lane×gate rows, gates never conflated: 4b-sft-v0.2 0.90/0.857, 4b-sft-v0.1 0.70, 4b-init 0.55, 30b-prompted 0.575/0.381).
- **Import chain run**: `fieldkit arena import` (idempotent, re-run verified) → `rebuild-leaderboard` (bench rows 12→18) → `mirror --repo-root arena-app`. **CDP-verified live** (cache disabled): bench group `the-refusal-floor-is-trainable:advisor_contract` renders 6 lanes, trained-4B ranked above the 30B teacher (85.7% vs 38.1%), 42 tok/s only on the promoted lane.
- **`advisor-bench-v0.1.yaml` manifest shipped** (closes the launch follow-up): full bench fields mirrored from the live HF card (5 shapes incl. corpus-manifest 182, sha12 pins, results = promoted lane under mode `retrieval`, real sample rows, positioning, sibling ↔ `advisor-gguf` reciprocal links). Copied byte-identical into `arena-app/src/content/artifacts/` (dual-copy discipline) — cockpit models page benches 3, detail page renders; main site 538 pages (+1) + both render verifiers green; demo rebaked/deployed/links verified.
- **Root cause of the recurring `src/data/arena-mirror/` leftover found**: `fieldkit arena import` writes the repo-root mirror BY DEFAULT (`importer.DEFAULT_MIRROR_LEADERBOARD_PATH`); nothing on the main site reads it. Deleted again; use `fieldkit arena import --no-mirror`, or fold "stop writing the root mirror by default" into the hardening batch.
- Path B (live tier `cockpit:advisor_contract` via eval runs against `:8091`) stays open as the narrated-session story.

### 2026-06-11 — Arena artifact-catalog drift cleared: Advisor/Kepler/Cortex on every cockpit surface

- **Root cause found via operator question** ("why is the advisor model not on the cost-quality chart?"): `arena-app/src/content/artifacts/` is a SEPARATE copy of the canonical `src/content/artifacts/` and had been frozen at the 2026-05-29 consolidation commit — the leaderboard frontier chart, models catalog, model detail pages, and command palette all build from it. Same two-copies trap as the M9 module-enum bake break.
- **Fix shipped**: all 17 canonical manifests synced into arena-app (adds `advisor-gguf`, `kepler-gguf`, `orionfold-cortex`; refreshes the 8 drifted with positioning/known_drift/lane_summary/siblings); arena-app `content.config.ts` artifacts schema ported up to canonical (bench fields `shapes/modes/results/samples/sources/how_to_load/citation` + `lane_summary` + `siblings`); `model-prompts.mjs` gained advisor + kepler example prompts and `advisor`/`astro` vertical badges. `evals.mjs` BENCH_MODELS verified — already an exact mirror of `fieldkit.arena.benches` (no drift).
- **Canonical `advisor-gguf.yaml` enriched** (was bare): `published_at` + positioning drawn from the published launch copy (30B-prompted 8/21 vs 4B-trained 18/21, refusals 9/9, 42 tok/s) **+ `article: the-refusal-floor-is-trainable` — NOTE this overrides the launch session's deliberate "no `article:`, product page is the methods surface" call** so catalog cards get a real link; revert is a one-line delete if the operator wants the omission back.
- **Leaderboard mirror refreshed** (`fieldkit arena mirror --repo-root arena-app`): was 2026-05-28 (pre-Advisor/Kepler); now 2026-06-11 — live rows 2→21, `kepler::Q8_0` ranks. Advisor serving-lane evidence stays in its offline preflight receipts (no cockpit leaderboard rows) — data-honest.
- **Verified live over CDP** (per pinned discipline): frontier chart legend now lists `advisor-gguf` (7 models · 30 builds; advisor at 42 tok/s × 0.86 eval, sweet-spot ring), models page shows advisor/kepler/cortex cards, detail page renders the new positioning. Main build 537 pages + both render verifiers green; demo rebaked + deployed + links verified. ⚠ Gotcha logged: the sidecar serves `_webui` with NO `Cache-Control`, so the parked Chromium heuristically served the STALE pre-bake leaderboard HTML after restart — verify post-bake pages with `Network.setCacheDisabled` (or a hard reload), not a plain `goto`.
- ~~Open follow-up: missing `kind: bench` manifest for `Orionfold/Advisor-bench`~~ — CLOSED 2026-06-11 (Path A session shipped `advisor-bench-v0.1.yaml`).

### 2026-06-10 — Cortex-grounded Advisor chat wired into Arena

- **Free-prompt chat can now retrieve live from the Advisor corpus pack** — closes the launch-era gap where the full retrieval loop existed only through eval-row replays. New `fieldkit.arena.cortex_chat` (packet builder: pgvector `advisor_corpus_v01` + NIM embedder via `MemoryIndex`, chunk→top-3-unique-source dedup, 900-char query-centered excerpts, verbatim `/no_think` system contract + `enable_thinking:false` rider — byte-compatible with `preflight.py` production packets). `ChatRequest.retrieval: bool`; eval mode wins; Cortex down = hard SSE `error`, never a silent ungrounded turn. `start` event carries a `retrieval` receipt (table + manifest sha `6b1e832d099c` + source cards w/ cosine dist). `/api/compare/options` flags `advisor: true` lanes + carries `retrieval_source` (active pack: table/sha/source-count); ChatLane defaults a "🧠 Cortex retrieval" toggle ON for advisor lanes, labels it with the active pack (`⛁ advisor_corpus_v01 · 182 src · 6b1e832d099c`), and renders grounded source chips per turn. 13 new tests (10 packet-builder + 3 chat-stream), arena suite 490 green; `_webui` rebaked (also clears the pending FieldkitModules copy rebake) + demo rebaked/deployed/verified.
- **Step-0 stack verification receipts**: `advisor_corpus_v01` intact (646 chunks / 183 slugs); `nim-embed-nemotron` restarted (was exited 25 h); live recall rescored `--skip-ingest`: source_recall@5 **0.977, gate pass** — identical to the 2026-06-09 receipt. Fixed `score_recall_live.py` signature drift (`_load_rows` gained pool/heldout args after the live script was written).
- **Live smoke (API + CDP browser)**: Hermes-brain question → grounded answer, exact `Citations: [article_the_hermes_harness_on_spark]`, 45 tok/s; urgency-pretext `.env.local` probe → clean refusal `Citations: []`. UI smoke: toggle ON by default on the advisor resident lane, chips render. Noted: fieldkit-v0.5 question hit the known safe-direction over-refusal drift (cited changelog sources but refused) — model behavior, not wiring.

### 2026-06-10 — /fieldkit/ landing un-froze (v0.13.0 → v0.31.0) + module-copy rebalance

- **Live `/fieldkit/` page was 18 releases stale**: `index.astro` read the retired two-repo-era mirror `fieldkit/_version.py` (frozen at 0.13.0 since the cutover) while releases bumped only the canonical `fieldkit/src/fieldkit/_version.py`. Page now reads the canonical file directly; mirror deleted; `sync-field-notes` mirror logic retired in scripts + SKILL.md (do not resurrect).
- **Module-card copy rebalanced** (operator-directed): all 18 `fieldkit/docs/api/*.md` `summary:` lines rewritten reader-facing at 96–222 chars (`training` was ~780; `arena`/`harness` read like internal ship logs with M*/H*/Bet codenames — detail stays in doc bodies). Taglines trimmed (`arena` 71→38 chars) and the arena-app `FieldkitModules.astro` copy synced 13→18 modules. NOTE: arena-app `_webui` rebake pending next cockpit session (copy-only change).
- **`audit_landing.py` upgraded 4→6 checks**: new `landing_version_source` (canonical version path + no mirror) and `doc_summary_balance` (60–260 chars, no internal codenames); `module_taglines` now audits BOTH component copies + a ≤56-char cap. 6/6 PASS; build 537 pages green + both render verifiers green.

### 2026-06-10 — SVG legacy debt cleared + orionfold.com brand realignment

- **`verify_svg.sh` legacy debt CLEARED** (was Open Items → Cleanup): 105 violations → 0 across the 12 old signature components. role/aria on every legacy `<svg>`, stroke-widths snapped to the {0.5,1,1.5,2} scale, referenced atmospheric gradients added where missing; orphaned `FeatureVelocity.astro` + `FieldkitConstellation.astro` deleted (unreferenced since the pre-cutover redesign). Commit `ad6a092`.
- **Marketing site realigned to the orionfold.com parent brand** (operator-directed): new spec `_SPECS/orionfold-brand-alignment-v1.md`. Tokens extracted from live orionfold.com → `src/styles/global.css` `@theme`: cool 260-hue neutrals, indigo primary `oklch(55% .18 260)`, orbit-gold `oklch(70% .14 82)`, navy→blue→gold hero ramp, SVG accents re-tuned (blue=brand indigo, orange=gold). Ported `.of-surface`/`.of-pressable` card idiom and applied to the home-page cards; hero gained the gold orb; field-notes hardcoded Airtable dark1 hexes → semantic vars. **`design-system-v1.md` (Airtable) re-scoped to operator panes (`arena-app/`) only** — index updated. Build 537 pages green; artifact/field-notes/SVG verifiers green; browser-smoked home/field-notes/article/artifacts at 1440px.

### 2026-06-10 — Orionfold Advisor PUBLIC LAUNCH

- **HF artifacts live**: [`Orionfold/Advisor-GGUF`](https://huggingface.co/Orionfold/Advisor-GGUF) (4B-SFT-v0.2 Q8_0 4.0 GB as `model-Q8_0.gguf`; `license: other` + nvidia-nemotron-open-model-license link; OOD-first card — headline eval is curveball-v0.2 85.7%, Spark-tested 42 tok/s measured live, 4 bounded known-drift entries, Methods → product page + evidence dir; pushed via `hf_push_resilient.py`, ~16 min) and [`Orionfold/Advisor-bench`](https://huggingface.co/datasets/Orionfold/Advisor-bench) (pool 75 / frozen heldout 28 / curveball-v0.1 40 / curveball-v0.2 21 / corpus-manifest 182, all sha12-pinned in the card; results table for 3 lanes). verify_stage 7/8 — the sole FAIL is check 4 (Methods must be a field-notes URL; the Advisor's methods doc is deliberately the product page), operator-accepted at the push gate. Artifact manifest `src/content/artifacts/advisor-gguf.yaml` written (no `article:` — product page is the methods surface).
- **Website launch shipped**: `products/orionfold-advisor/` (positioning per spec §2/§3; mined build block — 2 days/29.9 h, 10 sessions, 871 turns, 118.9M tokens 97.4% cache, 4,307 LOC `scripts/orionfold_advisor/`, 54 advisor tests, all Claude Fable 5; 7-feature tour: 5 fresh 2x clips + 2 authentic dogfood captures) + `AdvisorTeaser` on the home page (after CortexTeaser; reads the products collection) + fieldkit arena module blurb refresh. `verify_product_article.sh` PASS, build 537 pages green, both render verifiers green.
- **Article series started**: `articles/the-refusal-floor-is-trainable/` PUBLISHED (flagship: frozen-curveball discipline; 30B prompt-contract 8/21 + 3 private-state fabrications vs 4B trained 18/21 + refusals 9/9 on identical packets; v0.1 refusal regression 14/15→9/15 caught by frozen OOD bench, v0.2 corpus-design fix; new `RefusalFloor` signature SVG + dual-path inline fn-diagram + 8 explainers; transcript.md carries receipt provenance). Two `status: upcoming` placeholders queued: `gates-before-the-advisor` (recall gates, raw-base preflights, spec-contamination catch) and `governed-routing-with-receipts` (observables-only router, bakeoff economics, §14 receipt). NOTE: `verify_svg.sh` has ~105 pre-existing violations in OLD signature components (MarketGrowth, SystemArchitecture, etc.) — the new article's two SVGs pass; the legacy components are untouched tech debt.
- **Arena surfaces refreshed for the Advisor era**: `products/orionfold-arena/` gained 3 toured features (guarded lane lifecycle / measured benches in the eval drawer / vertical-proof Cortex cards; shots 12–14, feature list now matches the 14 count); **arena demo re-recorded** against the live cockpit — `fieldkit.arena.fixtures._STUB_ENDPOINTS` extended with the 4 `api/advisor/*` reads and `_FORBIDDEN_STUB_KEYS` gained `endpoint` (probed lane URLs were leaking 127.0.0.1:8091 into stubs; now 0 local-URL occurrences), 29 fixture tests green, fieldkit CHANGELOG [Unreleased] entry added; `ARENA_DEMO=1` rebuild + `deploy_arena_demo.mjs` → `public/arena/demo/` (37 product screenshots in sync, links verified) + headless smoke: **all 4 advisor cards render in the sidecar-less demo cortex page**.
- Advisor proof state (compact; receipt-level history in git log of this section): 4B-SFT-v0.2 PROMOTED serving lane (publish receipt 9/9 gates, `advisor-publish-receipt-v0.1.json`); 30B = teacher/comparison on disk; benches frozen (heldout 28 `3220b8e799cd`-combined, curveball-v0.1 `122bcd619e9d`, curveball-v0.2 `4b6cac85e41f`, corpus manifest 182 `6b1e832d099c`); AD-AE-11..17 all closed/fixed except the open §10 controls + §12 live ledger (Open Items).

### 2026-06-09 — Codex coexistence baseline adopted

- Codex reads `HANDOFF.md` at session start and updates it at session end when live state, public artifacts, release posture, or the status beacon changes. Coexistence layer at `ddb6626`, scoped to `AGENTS.md`, `CODEX-CC.md`, `.codex/`, `.agents/skills/`. Do not edit `.claude/` for Codex behavior unless explicitly requested.

### Published Baseline To Preserve

- **Advisor (NEW)**: <https://huggingface.co/Orionfold/Advisor-GGUF> (Q8_0 only; recommended) + <https://huggingface.co/datasets/Orionfold/Advisor-bench>; launch page `products/orionfold-advisor/`; flagship article `articles/the-refusal-floor-is-trainable/`. Headline held-out numbers: frozen curveball-v0.2 18/21 scored==strict, refusals 9/9, 0 private-state risk (vs 30B prompt-only 8/21, 3/9, 3 fabrications); heldout 28/28 hinted+hint-free; 42 tok/s / ~12 GB / warm 2 s on the Spark. Known drift: Route:-prefix soft class (2/5), one OOD over-refusal (safe), in-distribution heldout caveat, packet-contract-shaped behavior.
- Kepler: <https://huggingface.co/Orionfold/Kepler-GGUF> + <https://huggingface.co/datasets/Orionfold/Kepler-bench>; article `articles/the-gate-before-the-gpu/`; Q8_0 recommended; held-out: Q8 88.6% / curveball 84.1% local/$0. Drift: `hohmann_transfer`, `altitude_from_period`.

## Live Runtime

Last recorded runtime baseline: Field Edition recall-re-run session, 2026-06-13 (read-only dogfood; box up ~9 h, no reboot this session).

- **Cortex stack UP:** `pgvector` (:5432) + `nim-embed-nemotron` (:8001) containers up 9 h; `advisor_corpus_v01` intact (646 chunks / 183 slugs). **Cockpit `:7866` DOWN · Advisor lane `:8091` DOWN** (generation lane + sidecar not started this session). `/tmp/arena-venv` was gone (only `/tmp/fk` survives — base fieldkit 0.31.0 editable + the memory deps; it reaches the live Cortex stack fine). 30B + `fk-nim-8000` + `nemo-train` stopped (restartable). Recipes 8 in `~/.fieldkit/arena/lane-recipes.json`. **To resume operator-armed lane work:** rebuild `/tmp/arena-venv` (`fieldkit[dev,arena]` editable) + `arena_lifecycle.sh up --browser`, then guarded LaneTruth launch of recipe `nemotron3-nano-4b-sft-v02-q8`.
- This session's lane-adjacent actions (logged per pipeline discipline): 1× `up --browser` (added CDP Chromium beside the running sidecar); 1× guarded `lane_launch` through the LaneTruth UI (the AD-FK-1 live rep); 2× interactive eval-mode chat turns on the resident lane (the AD-FK-2 smoke — one grounded with live retrieval, one advisor packet replay); NO teardowns.
- Box rebooted twice since the AD-FK-2 fix session; `/tmp/arena-venv` (fieldkit 0.31.0 editable, `[dev,arena]`) was rebuilt by the operator pre-session and survives until next reboot. (HF/publish work will need huggingface_hub + `hf` CLI re-added; HF pushes need `HF_HOME=/home/nvidia/data/.hf-cache HF_HUB_DISABLE_XET=1`.)
- `.env.local` still carries `FK_ARENA_BUILD_DIR`/`FK_ARENA_CORPUS_DIR`/`FK_ARENA_SFT_DIR`/`FK_ARENA_REWARD_DIR` pointed at the Advisor proof.
- Tear down when done: `arena_lifecycle.sh down --browser` (and the `:8091` lane via LaneTruth two-step confirm if the box is needed for something bigger).

## Build / Verify

```bash
node node_modules/astro/astro.js build          # 538 pages
node scripts/verify_artifact_rendering.mjs
node scripts/verify_field_notes_rendering.mjs
node scripts/verify_arena_catalog_sync.mjs   # artifact yamls byte-match arena-app copies
# arena demo rebake (when arena-app or fixtures change):
ARENA_DEMO=1 node arena-app/node_modules/astro/astro.js build --root arena-app
node scripts/deploy_arena_demo.mjs && node scripts/verify_arena_demo_links.mjs
```

- `npm run build` remains broken on this checkout; use direct Astro.
- `build:og` is CI-only. Privacy gate before commits: `.codex/hooks/secret_scan.sh --cached`.
- arena-app has its OWN articles schema copy — summaries >300 chars pass the main build but fail the `ARENA_DEMO=1` build (bit this session; both placeholder summaries trimmed).

## Open Items

> 🎯 **NEXT SESSION (operator directive 2026-06-13): do ALL Arena Field Edition tasks first.** The Advisor v0.3 packet-contract work is **PAUSED and queued to the back** (see "Advisor / Arena" below — the diagnosis is captured so it isn't re-derived). Start in the Arena Field Edition section directly below.

### Arena Field Edition (M1) — ← START HERE next session: **make `up` a genuinely unattended `curl | sh` (fix the 3 first-boot orchestration findings)**

> ✅ **The first-customer happy-path sim is DONE (2026-06-15 — see the two top Current State entries).** It drove the REAL `get-orionfold.sh` bootstrap end-to-end on a pristine box → all-green §8 receipt in ~10–12 min (well under AC-1's 60), found+fixed the cortex refusal-scorer false-FAIL (shipped in v0.32.3), and surfaced **4 orchestration findings**. AD-FK-δ is closed by v0.32.3. **The remaining 3 (α/β/γ) are why `up` can't yet complete unattended** — a real `curl | sh` customer hits all three. None blocks selling (founding-25 is live), but they're the gap between "works when Claude babysits it" and "works for a customer cold." **This is the next-session task.**
>
> 🚀 **NEXT SESSION — fix AD-FK-α/β/γ in `fieldkit.field_edition` so the bootstrap runs end-to-end with zero manual steps, then re-run the pristine-box sim to prove it.** Each is a `field_edition` code change → land in `[Unreleased]` → a fieldkit-curator release ships them (same flow as v0.32.3). Build + test discipline: each needs field_edition tests + the offline suite green (`/tmp/fk` rebuilt with `pip install -e fieldkit[dev,arena,harness]` — reboot-ephemeral).
>
> 1. **AD-FK-α — wire the NGC key (High).** `get-orionfold.sh` never sources `~/.nim/secrets.env` and `compose.render_env` omits `NGC_API_KEY`, yet the compose file has `${NGC_API_KEY:?…}` on `of-embedder` → **`docker compose up` AND `down`/`repair` both fail** (every compose subcommand interpolates the required var; confirmed live — `field-edition down` errored until the key was exported). `compose.py:308` comment falsely claims the bootstrap sources it. **Fix options:** (a) `get-orionfold.sh` sources `~/.nim/secrets.env` before `up`, and/or (b) `up`/`down`/`repair`'s `_run` env (`LiveExecutor`) reads `~/.nim/secrets.env` and injects `NGC_API_KEY` into the `docker compose` environment. Either way make the compose.py:308 comment true. (v1.1 `--open-embedder` removes the dep entirely — but v1 default is NIM, so this must work.)
> 2. **AD-FK-β — add a corpus-ingest phase to `up` (High).** FE pgvector boots **empty** → the §8 cortex gate can't pass on a fresh box. `up`'s phase list (`matrix→bundle→pull→stack→sidecar→resident`) has no ingest step, yet the cortex gate's own fix-text (`verify.py:534`) claims `up` "brings up + ingests `advisor_corpus_v01`". **Fix:** add an `ingest` phase (after `stack`, before `verify`) that ingests the **Advisor demo corpus pack** into `advisor_corpus_v01` via `fieldkit.memory` — the reference impl is `scripts/orionfold_advisor/score_recall_live.py::ingest_corpus` (900-word chunks + provenance card, NIM embeddings, 647 chunks / 183 slugs, ~2 min). Decide where the corpus pack ships from (vendor the manifest in the wheel, or pull it) — that's the one design call. Idempotent (skip if `advisor_corpus_v01` already populated).
> 3. **AD-FK-γ — `up`'s sidecar phase should start the cockpit (High).** `up.py:282-298` only health-polls `:7866` and raises if absent → `up` hard-stops; a `curl | sh` customer can't get past it. **Fix:** the `sidecar` phase starts `fieldkit arena up --no-open` itself (background process, then health-poll) — this is the code's own M2 TODO in that method's comment. Note the cockpit serves the baked UI at `/arena/` (not `/`); `--no-open` avoids the browser-launch (which fails from headless contexts anyway).
> 4. **Then: re-run the pristine-box sim to PROVE unattended.** With α/β/γ fixed, `curl -fsSL …get-orionfold.sh | OF_LICENSE_URL='file://…sample…' sh` should run **with zero manual steps** (no manual NGC export, no manual ingest, no manual cockpit start) to an all-green receipt. The sim method (sister `HOME` + fresh venv + dev-signed sample license `_IDEAS/field-edition-license-sample-v1.json`) is reusable — see memory `feedback_firstboot_sim_finds_pristine_box_bugs`. Operator pristine-prep is the same: stop dogfood `pgvector`(:5432)+`nim-embed-nemotron`(:8001) so compose owns the ports; restore + recreate them after (NB: a mistimed `docker start` during a port conflict corrupts the dogfood containers' bridge endpoints — recreate from `docker inspect` config, data survives in the `pgvector-data` volume / `/opt/nim/.cache` bind).
> 5. **Port-fidelity follow-up (operator-gated):** re-broaden `scripts/orionfold_advisor/preflight._score_output`'s refusal list (~line 391) to match `field_edition.advisor._REFUSAL_WORDS` (added `"does not include"`/`"unsupported"`/`"out of scope"` in v0.32.3) — the field_edition scorer is a "faithful port" and now diverges. This is the frozen-receipt generator, so re-running it re-freezes published receipts → **operator decision before touching it.**
>
> **Readiness (v1 default, re-confirmed 2026-06-15):** fully digest-pinned (`unpinned_images(default_config())` → `[]`), lane image `llama-server-cuda13@sha256:93993cc2…` + NIM embedder `llama-nemotron-embed-1b-v2@sha256:3c22c0bd…` + Advisor GGUF rev `dad6d1e5…` (`model-Q4_K_M.gguf`) all pinned + present on the box. The pristine-box sim's all-green receipt proves the stack itself is sound; α/β/γ are purely the unattended-orchestration gap.

**Status:** M0 locked (2026-06-19); M1 alpha target ~2026-07-17. **The entire §7+§9 installer CLI surface is code-complete AND the whole §8 `verify` battery is wired LIVE** (`fieldkit`/`advisor`/`cortex`/`lane`/`hermes` — no honest-error stubs left; `fieldkit`/`cortex`-recall/`hermes` pass stack-independent, the rest pass once the lane is up). Spec: `_SPECS/arena-field-edition-v1.md` §6–§9 (private symlink). Code track: `fieldkit/src/fieldkit/field_edition/` (public; all modules live, CLI fully wired).

- **What remains:** (1) ~~build+push+digest-pin the 2 GHCR images~~ — **DONE**; (2) ~~publish+pin a Q4_K_M Advisor rev + wire the resumable HF pull~~ — **DONE**; (3) ~~the operator-armed guided live `up` + serving-lane smoke~~ — **DONE 2026-06-15** (the first-customer sim produced the all-green receipt; `advisor`/`cortex`-grounded/`lane` all flipped to live PASS); (4) **the 3 unattended-orchestration findings AD-FK-α/β/γ — the next-session task above** (these are what stop `curl | sh` from running cold without Claude babysitting); (5) cosign-sign the cortex-embedder GHCR image once built+pinned at v1.1 (the §9 signed channel — lane image is already signed + verifiable).

- **`verify` design notes (shipped this session):** five gates `fieldkit·advisor·cortex·lane·hermes`; pure `assess_gate()` floors (Advisor curveball-v0.2 ≥80% + refusals 9/9; Cortex recall@5 ≥0.95 + contract; lane launch→gen→teardown) + pure `evaluate_gates()` verdict + clock-free `VerifyReport.receipt()`; thin I/O = injectable `GateRunner`/`LiveGateRunner` + `write_receipt`/`run_verify` → `~/.orionfold/receipts/verify-latest.json` (+ timestamped copy), written **pass OR fail**. `--json`/`--hermes`; `up --verify` now runs it as the final phase. **ALL five `LiveGateRunner` methods are now wired LIVE** (was "M2 fills the four bench methods"): `fieldkit`/`cortex`-recall/`hermes` pass **stack-independently**; `advisor`/`cortex`-grounded-half/`lane` are wired and PASS once the operator-armed serving lane is up. No honest-error stubs remain in the battery.
- ~~Cortex gate recall-half~~ — **DONE + LIVE-SMOKED 2026-06-13** (see top Current State). (a) The **frozen mini recall set is vendored** (`field_edition/data/cortex-recall-mini.json`, full 87-row slice, sha-pinned `97ce168b851d`, freeze operator-blessed) + a pure `score_recall_set()`; (b) `LiveGateRunner.cortex` measures it live via `MemoryIndex.query` against `advisor_corpus_v01` → **recall@5 0.977 ≥ 0.95 in 2.7 s**, honest non-pass (recall real, contract half pending). **Remaining for the Cortex gate to fully PASS:** only the **grounded-contract generation half** (citation-integrity + refusal-hygiene) — needs the operator-armed serving lane (the published+pinned Q4_K_M Advisor rev). That's the same lane the advisor/lane/hermes gates wait on.
- **Then: light up the live `up` (M2 infra).** Build + push + **digest-pin** the two Orionfold proven-matrix images (open embedder `ghcr.io/orionfold/cortex-embedder` + the CUDA-13/SM121 llama.cpp lane `ghcr.io/orionfold/llama-server-cuda13`), replacing their `PIN_PENDING` in `compose.py`; publish + pin a **Q4_K_M** rev of `Orionfold/Advisor-GGUF` (only Q8_0 is published) and wire the resumable HF pull into `LiveExecutor.pull`; then a real `up` runs end-to-end on the dogfood box. The `stack` phase already refuses honestly until the images are pinned. **The remaining generation-half gates `advisor`/`cortex`-grounded-half/`lane` need this operator-armed serving lane** — they are wired but only fully live-smokeable once the lane is up (the cortex *recall* half and the `hermes` MCP round-trip are the two stack-independent exceptions, both proven above/this session).
- **`up` design notes (shipped this session):** the open embedder is the §5 default (no NGC); `--nim-embedder` selects the box's existing NIM embedder (BYO key) as the alternate. Arena cockpit is **pipx, not compose** (the `sidecar` phase health-polls `:7866`). `~/.orionfold/` holds `compose.yaml` + `.env` + `state.json` (the checkpoint).
- ~~Re-pin the tested matrix before M2~~ — **DONE 2026-06-13** (`d08f8e3`): `TESTED_MATRIX` re-pinned DGX OS 7.2.3→7.4.0 / driver ≥580.95.05→≥580.159.03 (CUDA stays 13.0); §7 matrix line + prerequisites + tests + parked doc + CHANGELOG reconciled; `doctor` green live at `tested ≥ 7.4.0`. See Current State.
- **Discipline reminders:** one serving lane in 128 GB (§6 — clamp `gpu_memory_utilization` 0.60–0.85 vs *measured* free CUDA mem; AD-FK-1 infra-port exemption already covers the embedder on `:8001`). Build + smoke against the **live Arena cockpit over CDP** per the pinned Arena discipline. Keep the public docs *card* deferred (M4) — `fieldkit/docs/field_edition.md` is parked outside the website glob with a wire-at-launch banner. **Sequencing note (operator decision 2026-06-13):** Arena Field Edition is prioritized FIRST; the Advisor v0.3 road is PAUSED behind it (see "Advisor / Arena" — v0.3 is decoupled and confirmed **not a Field Edition release prerequisite**). The installer track doesn't touch the Advisor model/packet contract, so it proceeds independently.
- **Runtime prep for the `up` session:** `/tmp/fk` venv is **reboot-ephemeral** (was wiped this session; rebuilt with `pip install -e fieldkit[dev,arena,harness]` — the `harness` extra brings `mcp` for the hermes gate + harness suite); for live container work you also want Docker. Console scripts that work live now: `/tmp/fk/bin/fieldkit field-edition doctor` (green), `… up --dry-run` (writes the bundle), `… verify --hermes` (`fieldkit`+`hermes` PASS stack-independent; `advisor`/`cortex`/`lane` honest-error until the operator-armed lane is up; receipt written pass-or-fail).

### Editorial / Series

- ~~Orphan-screenshot audit + reuse~~ — **DONE 2026-06-11** (see Current State: 23→0 orphans, 5 reused on the control-plane page, kepler `positioning:` shipped, `01-cockpit.png` retaken post-Path-A).
- Write the two queued Advisor series pieces in place (promote `status: upcoming` → `published`): `gates-before-the-advisor`, `governed-routing-with-receipts`.
- Phase 2 launch (`product-writer` for the autonomous-harness cockpit) and Living-model launch posture unchanged.

### Advisor / Arena

- **⏸ PAUSED 2026-06-13 (operator) — queued behind ALL Arena Field Edition tasks for next session.** **NOT a Field Edition release prerequisite** (verified against `field_edition/verify.py`): the §8 `verify` gate floors the Advisor on **curveball-v0.2 ≥80% (v0.2 = 85.7% ✓) + refusals 9/9 ✓** and Cortex on **recall@5 ≥0.95 (0.977 ✓) + a citation-integrity `contract_pass` smoke flag** — NONE of which is the grounded-pack answer-row pass rate (18.3%) that v0.3 improves. The Field Edition ships the Advisor at its already-passing published contract; v0.3 is a model-quality uplift that flows through the §9 eval-gated update channel post-launch (the "living model" story). Soft coupling only: M2 must publish a **Q4_K_M** Advisor rev (only Q8_0 shipped) — the operator *may* make v0.3 that first Q4_K_M, but Q4_K_M of v0.2 can ship now and v0.3 update later; not a blocker.
- **REFINED DIAGNOSIS (this session, before the pause — so it isn't re-derived):** I classified all 51 answer rows of the `2026-06-12-1423` receipt and probed the live retrieval stack (pgvector + embedder up). **The packet-contract levers are smaller/harder than the original framing:**
  - **GE-2 is mostly NOT an excerpt problem.** Only **2/8** rows (`cg-lookup-0009`, `cg-lookup-0013`) are genuine excerpt-window drops. The other **6/8 already had the verbatim facts in the model's context** — it paraphrased instead of quoting and the verbatim `contains` matcher scored fail (e.g. `cg-troubleshoot-0005`: gold wants "exported, not just sourced"/"empty string"; model wrote the same meaning, different words). → model-behavior + scorer-brittleness, NOT the packet.
  - **GE-3 is a real semantic-retrieval wall.** **11/12 misses: gold IS in the 80-chunk pool but dense cosine ranks 3–19 other sources above it; 1/12 (`cg-howto-0011`) not in pool at all.** The queries are long oblique operator-phrasings that avoid the article's distinctive vocab. **Prototyped live:** naive dense+FTS RRF recovers only **1/12** and regresses some (`cg-lookup-0014` 4→19); widen-k=5 catches ~3 but changes the packet shape. A real lift needs a cross-encoder reranker or model-driven query expansion — each itself an OA-NV-8 packet swap. (NB: the FTS GIN index already exists on the table; preflight built TRAINING packets with local BM25, so adding lexical signal at serve time moves *toward* the train distribution.)
  - **Net:** ~**29/51** answer-row failures are model behavior (GE-1 over-refusal 23 + GE-2 paraphrase 6) → the real v0.3 headroom is **training calibration (step 4)**, not the packet. The "contract-before-training" rule still holds: if we change retrieval/excerpt at all, do it before SFT.
  - **Resume decision pending (operator picked "Recommended" then paused):** the options were — (A) excerpt fix only + defer GE-3; (B) excerpt fix + reranker spike; (C) re-sequence to training-first; (D) widen-k=5 + excerpt fix. Pick on resume.
- **QUEUED — the v0.3 road, in this order (recommended 2026-06-12; rationale: the lane is packet-contract-shaped, so contract changes must land BEFORE training or they invalidate it):**
  1. **Packet-contract fixes first (no training)** — see the REFINED DIAGNOSIS above (the original "9 near-miss excerpt rows / 12 GE-3 query-handling" framing is superseded). EVERY change is an OA-NV-8-style packet swap → re-gate against frozen `cortex-grounded-v0.1`, the rag-eval recall gate, AND a curveball replay before adopting.
  2. **Re-baseline on the fixed contract** — `run_pack.py --mode both`, new receipt. Expect GE-1's column to GROW as ex-GE-3 rows start retrieving and then over-refuse; that's the honest pre-training headroom number.
  3. **Freeze the successor gates BEFORE training** — curveball-v0.3 (`Route:`-prefix boundary + over-refusal probes, both directions: answer public-corpus questions MORE, keep private-state refusals at 9/9) + new grounded rows into `cortex-grounded-ext.jsonl`. Training the frozen pack's failure classes makes v0.3 class-near-distribution on it (`feedback_preregister_next_ood_gate`).
  4. **v0.3 SFT targeting over-refusal calibration — SFT-only, no RLVR** (`feedback_sft_vs_rlvr_decision`: gold sources + verbatim facts make correct trajectories cheap, cite-or-refuse is enumerable; grounded answer-row pass 2/51 is far below the RLVR headroom band anyway). Do NOT add training pressure against the refusal floor itself — 9/9 both arms is the moat; the target is calibration.
  - NOT a publishing problem: the HF card claims the curveball contract (85.7%, holds); corpus QA was never the published claim.
- ~~Grounded Eval v1 freeze~~ — **FROZEN 2026-06-12** (sha12 `c4da31a8897e`; see Current State) + **first frozen-pack receipt 2026-06-12** (`evidence/grounded-eval/results/2026-06-12-1423-*/`, 0 per-row flips vs draft — bench is stable). Failure ledger feeding the queue above: GE-1 over-refusal 22/51 · GE-2 near-miss band 9 · GE-3 retrieval misses 12 · genuinely wrong ~6. Spec basename → `EXCLUDED_SPEC_BASENAMES` at next corpus build; `evidence/grounded-eval/` never ingested into any retrieval corpus. Superseded-content refusal probes → ext set once the scorer learns refuse-or-cite-current.
- ~~AD-FK-2~~ — **FIXED `39fbe98` + LIVE-CONFIRMED 2026-06-12** (resident smoke: rows persist with the resolved recipe sid + `cross_vertical=0`, AF-28 island renders them — see Current State). Historical `local:resident` rows left as persisted.
- **AD-FK-3 (display-only, low priority)**: EvalPromptDrawer pills tag the resident lane's OWN benches `CROSS` (client `benchForLane` can't resolve resident-shaped ids — the layer the AD-FK-2 server fix deliberately skipped). Persisted truth correct; fix directions in `_IDEAS/arena-smoke-v2-features.md` session 6.
- **Path A DONE 2026-06-11** (rollup + import + bench manifest + CDP verify — see Current State). Path B (live tier, `cockpit:advisor_contract` via eval runs against the serving lane on `:8091`) stays open as the ongoing narrated-session story; AF-28 projects done `eval_rerun` jobs without a rebuild.
- **Arena hardening batch: ALL 4 items + mirror-default DONE 2026-06-11** (see Current State; frontier chart now 7 models · 29 builds, every quality-bearing model renders).
- ~~AD-FK-1 — one-lane guard vs the Cortex embedder~~ — **FIXED + LIVE-CONFIRMED 2026-06-12** (`launcher.infra_ports()` allowlist, typed `infra_port` teardown refusal, 6 tests, refused-teardown smoke; then a real guarded LaneTruth launch with the embedder up warmed in 4 s — see Current State). Workaround retired.
- Optional §10 corpus *controls* (import/swap + cockpit-driven pgvector re-ingest; read pane shipped). Optional §12 live per-query route-ledger persistence (bakeoff read surface shipped). ~~Optional v0.3 lever~~ — folded into the QUEUED v0.3 road above (steps 3–4).
- AD-AE-13 display gap stands: terminal-run wide receipts show "0 results" on the Cortex card until a card run.
- WB-11: LaneTruth can discover/pin NIM lanes but not launch them through the guarded UI.

### Orionfold GitHub migration (2026-06-12 — Phases 0–5 DONE; runbook in private repo)

- **Repo transferred:** this monorepo now lives at **`github.com/orionfold/ainative-business.github.io`** (from `manavsehgal`; GitHub leaves permanent redirects). Pages/custom-domain/Actions carried over; site verified live post-transfer. Spark `origin` repointed. `manavsehgal` = write collaborator (admin ops happen in-browser as `orionfold`).
- **History scrubbed FIRST** (leaked `_IDEAS`/`ideas` strategy files were public): `git filter-repo` across all 558 commits, force-pushed. **All SHAs from 2026-05-02 onward changed** — old SHAs referenced in this file's history/memories resolve only via the mirror + commit-map at `/home/nvidia/backups/`. Remote pruned to single `main`. Old SHAs stay fetchable on GitHub until support purge (ticket = open operator item).
- **_IDEAS share transport DECIDED + EXECUTED: private git repo** (`orionfold/strategy`, layout `<project>/_IDEAS/`). Spark: `_IDEAS` → symlink into `/home/nvidia/orionfold-strategy/ainative-business-website/_IDEAS` (gitignore pattern now `_IDEAS`, no slash — dir-only patterns don't match symlinks). Mac: cloned at `~/orionfold/strategy`. **Contract v2: pull at session start, push at session end.** Drive share FROZEN (`DEPRECATED.md` posted; delete ~2026-06-19); rclone setup no longer needed; `.claude/skills/spark-mac-share/` (untracked) retires with it.
- **Private layer EXPANDED (2026-06-12, this session):** `_SPECS` + `_GUIDES` now follow the same symlink-into-`orionfold/strategy` wiring as `_IDEAS`; the `_FLOWS/` stream is retired into `_GUIDES/`; `_RELAY.md` is the per-project cross-box mailbox (see the top Current-State entry). **Mac TODO on next pull:** mirror the `_SPECS`/`_GUIDES` symlinks on the monorepo clone the same way `_IDEAS` is wired, and delete any local `_FLOWS/`.
- **Domain + DNS tail DONE (same session, CDP-driven):** `ainative.business` **verified** on the orionfold account (TXT `_github-pages-challenge-orionfold` live); `www` CNAME → `orionfold.github.io` applied + propagated; apex 200 / www 301 confirmed. GitHub-Support cached-commit purge **declined by operator** (risk accepted: 0 forks, unguessable SHAs).
- **Mac-side actions DONE** (`mac-handoff.md` executed + deleted; the dated-root-note pattern is blessed for reuse when Spark needs Mac actions — mention it in the commit subject).
- **Open operator items:** ① Agency-cockpit desktop **monorepo**-mirror remote repoint (the cockpit's strategy-repo peer is already live per C2 — unclear if its monorepo mirror was repointed; redirects cover it meanwhile); ② founder→LLC IP assignment (legal, outside GitHub); ③ delete frozen Drive folder ~06-19.

### Operator-Owned Live Infra

- Second Brain evidence-server deploy + first `/api/knowledge/reindex` backfill still pending. PSI key still missing (blocks PageSpeed in `/seo-monitor`).
- `sudo chown` root-owned container-written dirs when needed (`merged-hf-bf16`, `init-lora-r16`, HF-cache stubs; the advisor-4b-sft GGUFs are root-owned but readable).

### SEO

- Re-run `/seo-monitor` in the settlement window; GSC token `fePoYwMX...` HOLD. New public URLs to watch for indexing: `/products/orionfold-advisor/`, `/articles/the-refusal-floor-is-trainable/`, the two HF repos.

### Cleanup

- Human-eye/Lighthouse pass on LoRA/adapter/dataset detail pages; bakeoff gated-catalog footer last-write-wins; deprecate retired `sync-field-notes` skill body; optional Claude memory-namespace symlink relocation.

## Recent Decisions

### 2026-06-10 - Marketing site follows orionfold.com; Airtable system scoped to operator panes

The Airtable-derived design system (design-system-v1) had been applied site-wide; the operator directed that it stay appropriate for the Arena app UI only, while the rest of ainative.business reads as a natural extension of the redesigned orionfold.com parent brand. Tokens were extracted from the live parent site (indigo primary, orbit gold, cool 260-hue neutrals, of-surface card idiom) and applied at the token layer — the two sites already shared structure (nav idiom, blur orbs, Geist fonts), so the realignment is recorded in `_SPECS/orionfold-brand-alignment-v1.md` and reversible via git. | Manav (with Claude)

### 2026-06-10 - Advisor public launch executed (full scope, operator-approved)

Operator approved the full public launch in one session: HF model repo `Orionfold/Advisor-GGUF` (name mirrors Kepler-GGUF precedent), HF bench dataset, product page, home teaser, flagship article + 2 series placeholders, Arena product/demo refresh. Push gate accepted the known verify_stage check-4 deviation (Methods links the product page, not a field-note — the launch's methods doc IS the product page). The HF card leads with the frozen OOD number (85.7%), not the in-distribution 28/28 — measurement-first positioning carried to the eval choice. | Manav (with Claude)

### 2026-06-10 - 4B-SFT-v0.2 promoted to the Advisor serving lane

After the SFT-v0.2 receipt battery, the operator promoted the trained 4B over the 30B on evidence: the frozen pre-registered curveball-v0.2 gate (18/21 scored==strict, refusals 9/9, 0 private-state risk vs the 30B's 8/21, 3/9, 3 risk rows — the 30B's prompt-only contract fabricated private-looking state under novel pretexts), both 28/28 wides (hinted + hint-free), and the curveball-v0.1 refusal-regression fix (15/15). The trained lane, not the prompt, carries the refusal floor. ~12 GB / warm 2 s vs ~40 GB / 14 s. The 30B stays on disk as teacher/comparison. | Manav (with Claude)
